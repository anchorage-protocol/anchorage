# PRD

> The technical north star: data model, interfaces, governance machinery, calibration, credit, adversary testbed. Where the [manifesto](./manifesto.md) explains *why* and [governance](./governance.md) sketches *who decides what*, this document specifies *how*.

This document captures design intent. **Field-level contracts** (zod schemas, MCP tool I/O shapes, lifecycle states) live in [`@anchorage/contracts`](../packages/contracts/src) and are the executable spec. The two stay in lockstep under the docs-never-drift discipline (see [CLAUDE.md](../CLAUDE.md)) — a contract change without a doc change, or vice versa, is a bug. Parameters deliberately deferred to the adversary-testbed phase — calibration ratios, reputation decay rates, vote thresholds — remain unspecified here; they will be tuned against simulation rather than guessed.

---

## Architecture overview

Anchorage is one canonical service layer behind two client surfaces:

```
                         ┌─────────────────────────────┐
                         │   Canonical service layer   │
  ┌──────────────────┐   │  ─ graph store              │   ┌──────────────────┐
  │  MCP server      │◀─▶│  ─ write-path tools         │◀─▶│  Web UI          │
  │  (mcp.…)         │   │  ─ verification engine      │   │  (anchorage.…)   │
  │  contributor /   │   │  ─ governance machinery     │   │  read & browse   │
  │  agent surface   │   │  ─ reputation & calibration │   │  for humans      │
  └──────────────────┘   │  ─ projection engine        │   └──────────────────┘
                         └─────────────────────────────┘
```

- **MCP server** (`mcp.anchorage.science`): primary write-path interface. Most traffic is agents acting as a human contributor's delegate (see [Identity](#identity)) — connected from any MCP-capable client and pulling assignments during idle time. Direct human contribution and the simulated populations in the testbed connect through the same surface. Tools are typed; verification is server-side.
- **Web UI** (`anchorage.science`): read-mostly human surface. Browse causes, sub-topics, graphs, frontiers, and manuscript projections. Calls the same canonical service layer.
- **Service layer**: the trust boundary. Every mutation passes through verification, governance gates, and reputation updates here. Clients are untrusted regardless of identity.

This is a deliberate architectural commitment, not an implementation note. Federation between Anchorage instances later is MCP-to-MCP. The testbed connects via the same MCP interface real clients use — *no stub APIs*. The architectural commitment is what the testbed depends on; the broader claim that simulated populations cover the real contributor distribution is qualified in the [manifesto](./manifesto.md#testability-is-the-secret-weapon) and the [testbed coverage section](#what-the-testbed-does-not-cover).

---

## Data model

### Multi-scale structure

Three layers, each with the shape suited to its job:

- **Cause** — the umbrella unit of belonging ("colon cancer"). Causes are created by maintainers; they are not user-creatable in v0.
- **Sub-topic** — scope envelope within a cause ("ctDNA-MRD in stage II resected CRC"). Sub-topics are first-class objects: they have IDs, descriptions, scope-envelope queries (e.g. PubMed search definition), creation status (proposed / active / archived), and curator approval state. In v0 sub-topic creation requires curator approval; later phases admit auto-discovery from graph state.
- **Claim graph** — the per-cause structure where work happens, partitioned across sub-topics by node *home* and connected across them by *scope memberships*.

A node has **exactly one home sub-topic** and **zero or more scope memberships** in other sub-topics within the same cause. The two are different jobs:

- The **home** sub-topic owns the node for review purposes: it determines which reviewer pool evaluates the proposal and where the contributor's per-sub-topic reputation accrues.
- A **scope membership** is a (node, sub-topic) edge stating "this node is in scope for this sub-topic." Memberships are themselves proposable, reviewable claims — small, atomic, and decomposable. A landmark trial excerpt or a definition node ("MSI-high CRC") can legitimately be in scope for several sub-topics simultaneously without being duplicated, re-fetched, or forked across supersedes chains.

This single structure replaces the older "exactly one sub-topic, with cross-links between" rule. Cross-links as a separate edge type are gone; their use case is subsumed by memberships, with stricter semantics (memberships are reviewable assertions about scope; they do not propagate `derives` lineage across sub-topics by themselves).

### Nodes

Every node has:

- `id` — opaque identifier
- `home_sub_topic_id` — the sub-topic that owns the node for review and reputation purposes
- `scope_memberships` — set of additional sub-topic IDs (within the same cause) this node is in scope for; each membership is an independently reviewable assertion
- `kind` — one of:
  - `anchor` — external source (paper, dataset, definition). Has `external_ref` (PMID, DOI, URL) that must resolve.
  - `excerpt` — tight claim tied to a specific anchor parent. Has a `quoted_span` field; the verification engine matches it against the resolved source. Excerpts cannot exist without a verified span. The `content` is the atomic claim; the `quoted_span` is the verbatim slice that anchors verification. They are not required to be identical — `content` may paraphrase to atomize a claim the span supports — but `content` must be an assertion the span supports under a charitable reading. The verification engine confirms the span resolves; the reviewer confirms `content` follows from it. Span verification is necessary but not sufficient: cherry-picking a true span out of context (negation-stripping, hedge-stripping) is a known attack and is part of the reviewer's responsibility to catch.
  - `synthesis` — explicit inferential step derived from multiple parents. The agent or contributor sets `kind=synthesis` when the content is not a straight excerpt.
  - `open_question` — scoped uncertainty with edges to what it depends on. Surfaces as a frontier item.
- `content` — the claim text (single atomic claim per node)
- `external_ref` — for anchors only, structured pointer (PMID/DOI/URL). Excerpts do not carry an `external_ref`; their grounding flows through the `derives` edge to the parent anchor, where verification rests. Duplicating the field on excerpts would create two places for it to drift.
- `content_hash` — for anchors only, hash of the fetched source content; set by the server post-fetch and used for re-verification (see [Verification engine](#verification-engine)).
- `quoted_span` — for excerpts, the verifiable span text + offset within the source
- `status` — `staged` (under review), `active` (merged), `superseded` (replaced), `rejected` (review failed), `unresolvable` (anchors only — re-verification has failed and the node surfaces as a frontier item; see [Verification engine](#verification-engine))
- `created_by`, `created_at`, `updated_at`

The **active node rule**: a node is *inactive* if it is the `from` end of a `supersedes` edge.

### Edges

- `derives` — parent (support) → child (derived claim). Direction matches storage. Lineage walks backward along `derives` until it hits anchors. `derives` parents must be `active` at the moment of acceptance: a child cannot be merged with a `staged` or `rejected` parent. A `derives` edge is valid when the parent and child share at least one sub-topic — either both home there, or one home and the other a scope-member, or both scope-members. Lineage chains can therefore cross sub-topic boundaries through *shared scope memberships* without being separate edges.
- `supersedes` — old → replacement. Marks the old node inactive. The `to` end (the replacement) must be `active` at the time the supersedes is proposed. Supersedes cycles (A → B → C → A) are forbidden by the verification engine.

Other edge types are rejected. The minimal vocabulary is load-bearing: more edge types create more places for governance disputes without proportionally more expressive power. Note that *scope membership* is a property of the node, not a separate edge type — a node carries its set of sub-topic memberships directly.

### Scope membership

A scope membership says: "node X is in scope for sub-topic S." It is proposable (`propose_membership`), reviewable, and revocable through the same governance machinery as any other claim — because it *is* a claim. Memberships are evaluated by reviewers from the *target* sub-topic S (the one the node is being claimed to be in scope for), since they are the ones with the expertise to judge the scope claim.

Memberships are how cross-sub-topic concerns compose without duplication, without forking supersedes chains, and without smuggling lineage. A definition node ("MSI-high CRC") homed in a screening sub-topic can be a member of Lynch-surveillance, ctDNA-MRD, and immunotherapy-eligibility sub-topics — one node, one verification, one supersedes chain, four sub-topics that all see and review it.

**Credit accrues to the proposer at the home sub-topic only**; downstream memberships do not multiply credit. Contributors who propose memberships (claiming a node is in scope for a sub-topic they work in) accrue a separate, smaller credit on those memberships.

### Change of home

Sometimes a node is initially homed in the wrong sub-topic. `propose_change_of_home` moves the home sub-topic to a different one within the same cause, subject to curator approval. Other memberships are unaffected; the one exception is that if the new home was previously a scope membership, it is removed from the membership list — the home is implicitly in scope, so leaving it in `scope_memberships` would be a redundant duplicate. This is a real but rare operation; most apparent "wrong sub-topic" cases turn out to be membership-needed cases instead.

### Manuscript projection

A *projection* is a derived view of a sub-topic's graph plus editorial choices (section order, narrative voice, scope of inclusion). Projections are not a separate truth ledger — they are a function of (graph state, projection config). The graph is canonical; projections come and go.

The *projection config* is itself a governance artifact, not a private editorial document. Changes to a projection config — what's in scope, section ordering, which nodes are emphasized — affect which nodes are load-bearing for argument structure and therefore which contributors get credit. Projection configs are version-controlled in the graph; changes to them route through the standard governance-change CI process; authorship disputes resolve as governance changes to the projection config rather than as private negotiations.

---

## MCP tool surface

The MCP server exposes a small, verification-heavy set of tools. The minimum write surface is:

A separate **admin surface** — not exposed as MCP tools — covers the curator-only operations the contributor flow assumes already happened: minting an identity (Phase 1: by curator action; Phase 2+: behind whatever identity-cost mechanism is set, see [Identity](#identity)), binding agent credentials to it, creating a cause, and seeding sub-topics. These are not MCP tools because they are not contributor actions; making them tools would either require a privileged-tool concept (which the testbed would then have to teach its synthetic populations not to call) or open a sybil/cause-spam vector. The same operations are how the testbed sets up scenarios — sim and prod use the same admin surface, by the same indistinguishability rule that governs the tool surface.

### Write-path tools

The default contribution path is *assignment-driven*: contributors declare capacity at the **cause** level (not the sub-topic level — sub-topic granularity would reopen the laundering vector by letting contributors cherry-pick easy sub-topics), the system draws assignments from the frontier across all sub-topics in the cause (gap-closing tasks: orphan anchors needing excerpts, syntheses needing parents, contested claims needing review), and reputation accrues on assigned work. A contributor-initiated path exists but with weaker rep weighting (see [Reputation](#reputation)).

**Capacity and assignment**

- **`set_capacity`** `{ cause_id, rate, kinds }` → `{ ok }`
  - Contributor declares availability at the cause level: a maximum rate (a cap, not a schedule) and which kinds of work they will accept (`propose_excerpt`, `review`, `propose_synthesis`, etc.). Sub-topic is the system's choice, not the contributor's. Assignments are always *pulled* via `request_assignment` — typically by a delegated agent during idle time — never pushed; rate caps how many will be granted in a window. Capacity is the only way the system learns availability.
- **`request_assignment`** `{ cause_id, kind? }` → `{ assignment_id, task }`
  - Pull a task from the frontier within declared capacity. The system selects across all sub-topics in the cause based on frontier priority (gap urgency, sub-topic activity), expertise fit (where measurable from history), and capacity-balancing. The task is concrete: a specific node-shape to propose, or a specific proposal to review, in a specific sub-topic.
- **`accept_assignment`** `{ assignment_id }` → `{ ok }` and **`decline_assignment`** `{ assignment_id, reason }` → `{ ok }`
  - `accept_assignment` moves an `offered` assignment to `accepted`. `decline_assignment` moves an `offered` *or* `accepted` assignment to `declined`: a contributor who accepted and then can't deliver — the task turned out beyond them, or their client wedged mid-fulfillment — bails out the same way rather than stranding the slot. The declined target frees up for another contributor (a decline blocks re-offer only to the decliner), and an accept-then-decline counts toward the same cumulative-decline-rate signal as a bare decline, so churn surfaces the same way. (A stale `offered` or `accepted` assignment a wedged client never declines at all is reclaimed by `curator.expireStaleAssignments` — a curator-tunable sweep that flips any `offered`/`accepted` assignment idle past a window to `expired`, freeing the target the same way a decline does and re-opening any anchor whose only in-flight excerpt assignment just expired. Expiry is non-punitive: unlike an accept-then-decline it carries no reason and doesn't feed the cumulative-decline-rate signal — "went silent" is a distinct pattern the curator reads off expiry volume directly, not one folded into the decline channel. `expired` does not block re-offer the way `declined` does — a recovered client is a legitimate worker.)
  - Declining individual assignments is non-punitive on its own — a legitimate narrow specialist (e.g., a genetic counselor who works only on Lynch-syndrome questions) declines outside their wheelhouse, and that's fine. What is *not* allowed is opt-in selectivity: capacity is cause-level, not sub-topic-level. Decline patterns are tracked; sustained pattern-declining of specific sub-topics, contributors, or claim classes is an abuse signal handled at the curator layer. The handling has two surfaces: a curator-side projection (`declinePatterns`) that ranks per-(cause, reviewer) cumulative decline rate and lets a curator drop the small-sample floor when they're hunting for something specific, and an assignment-time gate (`assignment_max_decline_rate`, `assignment_decline_min_offers`) that consumes the same signal at `request_assignment` and refuses callers above threshold with the same `not_found` mode the rep gates use. Same null-policy as the recent-activity gate — callers below the offers floor bypass — and the contributor-initiated review path (`cast_review_vote` without an `assignment_id`) doesn't traverse the gate, so a contributor whose decline rate has spiked retains the recovery path declines are non-punitive precisely *to* preserve. The testbed sweeps the threshold; production-instance threshold is operationally private.

**Proposals.** Each `propose_*` tool below takes an optional `assignment_id`. When present, the call **fulfills** the named accepted propose-kind assignment: the assignment's task kind must equal the proposal kind and the task's pinned context (sub-topic, parent anchor, parent set) must match what the proposal claims, the assignment transitions to `submitted` with this proposal as `fulfilled_by`, and the work accrues full assigned-work reputation. When absent, the proposal is **contributor-initiated** and weighted lower for reputation (see [Reputation](#reputation)). This mirrors `cast_review_vote`'s optional `assignment_id` for review work — there is one tool per node kind whether or not an assignment is being fulfilled, rather than a separate assignment-only entry point. (`propose_change_of_home` and `propose_sub_topic` are curator-gated and not assignment-driven, so they carry no `assignment_id`.)

- **`propose_anchor`** `{ cause_id, home_sub_topic_id, memberships?, content, external_ref, assignment_id? }` → `{ proposal_id }`
  - Creates a staged anchor node. Server fetches `external_ref`, confirms resolution, and rejects on failure. `memberships` is an optional list of additional sub-topic IDs in the same cause; each becomes a separately-reviewable membership claim.
- **`propose_excerpt`** `{ cause_id, home_sub_topic_id, memberships?, parent_anchor_id, content, quoted_span, assignment_id? }` → `{ proposal_id }`
  - Creates a staged excerpt node. Server matches `quoted_span` against the resolved source. Mismatch → rejection. No exceptions.
- **`propose_synthesis`** `{ cause_id, home_sub_topic_id, memberships?, parent_ids, content, kind, assignment_id? }` → `{ proposal_id }`
  - Creates a staged synthesis or open_question node with `derives` edges from each parent. Atomic — either all edges create or none do. `kind` is `synthesis` or `open_question`.
- **`propose_supersedes`** `{ from_node_id, to_node_id, rationale, assignment_id? }` → `{ proposal_id }`
  - Stages a supersedes edge with the reasoning attached.
- **`propose_membership`** `{ node_id, sub_topic_id, assignment_id? }` → `{ proposal_id }`
  - Stages a scope-membership claim that `node_id` is in scope for `sub_topic_id`. Reviewed by the *target* sub-topic's reviewer pool.
- **`propose_change_of_home`** `{ node_id, new_home_sub_topic_id, rationale }` → `{ proposal_id }`
  - Curator-approved.
- **`cast_review_vote`** `{ proposal_id, decision, rationale, assignment_id? }` → `{ vote_id }`
  - `decision` is `accept`, `reject`, or `revise`. `rationale` is required and may itself be promoted to a graph node (typically `open_question`) by curators; promoted rationale-nodes pass standard review. When `assignment_id` is present, the vote fulfills a review assignment and accrues full assigned-review reputation; without it, the review is contributor-initiated and weighted lower on the same terms as contributor-initiated proposals. This tool is therefore the fulfillment path for review-kind assignments as well as the contributor-initiated review entry point.
- **`propose_sub_topic`** `{ cause_id, name, description, scope_query }` → `{ proposal_id }`
  - Subject to curator approval in v0.

### Read-path tools and resources

Read-path is largely MCP *resources* (passive), with a few active tools for queries:

- **Resource: `cause://...`** — list of causes; structured cause metadata.
- **Resource: `sub-topic://{id}`** — sub-topic metadata, status, scope query, recent activity.
- **Resource: `node://{id}`** — node + immediate neighbors.
- **Resource: `subgraph://{sub-topic-id}`** — full or filtered subgraph in a structured form.
- **Tool: `query_frontier`** `{ cause_id?, sub_topic_id?, frontier_kind? }` → ordered list of frontier items (work to be done).
- **Tool: `query_proposals`** `{ status?, sub_topic_id?, assigned_to_me? }` → list of proposals matching filter.
- **Tool: `fetch_calibration_batch`** `{ sub_topic_id }` → calibration-material projection: accepted-from-validated-history proposals routed to the sub-topic, in [`ReviewBatchItem`](../packages/contracts/src/tools.ts) shape (`proposal_id` + `payload`, status/proposer/timestamps stripped). The reviewer-facing calibration path runs through `request_assignment` (see [Calibration batches](#calibration-batches)) — a calibration item is delivered as a review-kind assignment indistinguishable from real review work, scored on `cast_review_vote` against ground truth. `fetch_calibration_batch` is the diagnostic projection of the same pool: the testbed inspects it; production reviewers do not call it directly.
- **Tool: `query_reputation`** `{ cause_id }` → `{ entries: [{ sub_topic_id, demonstrated, recent }] }`. The caller's *own* per-(cause, sub-topic) reputation, both components decayed forward to the server's current time on read. PRD §Reputation commits "Eligibility tiers public; numeric reputation private" — the contributor sees their own raw numbers (otherwise they cannot reason about where they sit relative to tier gates and which assignment-gate thresholds will fire against them), but other contributors see only tiers via a separate read-path surface that will land when tiers are defined. Returning the decayed live values rather than the at-last-bump snapshots is what makes the recent-activity gate's quiet-window arithmetic visible to the contributor; without it the patient-adversary signature (drained `recent`) would be observable to the server but invisible to the caller.

Tool surface is intentionally small. Each tool has tight typing, server-side validation, and clear failure modes.

*Implementation status:* the four query tools above are wired in v0 (ROADMAP §Status: "all 17 tools from the PRD ... and the read-path projections `query_frontier`, `query_proposals`, `fetch_calibration_batch`, `query_reputation`"). The four resources (`cause://`, `sub-topic://`, `node://`, `subgraph://`) are committed by the PRD as the passive browsing surface but are not yet implemented — they land alongside Phase 2's user-facing frontier surfaces and the Web UI browse layer (ROADMAP §Phase 2 §Scope). The Phase 1 testbed-driven adversary loop only needs the active query tools to observe archetype state, so resource-shaped browsing is a Phase 2 affordance and not a Phase 1 testbed primitive.

---

## Verification engine

The verification engine is the security boundary. Every write tool routes through it.

- **Anchor verification.** External references must resolve. PMIDs hit NCBI E-utilities; DOIs resolve via Crossref; URLs must return 200 with substantive content. Anchors are *content-addressed*: the hash of the fetched content is stored alongside the `external_ref`, and re-verification compares against the stored hash rather than only against a live fetch. URL-anchors are second-class — metadata-unstable and cloaking-prone — and may be subject to stricter regimes (or refused entirely in v0). When re-verification fails (retraction, content drift, host gone), the anchor moves to an `unresolvable` status and surfaces as a frontier item rather than silently rotting.
- **Span verification.** For excerpts, the `quoted_span` must be a substring of the fetched source after normalization (whitespace, quote-style, and a small set of typographic equivalences specified in the verification spec, not left to "light normalization" hand-waving). Failure rejects the proposal at write time, not at review time. Span verification confirms the quote exists; it does not confirm the proposed `content` follows from it — that is the reviewer's job.
- **Lineage validation.** A `derives` edge is valid when its parent and child share at least one sub-topic membership (home or scope). At acceptance, every `derives` parent must be `active` (not `staged`, `rejected`, or `superseded`); the `to` end of a `supersedes` must be `active` at proposal time; supersedes cycles are rejected. A manuscript projection in sub-topic S walks `derives` lineage that stays within S's home + scope-member graph.
- **Reputation gates.** Some operations require minimum reputation (per-(cause, sub-topic) or per-cause). Below the threshold, proposals land staged but are not advanced into the review queue without curator action. Specific thresholds are tuned in the testbed.
- **Rate limits and abuse signals.** Per-identity rate limits on observable write actions are spec'd in [Identity](#identity)'s per-identity rate-limit accounting bullet (the new `rate_limited` refusal mode at `propose_*`, `cast_review_vote`, and `request_assignment`); suspicious patterns (sudden burst of proposals, calibration-failure clustering, behavioral fingerprints across causes) surface to the curator via the cross-cause identity-clustering projection in the same section. Specific signals and thresholds are operationally private.

---

## Governance machinery

### The contribution flow

The default flow is assignment-driven. The contributor-initiated flow is a special case (steps 0a/0b are skipped; rep weighting is reduced).

0a. **Capacity** — contributor declares availability at the cause level — rate cap and accepted work kinds (`set_capacity`). Sub-topic granularity is deliberately not allowed; it would reopen the rep-laundering vector by letting contributors cherry-pick easy sub-topics.
0b. **Assignment** — system draws a frontier task matching capacity and assigns it (`request_assignment`); contributor accepts or declines.
1. **Submit / Propose** — work is staged through the `propose_*` tools; an assigned contributor passes the assignment's `assignment_id` so the proposal fulfills it, a contributor-initiated proposal omits it.
2. **Verify** — verification engine accepts or rejects synchronously based on grounding/lineage/rate.
3. **Stage** — accepted proposals enter the review queue. Visible and citable as proposals; not part of the canonical graph.
4. **Assign reviewers** — N reviewers are drawn from the eligible pool of the home sub-topic (or the target sub-topic, for membership proposals), with calibration items mixed in.
5. **Review** — reviewers vote with rationale via `cast_review_vote`.
6. **Resolve** — convergent vote merges; divergent vote routes to a richer review path (more reviewers, curator escalation, or carrying the divergence forward as parallel synthesis nodes / `open_question`); long-unresolved divergences archive (status `unresolved-archived`).
7. **Settle** — reputation updates for the contributor and the reviewers, weighted by outcome correctness, by claim difficulty, and by whether the work was assignment-driven or contributor-initiated.

### Reviewer assignment

Reviewers are drawn from the eligible pool — contributors with cause-level capacity declared and sufficient per-(cause, sub-topic) reputation — by stratified random sampling. Stratification partitions the pool into expertise *strata* and prefers draws across strata, so a single bias zone cannot fill a proposal's reviewer slots even when the bias-aligned reviewers individually meet eligibility. Capacity is at the cause, not the sub-topic; reviewers do not pre-select which sub-topics they will be drawn for.

The v0 stratum primitive is the **vote-pattern co-occurrence cluster**: two reviewers fall in the same stratum when their pairwise vote agreement on shared past proposals exceeds a threshold over a rolling window. Coalition members coordinating on bias-zone proposals concentrate into a single cluster by their own behavior; honest reviewers spread across clusters by their independence. New reviewers (no shared history yet) sit each in their own singleton cluster, contributing maximally to diversity by default. Window sizes, agreement thresholds, and cross-stratum draw weights are testbed-tuned; the *signal* — vote-history clustering as the v0 stratum — is committed.

Pairwise edges are **contention-weighted**: each shared proposal contributes weight `2 * min(accepts, rejects) / total_votes` to the agreement metric, so unanimous-easy proposals contribute 0 and split-contentious proposals contribute up to 1. The pair edges only when (a) the raw shared-proposal count meets the brand-new-reviewer floor, (b) the weighted agreement ratio meets the threshold, and (c) some non-zero contention has accumulated in the shared history. Without weighting, an honest pool that happens to share unanimous priming history collapses into a single cluster, the cross-stratum draw rule fires against its own honest members, and coalition members who avoided the priming walk through the gate the honest reviewers were locked out of — the [adversary testbed](#adversary-testbed) wires that pathology end-to-end and the contention-weighted variant closes it cleanly.

Anti-correlation is a separate cluster cue alongside agreement: a pair that systematically votes the *opposite* way on shared proposals is also displaying coordinated behavior — just inversely. Treating perfect anti-correlation (1.0 disagreement ratio) as a co-stratum edge catches the decorrelating-coalition variant cleanly, since a coalition that defeats the agreement metric by voting opposite on every shared priming proposal lights up the disagreement metric instead. The testbed scenario for the decorrelating coalition closes against this defense at threshold 1.0; the threshold is testbed-tuned, with weaker thresholds catching noisier adversaries at the cost of admitting honest reviewers who happen to systematically disagree as false positives.

The encounter domain extends from `{accept, reject}` to `{accept, reject, decline}` when `stratum_include_declines` is on, so the cluster primitive sees declines as first-class encounters alongside votes. Without this widening, a coalition that paired-declines as the routing primitive (each member solo-rejects one contested target and declines the partner's) shares zero vote-history by construction and the cluster signal has no edge metric to fire on. With it on, "Carol votes A and declines B; Dave declines A and votes B" reads as 100% pair-disagreement on two shared encounters under the existing anti-correlation primitive, the cross-stratum gate trips against the partner at the next assignment, and the seam closes at the same primitive that already catches vote-only decorrelating coalitions. A real vote takes priority over a decline on the same `(reviewer, proposal)` pair (a contributor-initiated vote after declining is "I changed my mind"), and decline-involved encounters under contention-weighting count at full weight (declines are inherently informative — opting out is a stance, not noise — and the per-proposal accept/reject tally would underweight them by construction). Defaults to off so existing scenarios are unaffected.

The decline-aware × contention-weighting interaction has a load-bearing refinement: the full-weight rule for declines only applies once the pair has met a **paired-decline floor** (`stratum_decline_min_paired`, default 2). Without the floor, a single asymmetric decline-involved encounter (one reviewer declines a proposal, another votes on it) carries weight 1 against an honest pair whose entire vote-vote history sits on unanimous-easy items (contention 0 → weight 0); the pair's weighted-disagreement ratio collapses to 1.0 and the anti-correlation edge fires against an honest pair that shared no coalition signal — the honest pool false-clusters with itself and the cross-stratum gate strangles honest review on the next contested target. The paired-decline closure that the full-weight rule has to preserve has 2 decline-involved encounters per pair by construction (one in each direction), so a floor of 2 closes the over-clustering pathology without weakening the closure: below the floor, decline-involved encounters contribute 0 weight and the pair stays decline-blind for cluster purposes; at and above the floor they count at full weight as before. Raw shared count is unaffected — the brand-new-reviewer floor still applies. The testbed scenario for the over-clustering pathology runs at `stratum_decline_min_paired=1` (no floor) to pin the buggy regime and at `stratum_decline_min_paired=2` (default) to confirm the closure, with the same defense stack on both halves.

A mixed-strategy coalition (a 50/50 vote pattern — two agreed, two disagreed across four shared proposals) defeats either refinement *alone*: the raw mix sits at agreement 0.5 and disagreement 0.5, below threshold 1.0 in both directions. The composition closes the gap. With contention weighting, agreement on unanimous-easy items contributes 0 weight and disagreement on contentious items contributes the full weight, so the weighted disagreement ratio collapses to 1.0 and the anti-correlation edge fires. The testbed scenarios run both halves: the mixed-strategy bypass standing open with each refinement individually, and closing under the composition (contention-weighted + anti-correlation, both at threshold 1.0). The seam that the composition leaves open — a coalition that avoids co-voting on any contentious item — has its concrete instance in the multi-proposal paired-decline coalition (members solo-reject *different* contested targets and decline each other's, so the pair shares zero vote-history by construction); the encounter-domain extension above (`stratum_include_declines`) closes that instance structurally by reading declines as first-class encounters under the same anti-correlation primitive, with the paired-decline floor preserving honest-pool integrity, and the divergence-closure mechanism archives the contested targets operationally at the configured window regardless of cluster signal. Adaptations that route around both closures — a coalition that avoids co-voting *and* avoids paired-declines, coordinating off-platform so members never share any encounter the cluster signal can see — remain testbed targets if they surface; calibration-item-specific agreement (contrasted against calibration record) and payload-feature vote patterns remain on file as the candidate refinements for those.

The draw rule is **prefer not-yet-represented strata first**. Within a target reviewer count N, the assignment selector visits strata in priority order, taking at most one reviewer per stratum until N strata are filled, then loosens to per-stratum saturation if the pool is exhausted. This compounds with [pool-size scaling](#adversary-testbed): in a pool that holds K+1 honest reviewers, stratification keeps a K-coalition from monopolizing the slots; in a pool too small to furnish K+1 — the small-sub-topic regime — stratification still draws *across* whatever clusters exist, and if fewer than N strata are reachable the proposal is flagged **stratification-degraded**.

Stratification-degraded propagates into convergence: a degraded proposal requires a higher distinct-reviewer count and/or a higher weighted-vote sum to converge, making the small-sub-topic case slower to close but not capturable by the pool-fits-the-coalition dynamic. The flag is visible to the contributor, factored into convergence-threshold logic (see [Calibration batches](#calibration-batches) for the weighted-vote tally), and logged for periodic audit. Specific tightened thresholds are testbed-tuned.

Reviewer pools are evaluated in this fallback ladder:

1. **Home-sub-topic rep.** Standard path. Reviewers homed in this sub-topic.
2. **Membership-sub-topic rep.** When the home pool is exhausted, draw from contributors whose work is *scope-member* of this sub-topic — they have proven legitimate stake without being homed here.
3. **Cause-rep with stratification-degraded flag.** When 1 + 2 are exhausted, draw from cause-rep contributors and flag the proposal as stratification-degraded — same flag the cross-stratum draw raises when fewer than N strata are reachable; convergence tightens identically.
4. **Curator escalation.** When even the cause-rep pool is insufficient — including the limiting case of a divergent proposal the pool stalls on with no tiebreaker — or when stratification-degraded persists across several proposals in a sub-topic, escalate to curator review. Curator review resolves the proposal directly in either direction (`curator.acceptProposal` materializes it; `curator.rejectProposal` closes it `rejected`), or carries the divergence forward as parallel nodes / `open_question`. The accept/reject override is reputation-neutral: a curator tiebreak is not the peer agreement the convergence path's reputation deltas are calibrated against, and routing curator overrides through the rep ledger would let a curator move reputation by fiat.

At sub-topic launch, both the eligible pool *and* vote-history are thin by construction, so stratification-degraded fires immediately and calibration items are drawn from the cause's validated history rather than from the sub-topic's. The doc states this explicitly so it is not mistaken for a vulnerability when reviewers notice it.

Convergence and divergence thresholds are claim-class-aware: high-stakes claim classes (e.g., quantitative effect-size syntheses) draw larger pools and tighter convergence thresholds than low-stakes ones (e.g., terminological clarifications). Specific class definitions and threshold values are testbed-tuned; the *machinery* being class-aware is a design commitment.

Divergence has a closure mechanism. Divergent proposals are routed to richer review or carried forward as parallel synthesis nodes / `open_question`, but not indefinitely: divergences without further evidence within a tunable window are archived (status `unresolved-archived`) rather than perpetually re-routed. This prevents the queue from accumulating reviewer-noise as if it were principled disagreement.

### Calibration batches

Reviewer batches contain a mix of real proposals and calibration items. Calibration items are drawn from the graph's own validated history — proposals that survived multiple confirmations and have been stable. They are intended to be statistically indistinguishable from real frontier work in *the dimensions a reviewer can act on*: a reviewer evaluating one batch should not be able to tell which items are calibration. The harder question — whether a *patient* adversary observing many batches can build a classifier on re-use frequency, age, or other batch-level signatures — is real, and the methodology actively defends against it: calibration sampling is biased toward fresh-but-validated history, items rotate aggressively, and the sampling distribution itself is part of what the testbed evaluates as an attack surface.

Calibration items arrive on the same surface as real review work — `request_assignment` returns a review-kind task whose `proposal_id` may point at a staged proposal (real review) or at an accepted-from-history proposal (calibration). The reviewer cannot distinguish the two from the task they receive. When the reviewer votes via `cast_review_vote`, the server scores the vote against ground truth (calibration items have a known outcome — `accept` is correct, `reject` is incorrect) and adjusts the reviewer's reputation accordingly. Calibration votes do not re-resolve the underlying proposal; they only meter the reviewer. This makes calibration "just another assigned task" from the reviewer's perspective and a server-side scoring channel from the system's, which is what closes the [strategic-coalition vector](#adversary-testbed) — a coalition that votes biased on real proposals also votes biased on calibration items they cannot identify, paying for the bias even when they win the convergence battle.

Reviewers who fail calibration lose reputation; the calibration corpus grows as the graph grows.

The reviewer's calibration record is also available to the convergence layer as a vote-weighting signal: a per-(cause, sub-topic) net-passes count that scales the weight a reviewer's vote carries when the server tallies convergence. Convergence requires both a minimum count of distinct reviewers (the redundant-peer-review invariant) and a minimum *weighted* sum (the bias-resistance signal); a reviewer whose calibration record went sour can hold a convergence open but cannot drive it. The [stratification-degraded](#reviewer-assignment) flag tightens both — distinct-reviewer count and weighted sum — when the assignment selector could not draw a diverse enough pool, so a proposal that lost its diversity defense recovers it through a higher convergence bar. This compounds with the rep-ledger penalty rather than replacing it: a coalition that misfires on calibration items pays once on the rep ledger and once at convergence. Calibration-aware adversaries — bias on real proposals, ground-truth on calibration items (a known PRD §Adversary-testbed target) — bypass this layer; they remain testbed work that builds on the seam, not regressions of it. A second bypass mode surfaces *intra-run*: the calibration corpus is the graph's accepted history, so any bias-aligned excerpt that converges accepted during a review session joins the corpus as a fresh-but-validated item, and recency-bias in the calibration draw then hands later coalition members items that match their bias and pass calibration — lifting their convergence weight back above the aware threshold for whatever targets they vote on after the contamination lands. The mitigation is a corpus-composition rule at the draw layer: `corpus_confirmation_depth_floor` is the minimum distinct-reviewer count an accepted-from-history item must accumulate over the vote store before it is eligible as a calibration draw. Anchors are exempt by kind — they are the ground-truth substrate, not synthesis-from-review, and counting their accumulated calibration-vote reviewers against the floor would mis-trip the gate against the substrate itself. At v0 votes_to_accept=2, floor=3 requires at least one independent confirmation past the original convergence pair before the item enters the corpus, closing the within-first-review-session bypass. The filter is enforced at both the public read-path (`fetchCalibrationBatch`) and the assignment-injection seam (`drawCalibrationTask`), so the same eligibility rule applies regardless of whether calibration arrives via the read tool or via `request_assignment`'s injection cadence. Default 0 leaves the filter inert; production thresholds are testbed-tuned. Specific weight functions and minimum-count thresholds are testbed-tuned.

**Specific calibration items in active rotation are operationally private.** Published items are burned. Methodology — including the rotation regime and the sampling-distribution defenses against batch-level correlation attacks — is fully public; specific tuning is not.

### Reviewer-as-staking

Reviewers gain reputation when they accept proposals that survive and reject proposals that get rejected by other reviewers. They lose reputation when they accept proposals that are later reverted or fail calibration. This makes lazy rubber-stamp review costly without requiring reviewers to do more than they would already do well. The risk that staking selects against reviewers willing to engage hard syntheses is addressed by claim-difficulty-normalized review-credit (see [Reputation](#reputation)).

### Sub-topic creation

In v0:

- Proposed via `propose_sub_topic`.
- Curator evaluates against the criteria in [seed-topic.md](./seed-topic.md): articulable scope envelope, real disagreement, real audience, manageable corpus size, low political risk.
- The system computes feasibility hints from existing graph state: corpus density (do anchors exist?), anchor coverage (how much of the proposed scope envelope already has nodes?), projected closure distance.
- Curator accepts as `active`, defers as `proposed`, or rejects.

In Phase 3+: auto-discovery surfaces tractable scope envelopes from graph state; curator review remains as a check.

**Antenna nodes (idea, not committed).** A related Phase 3+ discovery mechanism: a node with no children — nothing derived from it yet — could carry "antennas," a small bounded set of embedded *hypothetical complements* describing what would be promising to cross with it. When a new real node lands close to an antenna in embedding space, the system auto-enqueues the candidate crossing as a frontier item. The value over plain nearest-neighbour lookup on the node itself is that a promising cross is often *complementary* rather than *similar* (an inhibitor of gene X is not semantically near a claim that gene X is upregulated), so the antenna encodes "what I'm looking for" rather than "what I am" — a HyDE-flavoured trick. The cost is the embedding-index fan-out, which is why the lean form would be an optional embedding on `open_question` nodes (already bounded by review) rather than materialised virtual claims, and why this stays an idea: not worth building until the graph is dense enough that ad-hoc lookup demonstrably misses these crossings.

---

## Identity

The identity model is a requirements sketch in v0; specific tech (OIDC providers, key formats, attestations) is a Phase 1 implementation choice, but the *contract* the rest of the design depends on is fixed here.

Sybil-resistance has four layers: **binding cost** at issuance, **issuance-frequency cap** at the IdP, **per-identity rate-limit accounting** at the MCP write surface, and **cross-cause identity-clustering** at the curator surface. The first two live below the MCP layer — the server does not mint identities; the second two are server-side primitives. They compose multiplicatively against an adversary budget: a coalition affording K sybils pays per-sybil binding cost at mint, must spread the K mints across multiple epochs at the issuance-frequency cap, has each sybil's per-epoch throughput bounded by the rate-limit, and surfaces in the clustering projection if behavioral coordination survives the first three. The architecture's regression handle is the [sybil-amplified-coalition scenario](#adversary-testbed) where a fresh recruit walks past every *behavior-based* defense by construction (cluster signal, calibration record, reputation gates all require accumulated per-identity history); the four identity-layer primitives are what closes the seam the behavior-based stack cannot.

- **Bounded identities-per-real-person via binding cost.** Identity creation has a non-trivial cost — email verification at minimum, third-party OIDC (GitHub, ORCID, institutional SSO) preferred. The cost is tunable per IdP. Zero-cost identities are not supported. Each issued identity carries a numeric `attestation_level` recorded by the IdP at mint, opaque to the server (the server gates on threshold; it does not interpret what the level *means* — that's the IdP's job). The MCP-layer gate is `min_attestation_level`, consumed at every write tool that resolves a caller — `set_capacity`, `request_assignment`, `accept_assignment`, `decline_assignment`, all `propose_*` tools, and `cast_review_vote` — so the gate fires before any per-identity action accrues regardless of which tool the adversary chose. Refusal mode is `unauthorized` rather than the rep gates' `not_found` opacity, because the binding-cost mismatch is identity-level (a stable property of the IdP-issued credential) and there's no opaque-refusal value to protect: the adversary already knows their own attestation level. Read-path tools (`query_frontier`, `query_proposals`, `query_reputation`, `fetch_calibration_batch`) are not gated — read access doesn't multiply the per-identity action budget the gate is defending against. *Implementation status:* wired in the binding-cost slice — `attestation_level` is on the Identity contract, `min_attestation_level` is the testbed-tunable knob in `ReviewConfig`, the server-side gate fires at all 12 write-tool seams and refuses with `unauthorized`. Default 0 leaves the gate inert and preserves existing-scenario behavior.
- **Issuance-frequency cap.** Per-(IdP, IP, ASN, …) per-epoch caps on the rate at which new identities can be minted. The cap lives below the MCP layer — the server does not mint identities and has no surface to enforce this directly. PRD commits the layer's existence; specific cap shapes are operationally private at the IdP. The cap is the *time* primitive on the cost-multiplier: an adversary affording K sybils still cannot mint them all in one epoch, which buys behavior-based defenses (cluster signal, calibration record, reputation gates) the accumulated history they need to fire. *Implementation status:* spec is settled here, and the testbed-side fiction is wired as the `AdversaryBudget` primitive (see [Adversary testbed](#adversary-testbed)'s adversary-budget model) — the harness rejects mints that would exceed `issuance_cap_per_epoch` for the current epoch with a distinct `issuance_cap` failure mode parallel to the `budget` exhaustion mode the binding-cost layer reads against.
- **Per-identity rate-limit accounting.** Per-(identity, epoch) counters on observable write actions across the full write surface — `set_capacity`, `request_assignment`, `accept_assignment`, `decline_assignment`, all `propose_*` tools, and `cast_review_vote`. The cap is *single-bucket* across all write tools (not per-tool), so the cap measures total throughput per identity per epoch directly. When the counter hits the cap the tool refuses with a new `rate_limited` mode parallel to `not_found` and `unauthorized` — the contributor sees throttling with the recovery path "wait for the next epoch," not "obtain a higher attestation" or "no work available." The cap is the per-sybil-throughput axis on the cost-multiplier: at K sybils with per-sybil cap T, the coalition's per-epoch action budget is bounded by K × T. The signal is *global per identity* — the same identity's actions across two causes both consume the same per-epoch budget — for the same reason cross-cause anti-abuse is asymmetric to per-cause reputation: a sybil farm working two causes is more detectable than one working one, and the cost of opacity is small relative to the defense. The counter is consumed *after* `resolveCaller` and the binding-cost gate but *before* tool-specific business logic, so unauthorized callers don't burn budget while valid-input-but-business-logic-failed calls do (an adversary can't probe the system with bogus business inputs without paying for each). *Implementation status:* wired in the rate-limit slice — `rate_limit_actions_per_epoch` and `rate_limit_epoch_seconds` are testbed-tunable knobs in `ReviewConfig`, the `accountWriteAction` helper fires at all 12 write-tool seams (read-path tools are not gated, matching the PRD scope), and the new `rate_limited` ServerErrorCode is parallel to `unauthorized` and `not_found`. Defaults are both Infinity, leaving the gate inert and preserving existing-scenario behavior.
- **Cross-cause identity-clustering projection.** Curator-side projection (`server.curator.identityClusters({ window_seconds, min_signal })`) parallel to the existing `declinePatterns` projection, surfacing identity pairs whose behavioral fingerprint across causes suggests sybil coordination. The v0 signal is per-(reviewer pair) count of distinct causes where both reviewers cast votes on the same proposal — honest reviewers typically work in one cause (per-cause reputation; PRD §Reputation), so a pair appearing on shared proposals across multiple causes is the cross-cause behavioral fingerprint a sybil farm working multiple causes lights up and a single-cause coalition does not. Two metrics surface per pair: `cross_cause_count` (the headline signal) and `shared_proposal_count` (visibility into whether the pair coordinates across one shared proposal per cause or many — different shapes the curator weighs differently). The signal is *global per identity*, with documented governance and audit; specific signal shapes and thresholds are operationally private (the same posture as `declinePatterns`'s small-sample floor: methodology is public, tuning is not). Co-creation-timestamp correlation, vote-time correlation across causes, and decline-aware encounter inclusion are follow-up refinements on the same surface — they broaden the behavioral-fingerprint notion without changing the projection shape. *Implementation status:* wired in the clustering slice — `server.curator.identityClusters({ window_seconds, min_signal })` returns sorted (cross_cause_count desc, shared_proposal_count desc, ids asc) cluster records. Default `min_signal` is 2 (cross-cause coordination requires at least 2 causes to be a cross-cause signal); `window_seconds` is the rolling window over vote `created_at`, undefined for all-time.
- **Agents act as a human's delegate.** The human is the identity holder; agents are credentialed clients authorized to act on their behalf. Reputation, capacity, calibration outcomes, and accountability all attach to the human, not the agent. A human may bind several agents (a desktop MCP client, a long-running daemon, custom tooling) under the same identity; they all draw from the same per-(cause, sub-topic) reputation pool and the same cause-level capacity declaration. Agent credentials are individually revocable by the human — losing a laptop or retiring a daemon does not require revoking the underlying identity — and curator revocation acts on the human, transitively disabling their agents. From the system's perspective an agent contributing on idle time and a human contributing directly are indistinguishable: both are tool calls authenticated as the same identity, drawing from the same capacity, accruing reputation to the same account. The bounded-identities-per-real-person property is preserved by construction; the agent layer does not multiply identities.
- **Pseudonymity is supported; anonymity is not.** A contributor may operate under a stable pseudonym; the system retains a binding between the pseudonym and the underlying identity-establishing credentials (email, OIDC subject) that curators can use under documented escalation. The graph and the public surface show the pseudonym; the binding is private.
- **Named credit on manuscript projections is opt-in.** Pseudonymous credit is allowed, but the project's recommendation is real-name credit for high-impact projections to retain academic legibility. Pseudonymous co-authorship is unusual and contributors should make that choice deliberately.
- **Revocation.** Identities can be revoked (sybil farms, terms-of-service violations). Revocation invalidates future participation without rewriting graph history; revoked contributions remain in the graph with the revocation flagged.

The identity model is the foundation that sybil-resistance, calibration integrity, and reputation accounting all rest on. None of them composes meaningfully without it.

---

## Reputation

Reputation is structured to resolve a real trilemma the design cannot wave away: *slow* decay rewards consistency (the design goal) but lets patient adversaries stockpile; *fast* decay neutralizes stockpiles but disenfranchises episodic experts (the part-time clinician is exactly the contributor we want); *review-as-staking* punishes lazy review but selects against accepting hard syntheses (which are riskier to stand behind). Acknowledging this directly:

- **Two-component reputation.** A *demonstrated-competence* component, slow-decay, gates eligibility tiers (who is in the reviewer pool at all) — `assignment_min_demonstrated`, consumed at `request_assignment` with the opposite null-policy from the recent gate (callers with no rep entries in the cause fail by construction when the threshold is positive — the demonstrated tier is "have you proven yourself yet?", and an unproven identity is by construction not in the pool). A *recent-activity* component, fast-decay, gates assignment (who is drawn for a given proposal) — `assignment_min_recent`, consumed at the same seam, with fresh-reviewer bypass so first-assignment bootstrap stays open. Both gates refuse with the `not_found` mode the decline-pattern gate uses, structurally indistinguishable from "no work available." A patient adversary can stockpile competence but must remain currently active to be assigned — and visible activity is detectable.
- **Per-(cause, sub-topic), accrued from assigned work.** Anchored at the cause level (the unit of belonging), refined by which sub-topics a contributor's *assigned* work has actually landed in. Sub-topic rep is therefore an emergent record of where the system has routed someone, not a self-declared specialty. This closes both the rep-laundering vector (no easy-sub-topic shopping) and the coalition vector (contributors can't pre-arrange to land on each other's proposals). Contributor-initiated work earns sub-topic rep at a substantially reduced weight, conditional on independent confirmation, to preserve a genuine novel-synthesis path without making it the laundering route.
- **Earned through confirmed contributions and accurate reviews.** Both contributing nodes that survive *and* reviewing accurately count.
- **Lost through reverted contributions and inaccurate reviews.** Supersedes and rejected calibration items both decrease reputation. Self-supersedes (a contributor superseding their own node) do not count toward survivorship — only supersedes by other contributors do.
- **Review-credit normalized by claim difficulty.** Without normalization, the regime selects for reviewers who accept easy proposals. Difficulty proxies — review effort, prior divergence, sub-topic frontier-distance — weight review-credit so that engaging hard syntheses is not dominated by rubber-stamping easy ones. The first-iteration difficulty proxy is the same per-proposal contention signal the cluster-stratification refinement already computes (`2 * min(accepts, rejects) / total_votes`): unanimous-easy items contribute zero contention to the cluster signal and earn reduced rep credit by the same primitive; perfectly-split contentious items contribute the full signal both places. The wired knob is `review_credit_contention_alpha`: reviewer rep deltas (gain on accurate, loss on inaccurate) scale by `alpha + (1 - alpha) * contention`, where `alpha = 1` (default) recovers the v0 uniform-credit regime exactly and `alpha < 1` is the difficulty-aware regime. *Implementation status:* the alpha primitive is wired and pinned end-to-end at three cells (alpha=0 unanimous → zero reviewer credit, alpha=0 split → contention-scaled credit, alpha=0.5 unanimous → half-credit). Default 1.0 preserves the v0 behavior; calibration credit (PRD §Calibration batches) flows through `bumpReputation` directly and is *not* alpha-scaled — the calibration path is ground-truth-individual, not convergence-derived, and stays a pure-honesty channel for the patient archetype's accumulation. The fifth parameter sweep cube re-baselines the patient-adversary gate-threshold cube under alpha < 1: with cube #2's thresholds (recent=0.5, demo=1.5) calibrated against the alpha=1 bootstrap demonstrated of 2.0, alpha=0.5 cuts the bootstrap demonstrated to 1.0 and the demo gate's value-comparison threshold (1.5) false-positive-locks-out honest reviewers at first request_assignment — the patient archetype's drift never starts, but the closure observation is misleading because honest review collapsed at the same gate. The cube reads ASR alongside a separate lockout-rate metric so "attack closed by gate" is distinguishable from "honest pool collapsed at the same threshold," and pins re-tuned thresholds at alpha=0.5 (recent=0.5, demo=0.75) that recover cube-#2's closure without the false-positive — demo scales by alpha to preserve honest-pool headroom; recent scales less because its quiet-window decay is dominated by alpha-invariant calibration credit. The patient-adversary signature (priming through easy unanimous accepts on well-grounded excerpts to harvest rep cheaply) is exactly the pattern alpha < 1 makes expensive on the convergence path; the calibration path stays the same drift-bandwidth seam it was at alpha=1. Proposer rep deltas are unscaled (the PRD passage scopes difficulty-normalization to *review* credit; the proposer's accept/reject outcome carries its own weight independent of how much disagreement the reviewers had on the way there).
- **Eligibility tiers public; numeric reputation private.** Contributors can see what tier they are in (and what gates the next tier); raw numbers are not leaderboards. Reviewers receive batch-level performance feedback after-the-fact, not in real time.
- **Non-transferable, non-monetizable.** Reputation is a coordination signal, not a token.
- **Specific formulas tuned in testbed.** Initial values are chosen for testbed simulation; production values are confirmed against attack-success-rate measurements.

---

## Credit

When a sub-topic produces a manuscript projection, contributor credit is computable from graph state.

The basic shape of the credit function:

- **Node provenance.** For each node included in the projection, who proposed it, who reviewed it, when.
- **Survivorship weighting.** Nodes that survived multiple supersedes events count more than nodes that barely survived one review.
- **Load-bearing weighting.** Nodes whose removal would break a projection chain count more than peripheral nodes.
- **Review weighting.** Reviewers who voted correctly accrue partial credit, weighted lower than proposers.

Credit is **deterministic given graph state and projection config**. It is not negotiated in a meeting. Authorship disputes route to (a) a versioned change to the projection config, evaluated by the same governance-change CI as any other governance change, or (b) a governance change to the credit function itself, also CI-evaluated. Both paths are public and auditable. Projection configs are not a back-channel.

Specific weights are deferred to the testbed.

---

## Adversary testbed

The testbed is the project's continuous-integration story for governance — and the cheapest credible artifact Anchorage can ship.

### Architecture

- **Real graph schema.** The testbed uses the same schema, the same write-path tools, the same MCP server interface as the public instance.
- **Simulated contributor population.** Synthetic contributors connect via the same MCP protocol, salted with the adversary taxonomy below.
- **Two engine tiers under one population.** The fast-loop archetypes are *scripted*: deterministic content providers, review deciders, and fabricators that stand in for "a model on the other end of the connection" at zero API cost and with replayable behavior — this is the bulk of CI. The deep-loop archetype is *model-backed*: `runLlmAgent` is a generic MCP-tool agent loop — list the server's tools, hand their JSON schemas to the model's tool-use API, execute each `tool_use` block against the MCP client, feed the result back, repeat until the model stops or a turn budget is hit. It carries no Anchorage-specific logic; the role (honest contributor, strategic adversary, patient adversary, lazy reviewer) lives entirely in the system prompt, so the agent sees exactly the surface a real client sees and can do exactly what a real agent could. *Implementation status:* `runLlmAgent` is wired in the testbed package over the Anthropic Messages API (no SDK dependency — the loop speaks the API shape directly); a `pnpm --filter @anchorage/server live` runner points it at the wired in-process server for the manual "is the loop real" check, and an integration test drives the same loop with a scripted model so the path is CI-exercised without a key. The first two named roles — honest-strong, patient adversary — are wired as `LlmRole` configs (system prompt plus cause-keyed task message) in the testbed package and CI-exercised through the scripted-model path, so the role prompts are pinned and reviewed alongside the loop rather than written ad hoc at each call site (see §Continuous integration's implementation note). Surfacing the error-shape constraint a tool-listing client imposes — an MCP client that has called `tools/list` validates any `structuredContent` against the tool's output schema, so error results carry their typed `{ code, message }` payload as JSON text in `content` rather than as `structuredContent` — was part of wiring this; the typed code still survives the transport unchanged.
- **Adversary budget model.** Each adversary archetype carries a numeric **budget** the harness deducts from as the archetype acts. Minting an identity at attestation level α costs α (the [binding-cost](#identity) layer); minting under the issuance-frequency cap deducts at most N identities per epoch (the [issuance-frequency-cap](#identity) layer); each per-epoch observable write action consumes one unit of that identity's per-epoch capacity at the configured rate-limit T (the [rate-limit-accounting](#identity) layer); the [clustering projection](#identity) costs the adversary nothing directly but surfaces them at the curator surface. ASR is measured at fixed budget across the defense-threshold sweep; the sweep dimension PRD §Identity names — coalition-affordable-identities-per-epoch — is the budget axis, and parameter sweeps measure ASR as a function of budget at each defense configuration. The model is the harness-side fiction the testbed needs because issuance is a layer below MCP (the server sees only the issued identity record, not the cost paid for it), but the budget arithmetic is the testbed's regression handle on whether the four identity layers compose to make a target adversary infeasible. *Implementation status:* wired in the budget-model slice as the `AdversaryBudget` primitive — `tryMint(epoch)` operationalizes the binding-cost (α deducted from B) and issuance-cap (≤ N mints per epoch, distinct `budget` and `issuance_cap` failure modes) layers; `tryAct(identity, epoch)` operationalizes the per-(identity, epoch) throughput cap T mirroring the server-side `accountWriteAction` arithmetic. Cubes #6, #7, #8, and #9 compose the budget axis as a sweep dimension on top of the existing parameter-sweep cubes — cube #6 reads the cost-multiplier directly on a one-shot suppression attack at (B × T × N), cube #7 extends it to (B × E × N) and reads the issuance cap as a time primitive shaping K's distribution across epochs, cube #8 reads the action-axis sibling at (M × T × E) where T is the per-(identity, epoch) rate-limit (slice 3) and the suppression-capacity arithmetic reduces to T × E targets, cube #9 reads the cluster signal × binding-cost composition at (B × T) on the sybil-amplified-coordinated-voting attack pattern that defeats either layer alone. The four-layer architecture's full composition is now CI-checked across all the named regression-handle attack patterns.
- **Parameter sweeps.** Calibration ratios, reviewer-pool sizes, reputation decay rates, vote thresholds — swept over reasonable ranges, attack-success rates measured. Sweeps share an aggregate shape: each cell asserts the per-(attack pattern, defense config) outcome directly, and a paired aggregate test groups cells by defense config to lock in the per-config attack-success-rate. The first wired cube sweeps the cluster-signal knobs (anti-correlation threshold × contention-weighted edges) against the decorrelating and mixed-strategy coalitions; the second sweeps the assignment-gate thresholds (recent × demonstrated) against the patient-adversary and sybil-amplified-coalition archetypes on the same two metrics — ASR and lockout-rate — surfacing the orthogonal-defenses property as ASR=0% under the composition with lockout=0% on every cell: both runners bootstrap their honest pools via contributor-initiated rotating-pair convergences on disjoint excerpts (each pair shares one proposal, below the cluster-signal floor) so the honest reviewers reach demonstrated=2.0 without false-clustering with the coalition; the demo gate's sybil-side closure then reads as honest defense (Eve fails the demo gate by null-policy, Carol gets routed and rejects, Dave is cross-stratum-gated, Erin+Frank's accepts converge contested across singleton strata). The third sweeps the encounter-domain widening (`stratum_include_declines` ∈ {off, on}) against the co-voting decorrelated and paired-decline multi-round coalitions and reads the *additive lift* declines provide on top of the vote-only signal: 50% ASR vote-only, 0% ASR decline-aware. The fourth cube is the companion measurement on the contention-weighting × paired-decline-floor interaction (`stratum_contention_weighted` × `stratum_decline_min_paired`) against the paired-decline coalition and the over-clustering false-positive that tests whether the floor preserves the closure under the small-honest-pool case where there's no fresh-singleton bypass; the (cw=on, floor=2) composition is the stable point at (ASR=0%, lockout=0%), (cw=on, floor=1) sits at (ASR=0%, lockout=50%) — the over-clustering pathology lands the false-positive lockout on contested, not an attack — and the cw=off rows are inert because the full-weight rule for declines is a cw=on branch. The fifth cube re-baselines the second under the difficulty-aware regime (`review_credit_contention_alpha < 1` × {gates off, cube-#2 thresholds, re-tuned thresholds}) against the patient-adversary archetype on the same two metrics. Cube-#2 thresholds at alpha=0.5 read (ASR=0%, lockout=100%) — closure observation misleading because honest review collapsed at the same gate; re-tuned thresholds at alpha=0.5 (demo scaled by alpha to preserve honest-pool headroom) recover (ASR=0%, lockout=0%). The two-metric shape, introduced at cube #5 and retrofitted to cube #4, is the template for any cube whose closure operates by gating: a defense that closes by collapsing the honest pool is not a defense. The sixth cube sweeps the budget axis the four-layer sybil-resistance architecture composes against — (budget B × binding-cost threshold T × issuance cap N) on a one-shot sybil-suppression attack — and reads the cost-multiplier directly: at (T=2, N=2) ASR=50% (B=2 fails, B=4 lands, exactly the 2× budget delta the binding-cost gate imposes), at (T=1, N=2) ASR=100% (gates inert), at N=1 ASR=0% across both T values (the issuance cap as time primitive closes the one-shot attack regardless of cost-multiplier). Cube #6 is the budget-axis sibling of cube #2's assignment-gate-axis measurement on the sybil-amplified pattern: the two cubes cover complementary axes of the four-layer architecture (cube #6 the identity layer, cube #2 the assignment-time gates), with cube #6's runner consuming the `AdversaryBudget` primitive directly so K_eff = min(floor(B/T), N) reads off the same arithmetic the harness fiction operationalizes. The seventh cube extends cube #6 along the time axis, holding T=1 and sweeping (B × E × N) — eight cells over (B ∈ {1, 4}, E ∈ {1, 2}, N ∈ {1, 2}) on the same one-shot suppression pattern, with sybil mints spread across E epochs (issuance-cap refusal advances to the next epoch, budget refusal stops the loop). The aggregate groups by (E, N) and reads ASR across the budget axis: the headline lift is (E=1, N=1) at 0% → (E=2, N=1) at 50%, which reads the issuance cap as a *time* primitive — the cap closes a one-shot attack outright at N=1, but a multi-epoch attack lifts K_eff to 2 across two epochs at the same defense config, and the only remaining closure on the (E=2, N=1) cell is the budget axis B=1. PRD §Identity bullet 2's "an adversary affording K sybils still cannot mint them all in one epoch, which buys behavior-based defenses the accumulated history they need to fire" is what makes the time-buying operationally load-bearing in production; cube #7 reads the arithmetic of the time-buying without behavior defenses, leaving the joined cluster-signal-with-budget-axis cube under a coordinated-voting attack pattern as the follow-up that reads how the identity-layer and behavior-layer defenses compose. The eighth cube is the action-axis sibling of cube #7 — same arithmetic shape on a different primitive: cube #7 reads K_eff = N × E (issuance cap × epochs); cube #8 reads suppression_capacity_eff = T × E (per-(identity, epoch) rate-limit cap × epochs). Eight cells over (M ∈ {2, 4}, T ∈ {1, 2}, E ∈ {1, 2}) on a multi-target suppression attack — K=2 sybils try to suppress M contested targets, each requiring 2 reject votes, with the server-side `rate_limit_actions_per_epoch` cap firing `rate_limited` errors when each sybil exhausts its per-epoch budget. The aggregate by (T, E) reads numerically identical to cube #7's: (T=1, E=1) at 0%, (T=1, E=2)/(T=2, E=1) at 50%, (T=2, E=2) at 100% — the suppression-capacity axis is symmetric in T and E across the attack window, and at fixed K=2 the K-dimension cancels (every suppressed target consumes one vote per sybil). Together, cubes #7 and #8 pin the K × N × T × E coalition-budget arithmetic the four-layer architecture composes against: cube #7 the issuance-cap (slice 2) × time axes; cube #8 the per-identity rate-limit (slice 3) × time axes. The ninth cube reads the *full composition* of the four-layer architecture under the named regression-handle attack pattern (sybil-amplified coordinated-voting): Carol+Dave with 3 priming proposals shared (cluster signal fires, cross-stratum gate refuses Dave at `request_assignment`), recruiting Eve(s) as fresh singletons (cost T to mint per the binding-cost gate, walk past the cluster signal). votes_to_reject=3 so Carol's lone reject is insufficient and K_eff(Eves) = floor(B / T) — capped at K*-1=2 by the runner — determines whether the attack lands. Six cells over (T ∈ {0, 2}, B ∈ {1, 2, 4}) drive `it.each`. The aggregate by T reads T=0 at 100% — identity layer inert, cluster signal alone leaks at any budget (the sybil-amplified-coalition seam by name) — and T=2 at 33% — composition closes the threshold-budget cells (B=1, B=2) but leaks at B=4 where the operator affords K*-1=2 Eves. The aggregate by B reads (B=1, B=2) at 50% (T=0 leaks, T=2 closes by composition) and B=4 at 100% (both T values land — composition fails at the budget axis). Cube #9 is the first cube to read both layers active simultaneously and surfaces the multiplicative composition PRD §Identity commits: the full closure requires *every* axis (cluster signal on, binding cost > 0, budget < K*-1 × T) to align, and the ASR readings degrade gracefully along each axis. The tenth cube opens the calibration-density design pass on a non-identity axis the prior cubes left unswept, sweeping (`calibration_inject_every_n` ∈ {0, 4, 2}) × (`calibration_aware_convergence` ∈ {false, true}) against the strategic-coalition attack pattern (2-of-3 biased reviewers vs one honest reviewer on two well-grounded excerpts, naive bias misfiring on the anchor calibration corpus). The aggregate by aware reads aware=false at 100% across all densities (calibration alone never closes the convergence half — a regression handle on PRD §Calibration batches' "calibration alone does not close the convergence layer" framing) and aware=true at 67% (the gate fires only at every_n=2; at every_n ∈ {0, 4} the calibration record stays empty and the gate is silent). The aggregate by density reads every_n=0 at 100%, every_n=4 at 100%, every_n=2 at 50% — the lift from every_n=4 to every_n=2 at aware=true crosses the *density floor on the calibration-aware closure*: in the two-excerpt runner each reviewer exhausts the frontier within ~3 review-task offers, so at every_n=4 the 4th calibration offer never fires and the calibration record stays at (0,0), collapsing the aware gate's vote weights to the count-only baseline. At every_n=2 each coalition member sees ≥2 calibration items, fails them all, and their weighted vote drops to 0 — the bias-misaligned excerpt's reject sum fails the weighted threshold and the proposal stays staged rather than converging rejected. Cube #10 is the first cube to read PRD §Calibration batches' "and-once-at-convergence" half's *density-dependence* directly, complementing the rep-ledger inversion the standalone calibration-injection scenario already pins (which is density-monotonic and not the load-bearing axis here). The eleventh cube reads the reviewer-pool-size axis PRD §What's deliberately not specified names as deferred to the testbed, sweeping (`stratification_enabled` × honest pool size H ∈ {1, 2, 3}) against a cluster-signal-eligible coalition (Carol+Dave with priming history, calAwareBiasedDecider so calibration channels are out of scope) under the two-metric template (ASR + stall-rate). Coalition runs first so the strat=off baseline reads (ASR=100%, stall=0%) across all H — pool size alone is not a defense at coalition-first ordering, since the coalition's two rejects converge before the honest pool can reach votes_to_accept. strat=on reads (ASR=0%, stall=33%) — the cluster signal closes the *capture* at any pool size (cross-stratum gate refuses Dave once Carol-already-routed), and the stall-rate reads the *closure-quality* axis: H=1 stalls (1 accept + 1 reject below either threshold; the divergence-closure sweep eventually archives as `unresolved-archived`), H ≥ votes_to_accept=2 converges 'accepted'. The headline is *graceful degradation* — PRD §Reviewer assignment commits "small-sub-topic case slower to close but not capturable by the pool-fits-the-coalition dynamic"; cube #11 reads this property directly. Pool size doesn't buy capture-resistance (the cluster signal does that at any H); it buys closure-quality — the difference between staged-and-archived and converged-accepted. The two-metric template (cube #5 introduced; "a defense that closes by collapsing the honest pool is not a defense") generalizes here to *closure-quality on the pool-size axis*: the cluster signal closes the capture, but full closure requires the honest pool to drive convergence. The twelfth cube reads the third axis PRD §What's deliberately not specified names — vote-aggregation thresholds (`votes_to_accept` / `votes_to_reject`) — sweeping (vto ∈ {2, 3}) × (vta ∈ {2, 3}) × (H ∈ {2, 3}) on the same K=2 coalition pattern with stratification disabled to isolate threshold arithmetic from the cluster-signal closure cube #11 measures. The aggregate by vto reads vto=2 at (ASR=100%, stall=0%) — K = vto, coalition wins by arithmetic — and vto=3 at (ASR=0%, stall=25%) — K < vto, capture is closed by arithmetic and only one of the four sub-cells stalls (vta=3, H=2). The vta and H axes carry symmetric stall_rate readings (vta=3 lifts stalls when H<vta; H=3 saturates at vta=3 so stalls drop to 0), and the (vto=3, vta=3, H=3) cell is the K+1-honest-reviewer dynamic PRD §Reviewer assignment names — at H = vta = K+1 the honest pool reaches the threshold the coalition cannot. The headline is the *threshold-axis decomposition*: vto controls capture rate (capture-resistance bought purely by threshold), vta + H jointly control stall rate (the closure-quality cost paid on the honest side at the same threshold). The architecture generalizes across closure mechanisms: cube #11 reads (cluster signal closes capture, pool size buys closure quality) on the stratification axis; cube #12 reads (vto closes capture, vta + H interaction buys closure quality) on the threshold axis; the (ASR, stall) numerical readings are structurally similar — same two-metric shape, complementary defenses against the same K=2 coalition. The thirteenth cube is the resolution refinement of cube #10, sweeping (`calibration_inject_every_n` ∈ {0, 8, 4, 2}) × (`calibration_aware_convergence` ∈ {false, true}) on a 4-excerpt runner — twice the excerpt count of cube #10 — and validates the density-floor scaling law: cube #10 placed the floor between every_n=4 and every_n=2 in the 2-excerpt runner; cube #13 places it between every_n=8 and every_n=4 in the 4-excerpt runner. The floor halved when the excerpt count doubled — every_n* ≈ excerpt_count, validated at two resolutions. The aggregate by aware reads aware=false at 100% across all 4 density cells (regression on cube #10's reading at higher resolution); aware=true at 50% (the gate fires at every_n ∈ {2, 4} and is silent at every_n ∈ {0, 8}). Together cubes #10 and #13 pin the density-floor scaling law as a CI-checked invariant: at fixed reviewer count, every_n must be ≤ excerpt_count for at least one calibration item to reach each reviewer before frontier exhausts. The runner stages all 4 excerpts as bias-misaligned to keep the calibration corpus anchor-only — load-bearing because bias-aligned excerpts that converge accepted during Carol's loop would enter the calibration corpus and Dave's subsequent bias-aligned vote on them would pass calibration, restoring Dave's weight and breaking the closure (the corpus-contamination dynamic the all-no-effect runner avoids by construction; the dynamic itself is now wired as its own standalone scenario downstream — Erin → Carol → Dave on a 2-bias-aligned + 2-bias-misaligned runner reads (staged, rejected) on the bias-misaligned targets vs (staged, staged) on the anchor-only baseline, the order-dependent intra-run bypass of the aware closure §Calibration batches names). The fourteenth cube unifies cube #11's stratification axis with cube #12's vote-to-reject axis on a single primed runner against the K=2 coalition, sweeping (`stratification_enabled` ∈ {off, on}) × (`votes_to_reject` ∈ {2, 3}) × (H ∈ {1, 2}) at vta=2. The (strat × vto) joint aggregate is the headline: (off, 2) at (ASR=100%, stall=0%) — the default-threshold capture baseline (cube #12's vto=2 row, replayed under priming); (off, 3) at (ASR=0%, stall=50%) — closure-by-threshold; (on, 2) at (ASR=0%, stall=50%) — *closure-by-stratification-not-by-threshold*, the load-bearing cell where vto is at the K-affording value (cube #12 reads ASR=100% at strat=off here) but the cluster signal closes the same attack anyway; (on, 3) at (ASR=0%, stall=50%) — *overdetermined* closure (both defenses fire), no extra capture-resistance beyond either alone. The two closure mechanisms are *redundantly sufficient* at K=2, not multiplicative — either alone closes capture; the second buys nothing on either metric. Stall_rate is invariant across the three closure pairs (50% across all H={1,2} cells where capture is closed; H=1 stalls regardless of which defense fired) — the closure-quality axis is independent of which capture-defense fires, so PRD's "slower to close but not capturable" graceful-degradation property survives the (strat × vto) composition rather than being tied to a specific closure mechanism. The fifteenth cube opens the reputation-decay axis §What's deliberately not specified names — sweeping (`recent_half_life_seconds` T ∈ {60s, 600s}) × (drift-cadence ratio r ∈ {1, 2, 6}) on the patient-adversary archetype with W = T × r per cell, holding `assignment_min_recent`=0.5 fixed at cube #2's calibrated value to isolate the (T, W) axis from the threshold axis cube #2 already measured. The aggregate by ratio reads (ASR=100%, 100%, 0%) at (r=1, 2, 6) — a binary closure on the W/T axis: r=1 (R = R₀/2 ≈ 4) and r=2 (R = R₀/4 ≈ 2) both stay above the 0.5 threshold, r=6 (R = R₀/64 ≈ 0.125) closes; the closure crosses between r=2 and r=6 at log2(R₀/0.5) ≈ 4 half-lives. The aggregate by T reads (ASR=67%, 67%) — *identical values across both T axes* — the headline ratio-invariance observation pinned as a CI-checked invariant on §Reputation's exponential-decay commitment R(W) = R₀ × 2^(-W/T): the closure depends on W/T (decay measured in half-lives), not on T or W independently. Any future change to the decay law that breaks ratio invariance (e.g. a non-exponential decay or a per-event reset) would surface here as different aggregates across T groups before downstream cube drift. The sixteenth cube closes the calibration-density design pass on the corpus-composition axis the contamination scenario named — sweeping (`corpus_confirmation_depth_floor` ∈ {0, 2, 3}) × (contaminate ∈ {off, on}) on the corpus-contamination scenario, with the floor knob plumbed through to both calibration-draw paths (`fetchCalibrationBatch` and `drawCalibrationTask`'s assignment-injection seam) and anchors structurally exempt by kind so that accumulated calibration-vote reviewers on the substrate don't mis-trip the gate against itself. The aggregate by floor reads floor=0 (default, inert) at 50%, floor=2 (boundary cell admitting items at exactly votes_to_accept) at 50%, floor=3 (above the boundary) at 0% — the closure crosses between floor=2 and floor=3 at floor > votes_to_accept exactly, the headline structural property cube #16 pins. The aggregate by contaminate reads off at 0% across all 3 floors (the baseline-not-locked-out invariant — anchors bypass the floor by kind, so the legitimate anchor-only corpus is preserved at any floor value) and on at 67%. Together cubes #10, #13, and #16 close the calibration-density design pass at three layers: cube #10 the density-floor axis at 2 excerpts, cube #13 the density-floor scaling law at 4 excerpts, cube #16 the corpus-composition closure on the contamination bypass that density alone cannot reach. The seventeenth cube is the demonstrated-component sibling of cube #15 — sweeping (`demonstrated_half_life_seconds` T_demo ∈ {60s, 600s}) × (drift-cadence ratio r ∈ {1, 2, 3, 6}) on the patient-adversary archetype with the recent path inert at both layers (`assignment_min_recent`=0 and `recent_half_life`=Infinity), `assignment_min_demonstrated` held at cube #2's calibrated 1.5, and W = T_demo × r per cell. The aggregate by ratio reads (100%, 100%, 0%, 0%) at (r=1, 2, 3, 6) — *the closure cross-point bracketed at r ∈ {2, 3}*, sharper than cube #15's r ∈ {2, 6} bracket because the demo threshold (1.5) sits closer to the per-half-life decay step than the recent threshold (0.5). The aggregate by T_demo reads (50%, 50%) — identical across both T_demo axes, the ratio-invariance reading on the demo axis paralleling cube #15's observation on the recent axis. The headline reading is the *cross-point dependence on the gate threshold*: cube #15 closes at log2(R₀/0.5) ≈ 4 half-lives; cube #17 closes at log2(R₀/1.5) ≈ 2.4 half-lives. Both cubes show ratio invariance — the closure depends on W/T, not on T or W independently — but the *value* of the W/T cross-point depends on the threshold the gate is configured against. Together cubes #15 and #17 close the reputation-decay design pass at three layers: the recent axis (cube #15), the demonstrated axis (cube #17), and the structural ratio-invariance both cubes pin as the CI-checked invariant on §Reputation's exponential-decay commitment.
- **Public results.** Methodology and results published. Adversary populations and harness code open. Specific calibration items remain private.

### Why assignment-driven contribution closes several attack surfaces

Several adversary vectors that previously needed elaborate defenses collapse under assignment-driven contribution:

- **Reputation laundering** (build rep on easy sub-topics, parlay into contested ones) — closed by construction. Capacity is cause-level; sub-topic is the system's choice; sub-topic rep is an emergent record of routing.
- **Coalition pre-arrangement** (multiple identities landing on each other's proposals) — closed by construction. Reviewer assignment is system-driven; coalitions cannot pre-arrange the queue.
- **Calibration distinguishability via batch-level signatures** — narrowed substantially. Calibration items already arrive via assignment; with proposals also arriving via assignment, calibration is structurally indistinguishable from "the rest of the work" — it is just another assigned task.
- **Cherry-picking proposals to review** — closed. Reviewers do not select proposals; the system assigns them.

What remains, and what the testbed continues to exercise:

- **Patient adversary accepting clean assignments** to build rep over time, then drifting on a contested assigned proposal. Two-component reputation with fast-decay recent gating assignment (`assignment_min_recent`) + difficulty-aware review credit (`review_credit_contention_alpha`) are the wired convergence-path defenses; calibration injection is a parallel ground-truth-individual channel that stays alpha-invariant by construction.
- **Decline-pattern abuse** — declining everything outside the adversary's preferred sub-topic to approximate selectivity. The wired stack composes three layers: a curator-side `declinePatterns` projection (visibility), the assignment-time decline-rate gate (`assignment_max_decline_rate`, throughput-cap operational), and the cluster-signal decline-aware extension (`stratum_include_declines`, structural — encounters widen to `{accept, reject, decline}` so paired-decline coalitions light up the anti-correlation primitive that vote-only encounters miss).
- **Sybil farms providing capacity to absorb assignments** — addressed by the four-layer sybil-resistance architecture in [Identity](#identity): binding cost via `min_attestation_level` at the MCP write surface, issuance-frequency cap below the MCP layer, per-identity rate-limit accounting via the `rate_limited` refusal mode, and cross-cause identity-clustering at the curator surface. The four layers compose multiplicatively against the adversary's budget under the [budget model](#architecture) above. *Implementation status:* all four layers plus the testbed-side adversary-budget model are wired (ROADMAP §Status), and cubes #6-#9 CI-check the multiplicative composition across the named regression-handle attack patterns. The standalone sybil-amplified-coalition scenario in the [adversary taxonomy](#adversary-taxonomy) stays as the regression handle on the freshness-bypass observation in isolation; the MCP-layer closure stack composes with the identity layer below.
- **Hallucinated submissions on assigned tasks** — verification engine + reviewer responsibility.

### Adversary taxonomy

The testbed exercises the regime against the following synthetic contributor populations:

- **Honest-weak.** Modest-capability honest contributor (e.g. small local model). Should largely succeed; failure-to-contribute rate measures friction.
- **Honest-strong.** Frontier-model honest contributor on hard synthesis tasks. Should succeed even on cases where simpler contributors stall.
- **Lazy.** Reviewer that votes without reading. Should be caught by calibration.
- **Hallucinator.** High-temperature model with no grounding discipline. Should be caught at the verification engine (span mismatch, unresolved citations) before review.
- **Strategic adversary.** Hidden-objective model — instructed to bias the graph toward outcome X while passing calibration. Tests whether redundant peer review and reputation gates close the attack surface.
- **Patient adversary.** Strategic adversary with a long horizon — builds reputation honestly for months before drift attempts. Tests whether per-(cause, sub-topic) reputation tracking and review-as-staking prevent slow drift.
- **Sybil farm.** One operator running many identities at the per-identity cost set in the [identity model](#identity). The four-layer sybil-resistance architecture (binding cost, issuance-frequency cap, per-identity rate-limit accounting, cross-cause identity-clustering) composes multiplicatively against the operator's budget under the [adversary budget model](#architecture). *Implementation status:* all four layers plus the testbed-side adversary-budget model are wired (ROADMAP §Status), and cubes #6-#9 CI-check the multiplicative composition across the named regression-handle attack patterns — cube #9 specifically reads the freshness-bypass closure where the identity layer (binding cost) closes the seam the cluster signal cannot reach. The complementary standalone testbed-side regression handle is the sybil-amplified-coalition scenario where a fresh recruit walks past every *behavior-based* defense by construction (cluster signal, calibration record, and reputation gates all require accumulated per-identity history); the MCP-layer closure stack composes with the identity layer below.
- **Coalition.** Multiple identities coordinating reviews on each other's proposals. Tests whether reviewer-assignment stratification, calibration random-sampling, and the collusion-detection sample-size floor defeat coordination — and how the regime degrades on small sub-topics where the floor isn't reached.

### Continuous integration

Every governance change — calibration ratio, reputation formula, reviewer-assignment algorithm, sub-topic-creation rule, credit-function weight — runs against the adversary suite before merging. Proposals that fail simulation against known attacks do not merge.

The CI envelope has two tiers. A *fast loop* (cheap simulated populations, large numbers, low compute) runs on every governance-change PR. A *deep loop* (expensive populations including frontier-model adversaries, smaller numbers, higher compute) runs on a slower cadence and on changes that the fast loop flags as borderline. The honest framing is "a year of patient adversarial behavior in an hour" applies to the fast loop's cheap populations; the deep loop is where frontier-model patient adversaries live, and that loop is genuinely expensive. *Implementation status:* the deep-loop engine is the `runLlmAgent` archetype (the generic MCP-tool agent loop described under [Architecture](#architecture)), and the model-backed roles — honest-strong, strategic adversary, patient adversary — are wired as `LlmRole` configs in the testbed package: a system prompt carrying the role definition (PRD §Adversary taxonomy's language directly; the patient adversary's prompt makes the build-reputation-then-drift strategy explicit, the strategic adversary's the drift-now-on-any-contested-item one — both honest on routine work, neither ever risking a calibration misfire — and each takes the hidden objective as a parameter) plus a cause-keyed task message, with the agent loop role-blind by construction. The role prompts are the deep loop's experimental treatment, so they are pinned in one place and CI-exercised with a scripted model (the scripted model can't exhibit a role's distinguishing behavior — it drives the honest write-path that the patient adversary's build phase is by design indistinguishable from — but it proves each config plugs into the loop); the `live` runner picks a role from an env var so it and CI run the literal same configs against a real model. The deep-loop scenario that stands up a contested item — so the patient adversary's drift half is observable — is wired as `run-deep-loop.ts` over a shared seeded fixture (`deep-loop-scenario.ts`, the population-run analogue of `live-scenario.ts`), and a golden cassette of a small `ci`-preset run (2 honest-strong + the patient adversary, a 2-anchor excerpt frontier, a 2-item calibration corpus) replays it deterministically in CI (`golden-deep-loop.test.ts`) — the same regression-handle shape the single-agent `live` runner gets from `golden-cassette.test.ts`, so a drift in the agent loop, the tool surface, the population round loop, or the patient-adversary prompt surfaces as a cassette miss in CI. The model-backed roles are joined to the *parameter-sweep cube* layer that runs alongside the scripted populations as `run-deep-loop-cube.ts`: one model-backed cell per setting of a swept axis — four axes today, the calibration defense on versus off (against the patient adversary), the adversary role (a strategic-adversary cell on the brazen contested item, with `votes_to_reject` raised to 3 so the lone adversary, which runs after the two honest reviewers each round, is offered the contested review before the two honest rejects would otherwise close it), contested-item severity (a `borderline-contested` cell that holds the strategic-adversary role fixed and swaps the brazen overstatement for one the source's observational "associated with" framing is plausibly readable as supporting causal "increases"), and closure-stack version (a `borderline-contested-v1` cell with `escalation_revise_counts_as_reject` ON, a `borderline-contested-v2` cell with the strict v1 stack — both escalation knobs ON, also requiring `accepts >= votes_to_accept` at escalation — and a `borderline-contested-v3` cell adding `contested_votes_to_accept` on top, the first closure-stack knob to touch the auto-close-accept path), with the cell list (`DEEP_LOOP_CUBE_CELLS` in `deep-loop-scenario.ts`) the extension point for more — each cell its own cassette, replayed deterministically in CI by `golden-deep-loop-cube.test.ts`. The calibration on/off pair is the model-backed complement to the scripted calibration-aware cube: that cube shows the calibration defense is load-bearing when an adversary actually drifts; this one checks that on the honest baseline — where the model adversary keeps to the build-standing-first strategy its prompt encodes — flipping the defense off moves no outcome, so it carries no false-positive cost. The strategic-adversary cell on the brazen item is the model-backed counterpart to the scripted strategic-adversary closure cubes: the recorded run has the strategic adversary reviewing the overstated claim and rejecting it — the "improves response rates" overstatement too brazen for its pick-the-borderline-cases strategy to defend — with redundant honest review carrying the closure regardless of how the adversary votes (a 3-0 reject, or a curator-resolved 2-1 had it drifted). The `borderline-contested` cell is the cube's *first recorded closure failure*, and the finding the cube was built to surface: handed the borderline item, the strategic adversary plausibly drifts (the recording: votes accept — the pick-the-borderline-cases strategy *will* defend an observational-vs-causal framing gap), one honest reviewer splits the rejecting side with a careful *revise* (the source's caveats *should* be reflected in the claim's wording, but revise is not a vote against acceptance in the v0 aggregation), and the curator escalation pass closes the 1 accept + 1 reject + 1 revise *toward accept* (its v0 rule accepts on a tie). The overstated claim ends `accepted` — the v0 closure stack (`votes_to_reject` threshold + revise not counting as a rejection + curator accept-on-tie) does not contain model-driven drift on a borderline contested item when a careful "revise" splits the rejecting vote. The v1 closure stack against that finding is a pair of orthogonal knobs on the curator-escalation tiebreak. (1) `ReviewConfig.escalation_revise_counts_as_reject` (default `false`) flips the escalation rule from `r > a ? reject : accept` (revise ignored, accept-on-tie) to `r + revise > a ? reject : accept` (revise counted as a soft-reject — capturing the load-bearing fact that a careful reviewer voting "revise" is not endorsing the proposal). (2) `ReviewConfig.escalation_requires_votes_to_accept` (default `false`) additionally requires `accepts >= votes_to_accept` for escalation-to-accept — the same affirmative-supermajority floor the auto-closure path enforces during normal voting — so a single accept vote, or a plain 1-1 tie with no revise, no longer escalates to accept on the accept-by-default rule. The two compose: the rule is `(reject_side > a) OR (knob2 AND a < votes_to_accept) ? reject : accept`, with `reject_side = knob1 ? r + revise : r`. Both knobs govern only the *escalation* tiebreak, not the auto-closure thresholds; the harness-level v0/v1 (1-1-1) and v0/v2 (1-1-0) deltas are pinned byte-for-byte by scripted-decider pairs in `population-loop.test.ts`. The `borderline-contested-v1` cube cell is the v1 stack's real-model regression baseline (sampling is non-deterministic, so its recording is a different rollout from the v0 cell's: 1 accept + 2 reject + 0 revise, curator-escalated reject under both rules — confirms v1 doesn't break the realistic borderline run). The `borderline-contested-v2` cube cell — the strict stack, both knobs on — recorded the cube's *second* closure failure, at a *different path* than the v1 knobs reach: in this rollout the strategic adversary voted accept on the contested item AND at least one honest reviewer also voted accept, hitting `votes_to_accept=2` and auto-closing the proposal accepted via the *normal vote path* before the curator escalation pass could see it. The v1 knobs govern only the escalation tiebreak; the auto-close-accept path is unfortified. The v3 closure-stack knob against that finding is `ReviewConfig.contested_votes_to_accept` (default `0`, inert): when > 0 and the proposal has any reject or revise vote in its tally, the auto-close-accept threshold is raised from `votes_to_accept` to this value — so a 2-accept-1-revise tally that would auto-close accept under v0 at `votes_to_accept=2` does not auto-close at `contested_votes_to_accept=3` because `acceptCount=2 < 3`; the proposal is held for the curator escalation pass, where the same contested floor applies as an additional constraint on escalation-to-accept (`accepts >= contested_votes_to_accept` required when dissent is present, otherwise escalates reject). Self-contained: composes with v1 (`escalation_revise_counts_as_reject`) and v2 (`escalation_requires_votes_to_accept`) but does not require them — v3 alone closes the auto-close-path failure the cube's `borderline-contested-v2` cell recorded by holding the 2-accept-1-revise tally on the auto-close side and rejecting it on the escalation side. v3 is the first closure-stack knob to touch the auto-close-accept path; v1 and v2 govern only the curator tiebreak. The load-bearing v0/v3 delta is pinned byte-for-byte at the harness level by the scripted-decider pair in `population-loop.test.ts`: same 3-reviewer accept/revise/accept sequential setup, knob inert versus `contested_votes_to_accept: 3` — the v0 case auto-closes every excerpt accept on the second accept vote (0 escalations, every excerpt materialized as a node); the v3 case holds every excerpt off the auto-close path and the between-rounds curator pass escalates each reject (0 excerpt nodes materialized, every proposal `rejected`). The model-backed `borderline-contested-v3` cube cell is the v3 closure stack's real-model regression baseline on the borderline ctDNA-MRD scenario: the recorded rollout happens to exercise v3 directly — strategic adversary drift accept + a confused honest accept + an honest reject (2-accept-1-reject-0-revise), which under the v2 stack would auto-close accept at `votes_to_accept=2` but under v3 is held off the auto-close path (`acceptCount=2 < contested_votes_to_accept=3` with dissent) and escalates reject (`a=2 < contested_votes_to_accept=3` at escalation). The contested overstated claim ends `rejected`. What remains in this thread is broadening the cube further (more borderline items varying the kind of overstatement, more closure-stack cells exploring orthogonal auto-close rules) and standing up the slower-cadence CI job that runs the model-backed loop on its own schedule.

Threshold definitions ("what counts as passing") are themselves versioned. Threshold changes go through the same CI process; threshold-loosening proposals require multi-curator approval and are publicly logged. The testbed protects against the simulated attacks; the threshold-versioning regime protects the testbed from being defeated by changing the meaning of "pass."

CI fails-shut: if the testbed infrastructure or model APIs are unavailable, governance changes do not merge. Liveness is sacrificed for safety. This is a deliberate choice and is acknowledged.

### What the testbed does *not* cover

- **Sophisticated human adversaries with out-of-band coordination.** State actors, well-funded interest groups, attackers willing to coordinate by phone, and attackers exerting political pressure on identifiable curators are not simulated. The residual risk is met by non-simulation defenses: curator vigilance, audit log, post-hoc forensics, and federation as an exit.
- **Senior expert hand-prose contributions.** Models do not reproduce these well. The simulation under-approximates the friction the regime imposes on them. Worth measuring against real contributors as soon as Phase 2 begins.
- **Phase 3+ failure modes.** Cross-cause reputation transfer, federation-induced collusion, multi-instance state synchronization. These are out-of-scope for Phase 1 testbed by design and gain testbed coverage when their phases open.
- **Novel attacks not yet in the adversary suite.** Passing the testbed is necessary, not sufficient. Continuous addition of new adversary archetypes is part of the testbed's maintenance cost.

This is the core load-bearing claim of the project, stated honestly. Wikipedia debates policy changes for years because it cannot test them; Anchorage tests them in an afternoon for the threat classes the testbed covers, and is honest about the threat classes it does not.

---

## What's deliberately not specified here

These are intentionally open until the testbed surfaces them:

- **Numeric calibration ratios** (what fraction of a reviewer's batch is calibration items).
- **Reputation formula constants** (gain weights for proposing, reviewing, surviving supersedes; decay rates).
- **Reviewer pool sizes** (N for a given proposal class).
- **Vote-aggregation thresholds** (what counts as convergent vs divergent).
- **Federation contracts** (Phase 3+ — when independent Anchorage instances exist, how their state and reviewer pools relate).

Specifying these before testing them would be guessing. The testbed exists exactly to replace guessing with measurement.

---

## What's deliberately not in this document

- **Operational moderation specifics.** Calibration items in active rotation, abuse-signal heuristics, specific moderation actions on the public instance. These are operationally private.
- **UI/UX design.** The web UI's specific surfaces are a separate document, written when the UI is built.
- **Implementation details.** Database choice, hosting, deployment story, specific languages and frameworks. These are not load-bearing for the design and will be decided when the code surface opens.

---

## References to internal documents

- [README.md](../README.md) — elevator pitch and mental model.
- [docs/manifesto.md](./manifesto.md) — why this exists, why now, why this shape.
- [docs/governance.md](./governance.md) — roles, contribution flow, reputation, calibration, dispute resolution at a higher abstraction level.
- [docs/seed-topic.md](./seed-topic.md) — the v0 cause and starter sub-topics.
- [ROADMAP.md](../ROADMAP.md) — phase plan and exit criteria.
