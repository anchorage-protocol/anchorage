{
  "name": "anchorage",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0",
  "description": "Anchorage protocol monorepo — contracts, server, web read-UI, and adversary testbed.",
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "typecheck:tests": "tsc -p packages/contracts/tsconfig.test.json && tsc -p packages/testbed/tsconfig.test.json && tsc -p packages/server/tsconfig.test.json && tsc -p packages/web/tsconfig.test.json",
    "test": "pnpm -r run test",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/node": "^24.13.2",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@biomejs/biome",
      "esbuild"
    ]
  }
}
