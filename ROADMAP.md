# Roadmap

Anchorage is built phase by phase, with each phase producing a discrete artifact that's useful on its own and informs the next. This file is honest about what's planned, what's hand-waved, and what's load-bearing for everything else.

The roadmap is a living document. Phases will move; some will split or merge. What stays stable is the *order of dependencies*: each phase produces something the next phase needs, and we don't skip ahead.

---

## Phase 0 — Design (closed 2026-05-14)

**Goal:** A coherent design document set that the next person to read can decide *in ten minutes* whether they want to be involved.

**Concurrency.** Phase 0 and Phase 1 ran concurrently. Doc work and testbed work co-evolved, with the docs-never-drift discipline (see [CLAUDE.md](./CLAUDE.md)) keeping them in lockstep — every commit that touched a contract updated both. Phase 0 finished when the docs stopped moving under their own pressure; the testbed has been building from them through several deep-loop cube cycles without surfacing further design contradictions or underspecified load-bearing points.

**Artifacts:**

- [README.md](./README.md) — elevator pitch, mental model, status.
- [docs/manifesto.md](./docs/manifesto.md) — why this exists, why now, why this shape.
- [docs/governance.md](./docs/governance.md) — contribution norms, review responsibilities.
- [docs/prd.md](./docs/prd.md) — technical north star: data model, governance machinery, calibration, credit, adversary testbed.
- [docs/seed-topic.md](./docs/seed-topic.md) — first cause for the public instance, starter sub-topics, and rationale.

**Exit criterion (met):** the docs are stable enough that the testbed builds from them without surfacing further contradictions or underspecified load-bearing points. Adversarial review by capable models acted as a continuous pre-screen; outside-expert and friend review were welcome but not gating.

---

## Phase 1 — Adversary testbed (closed 2026-05-14)

**Goal:** A simulation harness that runs the full governance regime against a synthetic adversarial population, with a published results snapshot.

This is the unique technical asset of Anchorage and the cheapest credible artifact we can produce. **The exit from Phase 1 is the user-exposure gate** — no real users meet Anchorage until this phase's exit criterion is met. Phase 1 work ran concurrently with Phase 0 doc work.

**Scope:**

- Real graph schema (claim-graph substrate, multi-scale topic/sub-topic/claim, edges, anchors).
- Real write-path tools (the same tools the eventual public instance will expose).
- Real governance machinery: redundant peer review, calibration batches drawn from validated history, reputation scoring, staking.
- Synthetic contributor population spanning the taxonomy: honest-weak, honest-strong, lazy, hallucinator, strategic adversary, patient adversary, sybil farms, coalitions — both scripted (the fast loop) and model-backed (the deep loop, via `runLlmAgent`).
- Parameter sweeps and attack-success-rate measurements (the cube layer in `testbed.test.ts` for scripted populations and `run-deep-loop-cube.ts` for model-backed populations).

**Artifact:** the testbed code (open, AGPL-3.0) and a published results snapshot documenting attack success rates against tunable defenses ([docs/phase1-results.md](./docs/phase1-results.md), CC BY-SA 4.0).

**Exit criterion (met):** governance changes are CI-checked against the adversary suite (the cube layer is the regression handle for every named axis in PRD §What's deliberately not specified, with the harness pairs in `population-loop.test.ts` pinning byte-for-byte deltas of every closure-stack knob), and a published results snapshot exists at the cited artifact. The original ROADMAP language called for "third-party replication" of the published results; this was a category error borrowed from conventional scientific publishing. Anchorage's testbed is by-construction simulatable: cassettes pin every byte across the wire, the model is a published API, the code is open. A third-party rerun is *literally the same operation* as an in-house rerun — the third party has no epistemic advantage except "they ran it instead of us". The load-bearing question is sweep coverage, not who runs the sweep; further coverage is incremental work in the same testbed and does not gate Phase 2 ([docs/phase1-results.md §Methodology](./docs/phase1-results.md) carries the reproduce-it instructions for anyone — in-house or third-party — who wants to extend the sweep).

---

## Phase 2 — Single-cause public instance (opened 2026-05-14)

**Goal:** One umbrella cause running on Anchorage with real human contributors, two or three hand-seeded starter sub-topics, and a manuscript projection emerging from the first sub-topic to mature.

**Scope:**

- Auth, identity, per-(cause, sub-topic) reputation.
- The verifiable-anchor write path (PMID/DOI fetch, span verification, refusal of ungrounded citations).
- Frontier surfaces: cause-level (where sub-topics could productively open) and sub-topic-level (specific synthesis gaps).
- Review queue with calibration batches.
- Minimal manuscript projection: outline view tying sections to sub-topic subgraphs.
- Operational tooling: moderation, abuse-flagging, reviewer-fraud detection.

**Slice plan:** The phase decomposes into a sequence of shippable slices. Each lands doc-then-code per [CLAUDE.md §Conventions for working in this repo](./CLAUDE.md#conventions-for-working-in-this-repo); each preserves the sim≡prod invariant — the testbed continues to use the deterministic in-memory primitives (`FakeVerifier`, in-memory `Store`, harness-minted identities), and the production code path acquires its real counterpart at the same surface, with no `if (sim) ...` branching. Slices stack in dependency order; later slices may be reordered if mid-phase observations argue for a different sequence, but any contract change still lands doc-and-code in the same commit.

1. **Live-fetch verification (closed 2026-05-14).** Replace the `StructuralVerifier` placeholder with a real verifier hitting NCBI E-utilities (PMID) and Crossref (DOI); URL anchors refused by default per PRD §Verification engine (URL-anchors are second-class in v0, opt-in via `reject_url: false`). Span verification against the fetched source; content-addressed anchor hashing (sha256 over normalized content); shared `normalizeForSpanMatch` covering whitespace, smart-quote fold, en-/em-dash/minus and ellipsis folds, used by both the live verifier and the testbed `FakeVerifier`. The testbed continues to use `FakeVerifier` with seeded source maps — cassette-deterministic CI requires no production verifier in the loop. Re-verification (the path that transitions anchors to `unresolvable` on content drift) is a follow-up alongside slice 7's operational tooling; the verifier-side primitive is in place. Wiring `LiveFetchVerifier` as the default `Server` verifier is the production runtime in slice 4c (`run-prod.ts` constructs `Server` with `verifier: new LiveFetchVerifier()`).

2. **Persistence (closed 2026-05-14).** Replace the in-memory `Store` with a SQLite-backed implementation. The `Store` surface — `MapLike<K, V>` (`get` / `set` / `values` / `entries` / `size`) over the primary-record collections (identities, agent credentials, agent-credential secrets, causes, sub-topics, proposals, nodes, edges, review votes, assignments, capacities, reputations, calibration records, verified refs, rate limits — the `agent-credential secrets` index lands in slice 3b's bearer-token model) — is now an explicit interface in `packages/server/src/store.ts`; both `MemoryStore` (testbed default) and `SqliteStore` (production-runtime default; wired by slice 4c) satisfy it. `SqliteStore` is built on Node's built-in `node:sqlite` (Node ≥24 is already the engine floor, so no new native dependency); each collection is a single `(key TEXT PRIMARY KEY, value TEXT)` table holding JSON-encoded records, with iteration ordered by `rowid` to match JavaScript `Map`'s insertion-order semantics. Single-instance suffices for the single-cause v1; a Postgres path lands later if multi-instance becomes necessary before Phase 4. The testbed continues to use the in-memory store (cassettes need deterministic id ordering and zero-disk I/O), and a parity test (`packages/server/src/sqlite-store.test.ts`) drives the same end-to-end Server scenario through both backends and asserts byte-identical graph state plus close/reopen durability against an on-disk file — the sim≡prod invariant for the storage backend, in code. The persistent-data layout is JSON-in-text rather than structured columns because the Server's only access pattern against the store is by primary key plus full-scan iteration; richer columnar projections (frontier queries, identity clustering) are server-side derivations from iterated state and would be served by separate materialized projection tables alongside, not by reshaping the primary records. PRD has no commitments about the storage backend; the slice introduces no contract change.

3. **Identity backend.** Replace the harness-minted identities with real OAuth — GitHub for v1, covering the developer-and-researcher profile Phase 2 expects to draw first; ORCID and institutional SSO layer on later for the part-time-clinician profile. Server-side: token issuance, the agent-as-delegate path per PRD §Identity (one human binds N agent credentials; revocation per-agent without losing identity), `attestation_level` mapping from the IdP's signal, the issuance-frequency cap. Specific tech choice is a Phase 2 decision per PRD §Identity ("specific tech ... is a Phase 2 implementation choice"); this slice makes it, and updates PRD §Identity and `packages/contracts` in lockstep with whatever the IdP shape settles to. The slice decomposes into three sub-slices:

   - **3a — Authenticator seam (closed 2026-05-14).** Introduces the `Authenticator.authenticate(token) → Caller` trust boundary in `packages/server/src/auth.ts`, with `HarnessAuthenticator` as the default implementation (tokens are `identity_id` or `identity_id/agent_credential_id`, validated against the Store, no network). `Server` exposes `server.authenticator` (injectable via `ServerDeps`); `buildMcpServer` accepts `{ token }` and resolves through it at build, refusing malformed/unknown/revoked tokens with `unauthorized` *before* any per-tool gate runs. PRD §Identity gains the Authenticator-seam paragraph in the same commit; the seam is pinned in CI by `mcp.test.ts`'s `authenticator seam` suite (six cases: empty token, unknown identity, malformed delegation, cross-identity credential, delegated round-trip, revoked-identity refusal). No behavior change for downstream gates — they continue to see only a resolved `Caller` — which is the whole point: this is the keystone that lets 3c's production authenticator plug in at the same surface with `if (sim) ...` branching nowhere in the codebase.
   - **3b — Agent-credential bearer tokens (closed 2026-05-14).** Promotes `bindAgentCredential` to issue a one-time opaque secret (32-byte URL-safe-base64, returned exactly once at the bind call site, hashed in storage as `AgentCredential.secret_hash`); a new `Store.agentCredentialSecrets` index maps the SHA-256 hash back to the credential id, satisfied by both `MemoryStore` and `SqliteStore` (the slice-2 parity test exercises the new collection across backends). Secret-source threads through `IdGen.bearerSecret()` so `RandomIdGen` produces `crypto.randomBytes`-driven entropy in production and `SeededIdGen` produces deterministic counter-derived values in tests — same pattern as id generation, no `if (sim)` branching. `HarnessAuthenticator` accepts the bearer-secret grammar as its primary path; a transitional fallback to direct-identity-id tokens remains so the checked-in golden cassettes (`golden-deep-loop*.json`) replay without re-recording — both fallbacks resolve through the same `Authenticator.authenticate` surface, and the fallback retires when those cassettes are re-recorded. The PRD §Identity agent-credential-bearer-token commitment lands here, alongside the contract change to `AgentCredential` (`secret_hash` field) and the `BindAgentCredentialResult` shape (`{ credential, secret }`). The seam is pinned in CI: `mcp.test.ts`'s `authenticator seam` suite now exercises the bearer-secret path end-to-end (issued-secret round-trip, credential-id-as-token refused, credential-revoked refused, identity-revoked refused), and `server.test.ts` pins the no-collision invariant across distinct binds.
   - **3c — GitHub OAuth authenticator (closed 2026-05-14).** Concrete `GithubOAuthAuthenticator` in `packages/server/src/auth-github.ts`: device-code OAuth flow for desktop MCP clients (`startSignin` → `completeSignin(device_code)`), identity-on-first-signin keyed by `${provider}|${github_user_id}` against the new `Store.identityProviderSubjects` index, attestation_level computed by `computeGithubAttestationLevel` from the GitHub account signal (2FA on AND verified primary email AND account age ≥ 30 days → level 2; anything weaker → level 1). The `GithubApi` is an injected seam — `GithubApiHttp` over `globalThis.fetch` for production (registered against a real GitHub OAuth App), `FakeGithubApi` for tests and the testbed (scripted device codes, poll responses, user profiles). `Identity` gains `identity_provider: z.enum(['harness', 'github'])` and an optional `identity_provider_subject` on the contract; `bootstrap.mintIdentity` accepts both and validates the (provider, subject) invariant (subject required for IdP-driven providers, refused for `'harness'`, no double-mint against an existing (provider, subject)). PRD §Identity locks GitHub as the v1 IdP, records the attestation mapping, and notes the future-IdP plug-in shape (ORCID, institutional SSO add a new enum value and reuse the rest of the surface). Issuance-frequency cap (PRD §Identity bullet 2) is wired in code at the IdP layer against the new `Store.idpIssuanceCounters` collection — `completeSignin` bucket-keys per (provider, github_user_id) by default and refuses with the new `issuance_cap` `ServerErrorCode` before any identity mint or credential bind; the cap and epoch window default to `Infinity` (gate inert), production deployments pick finite values. The seam is pinned in CI: `auth-github.test.ts` covers seventeen cases (attestation mapping at every signal combination, device-code flow pending/slow_down/authorized/expired/denied transitions, identity-on-first-signin reuse, distinct-user mints, idempotent retry returning the same credential, revoked-identity refusal at signin, revoked-credential refusal at the seam, unknown-token refusal at the seam, issuance-cap fires exactly once per epoch, inert configuration never fires); `mcp.test.ts` continues to pin the `HarnessAuthenticator` path through the seam; the slice-2 parity test now drives an IdP-driven mint alongside the harness identity so the two new collections are exercised across `MemoryStore` and `SqliteStore`. Sim and prod are by-construction indistinguishable: downstream gates see only a resolved `Caller`, the bearer-secret lookup path is identical across authenticators (SHA-256 hash through `Store.agentCredentialSecrets`); the HTTP transport that exposes the device-code endpoints lands in slice 4a, and the production runtime that registers `GithubApiHttp` against a real OAuth App lands in slice 4c. PRD §Identity (bullets 1, 2, Authenticator seam, and a new GitHub-OAuth bullet) lands in the same commit per docs-never-drift.

4. **Public deployment.** Stand up `mcp.anchorage.science` as the production MCP endpoint. Hosting, TLS, secrets management, edge rate-limit, logging, backup — wires existing code into a hosted runtime, no new write-path surface. This is the slice where Anchorage stops being "runnable locally" and starts being "addressable from the internet." Curator-key bootstrap (the human-or-humans who can fire `acceptProposal` / `rejectProposal`) lands here; the operational tooling slice surfaces it through a UI later. The slice decomposes into three sub-slices:

   - **4a — HTTP transport (closed 2026-05-14).** Stands the Authenticator seam up over HTTP. `packages/server/src/http.ts` exposes three surfaces on one Node `http.Server`: `POST /mcp` (plus the optional `GET`/`DELETE` the streamable-HTTP transport spec admits for SSE and session teardown) routes through the MCP SDK's `StreamableHTTPServerTransport` in stateless mode — bearer token from `Authorization: Bearer` resolves through `server.authenticator` once per request, a fresh `buildMcpServer` binds to the resolved `Caller`, the transport handles the single JSON-RPC round-trip, both tear down on response close (the per-connection bind the in-memory transport uses, now per-request); `POST /auth/github/start` and `POST /auth/github/complete` expose `GithubOAuthAuthenticator.startSignin` / `completeSignin` unauthenticated (these are how a brand-new client *acquires* its bearer secret); `GET /healthz` is the load-balancer liveness probe. Refusal mapping carries the typed `ServerErrorCode` to HTTP status (`unauthorized` → 401, `rate_limited` / `issuance_cap` → 429, `invalid_input` / `invalid_state` → 400, `not_found` → 404), so a client branching on `code` reads the same payload it sees on the MCP wire. The seam is pinned in CI by `http.test.ts` (thirteen cases covering `/healthz`, three `/mcp` refusal modes, an end-to-end MCP tool round-trip through `StreamableHTTPClientTransport`, the device-code → bearer-secret → `/mcp` sim≡prod round-trip — the slice's load-bearing assertion that a secret produced through the OAuth path authenticates identically to one produced by in-process `bindAgentCredential` — malformed-body refusals, the issuance-cap → 429 mapping, the harness-only deployment posture where `/auth/github/*` is absent, and the 404/405 routing edges). PRD §Identity gains the HTTP-transport bullet and updates the GitHub-OAuth bullet's slice-4 reference to the 4a/4c split, in the same commit per docs-never-drift. No contract change: the slice introduces no new write-path surface, no new schema, and no `if (sim) ...` branching — `Caller` is what flows downstream regardless of which transport produced it.
   - **4b — Curator role + admin CLI (closed 2026-05-14).** Adds `role: 'contributor' | 'curator'` to `Identity` on the contract (`IdentityRole` enum; `Identity.role` defaults to `'contributor'` so pre-4b records and cassettes parse cleanly through the default without re-recording). `MintIdentityInput.role` is optional and consumed by `bootstrap.mintIdentity`, which enforces the role-provider invariant: only `'harness'`-provider mints can produce a curator (an OAuth-driven mint that requested `role: 'curator'` would open a coordination back-door where any GitHub account could self-promote on first signin; the operator-only bootstrap precludes it by construction). The operator bootstrap path is the `anchorage-admin` CLI (`packages/server/src/admin-cli.ts`, exposed as `pnpm --filter @anchorage/server admin`): `mint-curator --db=<path> --display-name=<name>` opens the on-disk `SqliteStore` directly, mints a curator identity, binds an agent credential, and prints `{ identity_id, credential_id, display_name, secret }` to stdout as the one-shot bearer-secret reveal — the operator stashes the secret in a secrets manager and hands it to the curator. `list-curators --db=<path>` prints the seated curator roster; `revoke-identity --db=<path> --identity-id=<id>` flips an identity to `revoked` (idempotent on already-revoked). The curator-only MCP tool surface that reads the role to authorize `acceptProposal` / `rejectProposal` over the wire is slice 7 alongside the operational tooling UI — slice 4b establishes the data-model side and the offline bootstrap path; slice 7 wires the wire-level path. The seam is pinned in CI by `admin-cli.test.ts` (twelve cases — usage / unknown-subcommand, mint-curator round-trip + label override + missing-flag refusal, list-curators with mixed roles + empty store, revoke-identity active→revoked + idempotency on already-revoked + not-found, flag-parsing for both `--k=v` and `--k v` forms), `server.test.ts`'s role suite (default-to-contributor, harness-provider curator mint, IdP-provider curator refusal), and `identity.test.ts` on the contract side (default-to-contributor, explicit curator, unknown-role refusal). PRD §Identity gains the curator-role bullet in the same commit; the contract change is doc-and-code lockstep per docs-never-drift.
   - **4c — Production runtime + deployment docs (closed 2026-05-14).** `packages/server/src/run-prod.ts` wires `SqliteStore` (configurable on-disk path), `LiveFetchVerifier`, `GithubOAuthAuthenticator` over `GithubApiHttp` (against an env-provided client_id), and `startHttpServer` into a single bootable process. Two seams stay injectable (`verifier`, `githubApi`) so the runtime can be smoke-tested end-to-end against deterministic fakes without hitting NCBI / Crossref / GitHub. Env-driven configuration: `ANCHORAGE_DB_PATH` (required), `ANCHORAGE_HOST` (default `127.0.0.1`), `ANCHORAGE_PORT` (default `8080`), `ANCHORAGE_GITHUB_CLIENT_ID` (optional — omitted in local-dev / testbed postures, which the runtime stays on the harness authenticator for; production sets it and the `/auth/github/*` routes light up), `ANCHORAGE_ISSUANCE_CAP_PER_EPOCH`, `ANCHORAGE_ISSUANCE_EPOCH_SECONDS`, `ANCHORAGE_ATTESTATION_AGE_DAYS_FOR_LEVEL2`. The runtime refuses to start on any malformed value (loud-failure on misconfiguration — silent fallbacks would mask a misconfigured production launch); SIGINT/SIGTERM trigger a graceful shutdown that closes the HTTP server then the SQLite store. The `Dockerfile` at the repo root is a single-stage `node:24-alpine` image that copies the workspace, runs `pnpm -r build` at image-build time to catch type errors, and `CMD`s `pnpm --filter @anchorage/server run prod`; `EXPOSE 8080`, `VOLUME /data`, and `HEALTHCHECK` against `GET /healthz`. `docs/deploy.md` (the new operator guide linked from README) covers the GitHub OAuth App registration procedure, the env-var contract, the bare-metal and Docker boot recipes, the curator-bootstrap sequence through the slice-4b admin CLI, the backup cadence (SQLite snapshot or `.backup` cron), and the observability seams (the `anchorage.server.started`/`stopped` / `auth.github.*` / `http error` log events). The runtime is pinned in CI by `run-prod.test.ts` (eleven cases — `parseProdConfig` defaults, required-env refusals, range-checked tunables, github-block presence/absence; `runProdServer` end-to-end against an on-disk tempdir SQLite store hitting `/healthz`, the `/auth/github/*` 404 under no-github config, the OAuth device-code → bearer-secret round-trip with `FakeGithubApi` injected, and close-and-reopen durability). This is the slice where `mcp.anchorage.science` can actually answer traffic — the runtime is bootable end-to-end with a single `docker run`, and the operator's path from "fresh disk" to "first curator seated" is the documented sequence in `docs/deploy.md` against the slice-4b admin CLI.

5. **Web read-UI.** A minimal client at `anchorage.science` surfacing the graph: causes, sub-topics, anchors, excerpts, frontier, contributor profiles, reputation tiers. Read-only against the MCP read-path tools; the write surface stays MCP-only for v1, matching the protocol's agent-as-delegate model — humans direct agents (Claude Desktop, Cursor, custom clients) that interact via MCP; the web is for reading. A web write surface is a Phase 2 add if observed demand argues for it, not a default. Decomposed:

   - **5a — Read-path resources (closed 2026-05-14).** The four MCP resources [PRD §Read-path tools and resources](docs/prd.md#read-path-tools-and-resources) has been carrying as committed-but-not-yet-implemented: `cause://` (active causes with their active sub-topics), `sub-topic://{id}` (metadata + an activity-counter projection: active nodes, staged proposals, frontier items), `node://{id}` (node + immediate active edges + hydrated neighbors), `subgraph://{sub-topic-id}` (active node set with edges between them). Wire shapes are added to [`@anchorage/contracts/resources`](packages/contracts/src/resources.ts) (`CauseDirectory`, `SubTopicDetail`, `SubTopicActivityCounters`, `NodeNeighborhood`, `Subgraph`, plus a `ResourceName` enum parallel to `ToolName` so the harness can introspect the read-path surface without crawling the SDK registration table). Server-side handlers live on a new `server.resources.*` namespace; the MCP-side registration in `packages/server/src/mcp.ts` uses `registerResource` with `application/json` content blocks, and `ServerError('not_found' | 'invalid_input')` lifts to `McpError(InvalidParams, …, { code })` so clients pattern-match on the typed PRD code exactly as they do on tool-error payloads. Caller gating matches the existing read-path tools — the token must resolve through the Authenticator, but resource reads do not consume the per-identity rate-limit budget. Exhaustiveness is pinned both sides: a contracts test asserts `ResourceName.options` matches the spec list, and a wrapper test asserts `listResources` + `listResourceTemplates` cover the same four schemes. Sim≡prod is preserved by construction — the testbed and the production runtime serve the same resources through the same MCP transport, no `if (sim)` anywhere. The web client in 5b reads against this surface; the server-side scope of slice 5 is done.
   - **5b — Web client scaffold + first views (closed 2026-05-15).** A new `packages/web` workspace project brought up as the read-only frontend at `anchorage.science`. The three load-bearing decisions the slice settles:
       - **Framework**: server-rendered HTML via a tiny escape-by-default tagged-template helper, no bundler, no client React. The repo is bundler-free Node-native TypeScript end-to-end; adopting a meta-framework for two read-only pages would have imported an entire build philosophy this codebase has consciously avoided, and would be a fake-mature-signal footprint disproportionate to the slice. The public face is a recruitment surface — fast, indexable, intelligible at view-source matter more than interactivity, and slice 5b commits no interactivity beyond navigation. Progressive enhancement is a Phase 3 problem; the slice-5c surfaces (node detail + contributor profile) and slice 6 (manuscript projection) stay readable HTML by construction.
       - **MCP-over-HTTP client posture**: *no client*. The web tier runs in-process with the MCP server (`run-prod.ts` mounts the web handler on the same `node:http` listener as `/mcp`), sharing a single `Server` instance and holding a privileged read-only `Caller` to satisfy resource-level Authenticator gating. The earlier ROADMAP guess at "a per-session anonymous identity behind a public endpoint" is corrected here: minting an identity per browse session would multiply identity count for no gain (read-path doesn't consume rate-limit budget, PRD §Read-path tools and resources — slice 5a commits this), and would press needlessly against the IdP's binding-cost gate. Anonymous browse collapses to a single operator-minted service caller (`anchorage-admin mint-reader`, this slice). Auth-required contributor views (later slice) will still mint per-human identities through the OAuth flow — the posture decision here scopes to the *anonymous* surface only. A future `HttpMcpReader` could plug in for deployments that separate the web tier from the MCP tier; the v0 deployment serves both surfaces from one process by design.
       - **Styling baseline**: a single static CSS string with no preprocessor and no utility framework — theme tokens as CSS custom properties so slice 5c can introduce a dark-mode variant or contributor-view accent without rewriting rules. The visual register is intentionally restrained (dense information, narrow column, generous line-height) matching the README / manifesto tone.

     Slice deliverables: `packages/web` with `render.ts` (HTML helper + `renderDocument` shell), `styles.ts` (baseline stylesheet), `pages/{home,sub-topic,layout}.ts` (the two views + shared chrome + `notFoundBody`), `web.ts` (`buildWebHandler` mapping `/` → home, `/sub-topic/:id` → sub-topic detail, `/healthz` → JSON liveness, with HEAD support, 404/405 HTML shells, and a `not_found` / `invalid_input` → typed HTTP-status mapping that mirrors `packages/server/src/http.ts`). The in-process composition lives in `packages/server/src/reader.ts` (`InProcessReader implements AnchorageReader`); the dependency direction is one-way at runtime (`@anchorage/web` → `@anchorage/contracts` only; the server composes the web handler with this reader). `ServerError` moved from `@anchorage/server` to `@anchorage/contracts` so any package on the wire (web, future federated peers) can `instanceof`-check without taking a runtime dependency on the server; the historical `@anchorage/server` import path re-exports it for back-compat. `run-prod.ts` reads `ANCHORAGE_WEB_READER_IDENTITY` and, when set, validates the named identity is active in the store before mounting the web handler — a stale or revoked env value refuses loudly at boot. `anchorage-admin mint-reader` is the operator's bootstrap path: mints a contributor-role harness identity bound to no human, prints `{ identity_id, display_name }` (no bearer; the in-process posture has no transport boundary to authenticate across). `docs/deploy.md` documents the env var, the bootstrap, and the new `web_tier: boolean` boot-log field. End-to-end coverage in `packages/server/src/web-integration.test.ts` drives a real `Server` + `InProcessReader` + `buildWebHandler` against an ephemeral `node:http` socket, asserting routing, HEAD semantics, empty-state branches, reader-graph coupling (counters, node content, breadcrumbs), and escape discipline (HTML metacharacters in cause descriptions round-trip as escaped text). The `run-prod.test.ts` suite adds boot-time tests for the three branches (unset env → MCP-only 404 on `/`; valid identity → HTML home page; unknown/revoked identity → boot refusal).
   - **5c — Coverage to spec (closed 2026-05-15).** Remaining surfaces named in the slice description: node-detail page (anchors and excerpts with their neighborhoods and provenance), contributor profile + public reputation-tier projection (the second piece [PRD §Reputation](docs/prd.md#reputation) commits as separate-from-`query_reputation`; requires the tier definition to land first, which 5c does), and the navigation that ties them together. The slice landed in two commits:
       - Server-side half — the v0 tier mapping is committed in PRD §Reputation: three tiers per (cause, sub-topic), derived from the contributor's decayed `(demonstrated, recent)` against the server's existing `assignment_min_*` review-config thresholds. **none** when demonstrated is below the gate threshold (matches the demonstrated gate's opposite null-policy — unproven identities are by construction not in the pool); **quiet** when demonstrated passes but recent doesn't (the episodic-expert case in a between-windows state); **contributing** when both pass. The public tier and the assignment gate agree by construction with no separate tier-threshold knob layered on top — when the operator runs both gates inert (the defaults), every entry collapses to `contributing`, scaling with the operator's chosen thresholds rather than a second mechanism. The fifth MCP resource `contributor://{id}` is wired (`server.resources.getContributorProfile`, `buildMcpServer` registers it through `registerResource`) and returns `ContributorProfile` = `PublicContributor` (display fields only — id, display_name, created_at, status; *not* identity_provider, identity_provider_subject, attestation_level, or role, all of which leak operational signal or PII) + `PublicReputation.entries` of tier-labeled per-(cause, sub-topic) records (no raw numeric reputation, even when the entry exists). Revoked contributors resolve normally so graph history stays browsable; unknown identity ids refuse with typed `not_found` matching the other resources' shape.
       - Web-side half — two new pages and the navigation that ties slice 5 together. `/node/:id` reads the existing `node://{id}` resource and renders the kind-specific surface (anchor: external_ref link + content_hash; excerpt: quoted_span in a blockquote with the offset; synthesis/open_question: parents-via-inbound-edges) plus a provenance list (created_by → `/contributor/:id`, created_at, status) and a neighbors section linking each counterpart back to its own node page with a `from → to` arrow indicating edge direction. `/contributor/:id` reads `contributor://{id}` and renders the display-fields header (with a revoked-notice inline when status is revoked, since the page continues to render for graph-history readability) and an eligibility-tier list with human-readable tier labels (`not in pool` / `proven, currently dormant` / `actively contributing`), each linking to its (cause, sub-topic). The sub-topic page's node list and frontier items now link to `/node/:id`; the node-detail page links to `/contributor/:id` for the proposer and to neighbor `/node/:id` pages; the contributor profile links back to `/sub-topic/:id` for each tier entry. Routing surface in `web.ts` grows two new branches with a shared `matchSingleSegmentRoute` helper; route matchers are exported for testability. End-to-end coverage in `packages/server/src/web-integration.test.ts` adds node-detail tests (anchor: source ref + content hash + contributor link + neighbor link; excerpt: quoted span + reverse edge to anchor; unknown id → 404) and contributor-profile tests (display fields + tier list with no raw demonstrated/recent leakage; revocation notice on revoked identity; empty-state when no rep entries; unknown id → 404). Slice closes the read-only public face of the v0 instance; manuscript projection (slice 6) builds on top of the same `Server.resources.*` surface.

6. **Manuscript projection.** The first time the graph is projected into something a contributor can put their name on. PRD §Manuscript projection and PRD §Credit commit the shape; this slice implements a sub-topic-keyed walk producing an outline + cited-claim list + named contributors via the credit function, surfaced through the read-UI from slice 5. The first half of the exit criterion attaches here. The slice decomposes into two sub-slices:

   - **6a — Manuscript contract + server resource + MCP wiring (closed 2026-05-15).** Wire shape (`Manuscript`, `ManuscriptSection`, `ManuscriptCitation`, `CreditAttribution`) lands in [`@anchorage/contracts/resources`](packages/contracts/src/resources.ts); the v0 implicit-default projection config is committed in PRD §Manuscript projection (no separate `ProjectionConfig` graph record yet — the versioned-config-as-governance-artifact path is a later slice). `server.resources.getManuscript` walks the same active node set `getSubgraph` returns (home OR scope-member, status=active), groups nodes into four fixed-order sections (`sources` → anchors by created_at; `quotations` → excerpts by created_at; `synthesis` → synthesis nodes by induced-subgraph derives degree descending then created_at; `open_questions` → open_question nodes by created_at), and computes credit attribution via the PRD §Credit factors (proposer at full weight, accepted-aligned reviewers at the strictly smaller reviewer weight, both scaled by per-node survivor and load-bearing factors over the induced subgraph). Specific weights are the four new testbed-tunable `ReviewConfig` knobs (`credit_proposer_weight`, `credit_reviewer_weight`, `credit_survivor_bonus_per_supersede`, `credit_load_bonus_per_induced_derives`) with non-zero v0 defaults — no baseline to preserve because the projection is new; the testbed will sweep them once a Phase-2 corpus exists. The MCP-side registration in `packages/server/src/mcp.ts` mirrors the other five resources; `ResourceName` grows to six and the wrapper-exhaustive test catches drift. Revoked-status contributors remain in the credit list with the status flagged (PRD §Identity). `InProcessReader` / `AnchorageReader` grow `getManuscript` so slice 6b can render against the same in-process composition. Coverage in `packages/server/src/mcp-resources.test.ts` pins the load-bearing properties: section order + kind-specific citation fields (external_ref + content_hash on anchors, quoted_span on excerpts, induced-subgraph parents on syntheses), proposer-vs-reviewer credit weights and self-vote exclusion, load-bearing multiplier (an anchor with a child excerpt outweighs a peripheral anchor), empty-state for sub-topics with no active nodes, revoked-status preserved, unknown-id refused.

   - **6b — Web rendering at `/manuscript/:sub-topic-id` (closed 2026-05-15).** Reads `manuscript://{sub-topic-id}` through the in-process reader; renders the four sections as readable HTML with anchored citations (excerpt blockquotes carry the quoted span; anchors link to their external_ref + content hash; syntheses + open questions cite their in-scope derives parents); names contributors in credit order with links to `/contributor/:id` and inline `(revoked)` flags where applicable; links back to the sub-topic page. The sub-topic page grows a link to `/manuscript/:id` so the projection is discoverable from the sub-topic surface that slice 5 closed. End-to-end coverage in `packages/server/src/web-integration.test.ts` adds manuscript-page tests (fixed section order, anchor and excerpt citation surfaces, proposer attribution, units present, empty-state-per-section for an empty sub-graph, revoked-flag persistence, 404 on unknown id) and a sub-topic-page assertion that the manuscript link is present. The web route-matcher suite in `packages/web/src/web.test.ts` gets a `matchManuscriptRoute` case to pin the routing surface in isolation. Slice 6 closes the manuscript-projection half of the Phase 2 exit criterion (the first sub-topic now ships a manuscript projection with named contributors traceable back to graph nodes); the testbed-caught-governance-change half is slice 8.

7. **Operational tooling.** The curator-side surface: the over-the-wire MCP path for the in-process `server.curator.*` namespace, a curator console UI for moderation, and the anchor re-verification scheduler. The server-side primitives exist; the wire-level path, the UI, and the scheduler do not. The slice decomposes into three sub-slices:
   - **7a — Curator-only MCP tool surface (closed 2026-05-15).** Wires the eight curator operations as MCP tools (`curator_accept_proposal`, `curator_reject_proposal`, `curator_defer_sub_topic`, `curator_revoke_identity`, `curator_archive_stale_proposals`, `curator_expire_stale_assignments`, `curator_decline_patterns`, `curator_identity_clusters`) — the wire-level path 4b deferred ("the curator-only MCP tool surface that reads the role to authorize `acceptProposal` / `rejectProposal` over the wire is slice 7"). Two-layer gating on `Identity.role`: a *discovery filter* in `buildMcpServer` (`packages/server/src/mcp.ts`) skips registering the curator-tool block when the resolved caller's role is not `'curator'`, so a contributor's `tools/list` omits it entirely — also what keeps the testbed's LLM-backed contributor archetypes byte-identical to pre-7a (the request body to the Anthropic Messages API is a function of `mcpClient.listTools()`, so the discovery filter is what protects the checked-in cassettes); and a *wire-level gate* (`wrapCurator`) that re-resolves the caller from the store on every call and refuses with `permission_denied` if the role is no longer `'curator'`. The new `ServerErrorCode` `permission_denied` lands in `@anchorage/contracts/errors` with a distinct-recovery-path justification mirroring `rate_limited` and `issuance_cap` (PRD §Curator-only tools commits the code), and the HTTP transport maps it to 403. `revokeIdentity` lands on `server.curator` (taking the in-process write that admin-cli held in 4b), and the `anchorage-admin revoke-identity` CLI now delegates so on-the-wire and offline revocation cannot drift. The seam is pinned in CI by `packages/server/src/mcp.test.ts`'s `curator tool surface` suite — discovery filter (contributor sees no `curator_*`, curator sees all eight), accept / reject round-trips, idempotent revoke, decline-patterns and identity-clusters empty-result paths, wire-level `permission_denied` on a mid-connection role demotion, and `unauthorized` on a mid-connection identity revocation — plus the contracts-side `ToolName` exhaustiveness test growing the eight new names. PRD §MCP tool surface gains the *Curator-only tools* subsection in the same commit; the contract change is doc-and-code lockstep per docs-never-drift.
   - **7b — Curator web console (open).** A moderation queue page surfacing `status === 'staged'` proposals; pages for the `declinePatterns` and `identityClusters` projections; both gated by a curator-identity reader separate from the public reader from 5b (an `anchorage-admin mint-curator-reader` analog or the curator's own `ANCHORAGE_WEB_READER_IDENTITY` — the choice lands at 7b open). Read-only: action buttons are *not* the path — the curator clicks through, sees what's queued, and directs their agent to fire the `curator_*` tools over MCP. The web stays in the agent-as-delegate posture PRD §MCP tool surface (Curator-only tools) commits.
   - **7c — Re-verification scheduler (open).** Periodic re-fetch of `active` anchors against their stored content hash; on drift, transition to `unresolvable` and emit through a curator-side projection. The verifier-side primitive (`verifyExternalRef` callable on demand, returning a fresh hash) is in place from slice 1; this slice adds the scheduler, the `unresolvable` transition path, and the curator surface for flagged anchors. PRD §Verification engine's "Re-verification (the path that transitions anchors to `unresolvable` on content drift) is a follow-up alongside slice 7" closes here.

8. **First testbed-caught governance change.** Operational milestone, not a code slice. Some time during Phase 2, a parameter will need re-tuning — a threshold based on observed honest-pool behavior, a window based on observed contributor cadence, an attestation gate based on observed abuse. The discipline is that the change runs against the testbed before merging; the milestone is met when at least one such change is caught by the testbed and revised before shipping. This is the second half of the exit criterion.

Slices 1-4 are infrastructure with no user-facing surface; slices 5-7 are user-facing; slice 8 is the governance discipline the testbed underwrites.

**Exit criterion:** the first sub-topic ships a manuscript projection with named contributors, traceable back to graph nodes, and the testbed catches at least one governance proposal that would have been an attack vector.

---

## Phase 3 — Second cause + protocol hardening

**Goal:** A second umbrella cause running on the same instance, validating that the protocol is cause-agnostic.

**Scope:**

- Sub-topic auto-discovery as a graph-derived feature (proposing tractable scope envelopes from graph state).
- Cross-cause reputation: how (or whether) reviewer credibility transfers between causes.
- Federated read; optional federated write.
- Manuscript-projection improvements: section-level claim provenance, citation export, reviewer comments tied to graph nodes.

**Exit criterion:** the second cause produces an independent manuscript projection without governance regressions on the first.

---

## Phase 4 — Independent fork

**Goal:** At least one institution we don't control runs an independent Anchorage instance with a different cause focus.

**Scope:** documentation, deployment story, governance handoff, federation contract.

**Exit criterion:** the independent instance is producing manuscript projections on its own cause, and protocol changes can be coordinated across instances without breaking either.

---

## What's deliberately *not* on this roadmap

- A token, marketplace, or paid tier.
- Generic chat or freeform-wiki features.
- Auto-merge of contested syntheses (the system surfaces them as `open_question` instead).
- Replacement for journal review or empirical research.
- Promises about specific timelines. Phases happen when their exit criteria are met.

---

## Status

Phases 0 and 1 closed 2026-05-14 (see Phase entries above for the as-met exit criteria). Phase 2 opened 2026-05-14; slice 1 (live-fetch verification) closed the same day — `LiveFetchVerifier` wires PMID resolution via NCBI E-utilities `efetch` (abstract + bibliographic header as plain text) and DOI resolution via the Crossref `works` endpoint (title + JATS-stripped abstract), with URL anchors refused by default (PRD §Verification engine commits URL-anchors as second-class in v0). Content hashing is sha256 over normalized source (`normalizeForSpanMatch`: smart-quote fold to ASCII straight quotes, en-/em-dash/minus-sign and ellipsis folds, whitespace collapse — the same function the testbed `FakeVerifier` uses for its source-fixture match, so the test and production paths agree on what counts as a match). The verifier caches fetched source in-process under the ref so that an anchor proposal followed by an excerpt proposal in the same contributor session skips a second fetch. The verifier is available as `LiveFetchVerifier` from `@anchorage/server`; the production runtime that wires it as the `Server` default is slice 4. Re-verification (the path that transitions anchors to `unresolvable` on content drift) is a follow-up alongside slice 7's operational tooling — the verifier-side primitive (`verifyExternalRef` itself, callable on demand) is in place; the scheduler and the curator surface are not. Slice 2 (persistence) closed 2026-05-14 — `Store` is now an explicit `MapLike`-shaped interface satisfied by both `MemoryStore` (testbed default) and `SqliteStore` (production-runtime default, built on Node's built-in `node:sqlite`; one `(key TEXT PRIMARY KEY, value TEXT)` table per collection, rowid-ordered iteration matching JavaScript `Map`'s insertion-order semantics). The parity test drives the same end-to-end Server scenario through both backends, asserts byte-identical graph state across every collection (sixteen as of slice 3c — the two new IdP-side indexes land alongside the rest), and exercises close/reopen against an on-disk file — the storage-backend sim≡prod invariant in code. Slice 3 (identity backend — GitHub OAuth as the v1 IdP, per PRD §Identity) closed 2026-05-14 across all three sub-slices: 3a (Authenticator seam), 3b (agent-credential bearer tokens), and 3c (`GithubOAuthAuthenticator` with device-code flow, identity-on-first-signin, attestation mapping, and the IdP-layer issuance-frequency cap). Slice 4 (public deployment) closed 2026-05-14 across all three sub-slices: 4a (HTTP transport — `packages/server/src/http.ts` exposes the Authenticator seam over HTTP), 4b (curator role + `anchorage-admin` CLI for the operator bootstrap path), and 4c (`run-prod.ts` runtime entrypoint, `Dockerfile`, `docs/deploy.md` — the runtime is bootable end-to-end against an env-configured GitHub OAuth App and an on-disk SQLite store). Slice 5 (web read-UI at `anchorage.science`) closed 2026-05-15 across all three sub-slices: 5a (MCP read-path resources — `cause://`, `sub-topic://{id}`, `node://{id}`, `subgraph://{sub-topic-id}`), 5b (web scaffold + first views: a new `packages/web` workspace package serving server-rendered HTML over the in-process `Server.resources.*` surface through `buildWebHandler` + `InProcessReader`, opt-in at the deployment via `ANCHORAGE_WEB_READER_IDENTITY` and `anchorage-admin mint-reader`; framework decision: server-rendered HTML, no bundler, no client framework; MCP-over-HTTP posture decision: in-process, single operator-minted service caller; styling decision: static CSS with theme tokens; `ServerError` moved to `@anchorage/contracts` so the dependency graph stays one-way at runtime), and 5c (node-detail page + contributor-profile + public reputation-tier projection: fifth MCP resource `contributor://{id}` returning `ContributorProfile` = `PublicContributor` (display fields only) + tier-labeled per-(cause, sub-topic) entries; v0 tier definition in PRD §Reputation as three tiers (`none` / `quiet` / `contributing`) derived from `(demonstrated, recent)` against the existing `assignment_min_*` thresholds — the public tier and the assignment gate agree by construction; `/node/:id` and `/contributor/:id` pages with cross-page navigation; no raw numeric reputation surfaces anywhere on the public face). The read-only public surface is complete. Slice 6 (manuscript projection) opened 2026-05-15 and sub-slice 6a closed the same day: the sixth MCP resource `manuscript://{sub-topic-id}` returning `Manuscript` = (sub_topic, cause, fixed-order `ManuscriptSection[]` (sources, quotations, synthesis, open_questions), `CreditAttribution[]` ordered by descending units). The v0 implicit-default projection config lands in PRD §Manuscript projection (no separate `ProjectionConfig` graph record yet — the versioned-config-as-governance-artifact path is a later slice). Credit attribution combines proposer weight (full) and accepted-aligned reviewer weight (smaller per PRD §Credit), scaled by per-node survivor and load-bearing factors over the induced subgraph. Four new testbed-tunable `ReviewConfig` knobs (`credit_proposer_weight`, `credit_reviewer_weight`, `credit_survivor_bonus_per_supersede`, `credit_load_bonus_per_induced_derives`) carry the v0 weights; non-zero defaults because there is no baseline to preserve (the projection is new). Revoked-status contributors remain in the credit list with the status flagged. Sub-slice 6b closed 2026-05-15: `/manuscript/:sub-topic-id` reads `manuscript://{sub-topic-id}` through the in-process reader and renders the four sections as HTML — anchors with their external_ref link + content hash, excerpts as blockquoted quoted spans, syntheses and open questions with their in-scope derives parents; the contributor section names credit-holders in unit-descending order with links to `/contributor/:id` and inline `(revoked)` flags where applicable. The sub-topic page grows a link to the manuscript so the projection is discoverable from the surface slice 5 closed. End-to-end coverage in `packages/server/src/web-integration.test.ts` pins fixed section order, citation surfaces, attribution, empty-state-per-section, revoked-flag persistence, and 404 on unknown ids; `packages/web/src/web.test.ts` adds `matchManuscriptRoute` cases. Slice 6 closes the manuscript-projection half of the Phase 2 exit criterion (the first sub-topic now ships a manuscript projection with named contributors traceable back to graph nodes); the testbed-caught-governance-change half is slice 8. Slice 7 (operational tooling) opened 2026-05-15 and sub-slice 7a closed the same day: eight `curator_*` MCP tools (`curator_accept_proposal`, `curator_reject_proposal`, `curator_defer_sub_topic`, `curator_revoke_identity`, `curator_archive_stale_proposals`, `curator_expire_stale_assignments`, `curator_decline_patterns`, `curator_identity_clusters`) wrap the in-process `server.curator.*` namespace. Two-layer gating on `Identity.role`: a discovery filter in `buildMcpServer` (the curator-tool block is only registered to sessions whose resolved caller has `role === 'curator'`, so a contributor's `tools/list` omits the block entirely — which is also what kept the testbed's LLM-backed contributor archetypes byte-identical to pre-7a, since the Anthropic-API request body the cassettes pin is a function of `mcpClient.listTools()`) and a wire-level gate `wrapCurator` that re-resolves the caller on every call and refuses with `permission_denied` if the role is no longer `'curator'`. The new `ServerErrorCode` `permission_denied` lands in `@anchorage/contracts/errors` with a distinct-recovery-path justification mirroring `rate_limited` and `issuance_cap`; HTTP transport maps it to 403. `server.curator.revokeIdentity` lands as the in-process write (taking the write 4b held in admin-cli); the `anchorage-admin revoke-identity` CLI now delegates to it so on-the-wire and offline revocation cannot drift. PRD §MCP tool surface gains the *Curator-only tools* subsection in the same commit. Coverage in `packages/server/src/mcp.test.ts`'s `curator tool surface` suite pins the discovery filter (contributor sees no `curator_*`, curator sees all eight), the accept / reject round-trips, idempotent revoke, decline-patterns and identity-clusters empty-result paths, wire-level `permission_denied` on mid-connection role demotion, and `unauthorized` on mid-connection identity revocation; the contracts-side `ToolName` exhaustiveness test grows the eight new names; the existing testbed and LLM-cassette suites continue to pass unchanged. Slice 7b (curator web console) and 7c (re-verification scheduler) are up next. The v0 MCP tool surface is implemented end-to-end as a TypeScript `Server` class — all 16 tools from the PRD (4 assignment-loop tools, 7 propose-* tools, `cast_review_vote`, and 4 read-path projections), the curator-mediated acceptance path for every proposal kind, the assignment loop (capacity → request → accept → submit), the review path (`cast_review_vote`), and the read-path projections (`query_frontier`, `query_proposals`, `fetch_calibration_batch`, `query_reputation`). The contributor lifecycle composes correctly under test. MCP transport runs the same code paths a real client sees, and the testbed harness drives synthetic archetypes (honest contributors, honest-weak contributors, honest and lazy reviewers, hallucinator, strategic-coalition reviewers — all *scripted* for the fast loop — plus `runLlmAgent`, a *model-backed* archetype: a generic MCP-tool agent loop that drives the same wired surface from an actual LLM over the Anthropic Messages API, with the role carried entirely in its system prompt) end-to-end through the wired surface — with reputation deltas, verifier rejections, and bias-driven convergence outcomes observable over the wire. The strategic-coalition scenario surfaces an open attack: pure-vote convergence + reputation-against-converged-outcome lets a 2-of-3 biased coalition flip both ledgers against an honest reviewer of well-grounded work. Calibration is wired as the rep-ledger defense (assignment-injected items drawn from validated history, scored on `cast_review_vote` against ground truth) and the testbed shows it costs the coalition rep on bias-misaligned items while crediting the honest reviewer. Calibration-aware convergence weights votes by per-(cause, sub-topic) calibration record and requires both a minimum distinct-reviewer count and a minimum weighted sum; with that flag on, the naive coalition can no longer drive a bias-aligned suppression of a well-grounded excerpt past the threshold — the convergence half of the attack closes against the naive archetype while the rep-ledger half stays inverted, the two layers compounding. Against the calibration-aware variant of the archetype (PRD §Calibration batches — bias narrow enough that the calibration corpus falls entirely outside the bias zone), both defenses go quiet by design — the coalition passes every calibration item, keeps full convergence weight, and reclaims rep dominance as well as suppression. Pool-size scaling closes that vector against any fixed-size coalition where the eligible honest reviewer pool is large enough: at threshold N a coalition of size < N cannot solo-drive convergence on either side, so K+1 honest reviewers outvote a K-member coalition. The minimum K+1-honest-reviewer regime is testbed-handled. The small-sub-topic case where the eligible pool can't furnish K+1 honest reviewers is now closed by stratified-by-history assignment: PRD §Reviewer assignment commits the v0 stratum primitive (vote-pattern co-occurrence cluster), the prefer-not-yet-represented-strata draw rule, and the stratification-degraded flag that tightens convergence thresholds when fewer than N strata are reachable; `request_assignment` and `resolveByConvergence` are wired to that partition. The testbed scenario shows the calibration-aware coalition that bypassed both calibration defenses failing here — once Carol and Dave have built shared vote-history, the cross-stratum draw routes Dave away from the contested proposal, and the bias-suppression vector closes against a 3-reviewer pool too small for K+1 honest. The vote-decorrelating coalition variant — Carol and Dave deliberately disagreeing on every non-bias-zone proposal so their pairwise agreement falls below the cluster threshold — is now also wired as a testbed scenario, and confirms what the v0 cluster primitive cannot catch: the coalition holds singleton strata, the cross-stratum gate has nothing to enforce, both members vote on the contested item, and the bias-suppression vector reopens. The seam is the cluster signal itself: vote co-occurrence is lossy in two opposite directions. Contention-weighted edges are now wired as the first refinement and close the *over-aggregation* failure mode — where unanimous priming history pulls honest reviewers into one cluster and the cross-stratum gate locks them out, while coalition members who skipped priming walk in through the gap. The over-aggregation testbed scenario shows the raw primitive driving the contested item to rejected (honest review strangled), and the contention-weighted companion shows it converging accepted (unanimous-easy priming carries 0 weight, no edges form, all five reviewers vote). Anti-correlation detection is now wired as the second refinement and closes the decorrelating-coalition bypass. The cluster computation treats systematic disagreement as a co-stratum edge alongside systematic agreement: a pair that votes opposite on every shared priming proposal lights up the disagreement metric the agreement metric can't see, gets pulled into one cluster, and the cross-stratum gate trips against the second coalition member when the first has already been routed. The testbed scenario for the decorrelating coalition closes at threshold 1.0 (perfect decorrelation). The mixed-strategy coalition that targets the *gap* between the two thresholds — a 50/50 vote pattern across shared priming, sitting below both — defeats either refinement individually but the *composition* (contention-weighted + anti-correlation, both on at threshold 1.0) closes it: contention weighting reweights the pair-stat so agreement on unanimous-easy items contributes 0 weight and disagreement on contentious items contributes the full weight, collapsing the weighted disagreement ratio back to 1.0. The testbed wires both halves: the mixed-strategy bypass open with each refinement alone, closed under the composition. The remaining seam — a coalition that avoids co-voting on any contentious item, which constrains them sharply (suppressing the contested target inherently creates a contentious item) — has its concrete instance in the multi-proposal paired-decline coalition wired in the paragraphs below: the structural closure (encounter-domain extension via `stratum_include_declines`, paired-decline floor) and the operational closure (divergence-closure archival) both land on it, and cube #3 measures the ASR lift from declines on top of votes.

The decline-pattern projection (PRD §Adversary testbed: "decline-pattern abuse — declining everything outside the adversary's preferred sub-topic") is now wired as a curator-side surface (`server.curator.declinePatterns(cause_id)`), with `min_offers` and `min_rate` filters that the curator picks per their threshold (operationally private). The testbed scenario shows a reviewer who declines everything outside their preferred shape surfacing at the top of the projection; an honest accept-all reviewer ranks at zero and is filtered out at any non-trivial `min_rate`. The divergence-closure mechanism PRD §Reviewer assignment commits is also now wired (`server.curator.archiveStaleProposals({ window_seconds, cause_id? })`): a staged proposal whose most recent vote is older than the window flips to `unresolved-archived`, the terminal status the contracts already declared but no path produced. Never-voted proposals are explicitly excluded — they're unstarted, not divergent. The sweep is idempotent (already-archived proposals are skipped on re-run) and curator-triggered, with the production scheduler kept operationally private.

A first parameter sweep is wired as the Phase-1-exit-criterion infrastructure pattern: a 3D cube over (coalition pattern ∈ {mixed, decorrelated}, anti-correlation threshold ∈ {0, 0.5, 1.0}, contention-weighted ∈ {off, on}) drives twelve it.each cells, each running the same end-to-end scenario through a shared `runDecorrelationScenario` helper and asserting the contested-status outcome. The matrix exercises all four corners of the closure: threshold 0 leaves every cell open; threshold 0.5 catches both patterns; threshold 1.0 closes the decorrelated pattern outright but only catches the mixed pattern under the contention-weighted composition (the matrix cell that was the standalone-test headline). The cube is now layered with an aggregate-attack-success-rate assertion that groups cells by defense config (anti-correlation threshold × contention-weighted) and asserts the per-config ASR — 100% with anti-correlation off, 0% at threshold 0.5, 50% at threshold 1.0 alone (the named gap), 0% under the composition. The aggregate is derived from the static expected-status fields the per-cell tests already validate, so it stays a fast read over locked observations rather than a re-run of the cube. The shape — named defense config to expected ASR, with a coverage invariant that catches uneven attack-pattern counts per group — is the template the sixteen cubes that followed all layer on (cube #2 across the assignment-gate thresholds, cube #3 across the encounter-domain widening, cube #4 across contention-weighting × the paired-decline floor, cube #5 across the difficulty-aware re-baseline, cube #6 across the budget × binding-cost × issuance-cap axes, cube #7 across the multi-epoch budget axis, cube #8 across the per-identity rate-limit at the action axis, cube #9 across the cluster-signal × binding-cost composition, cube #10 across the calibration-density × calibration-aware-convergence composition, cube #11 across the stratification × pool-size composition, cube #12 across the vote-aggregation thresholds × pool-size, cube #13 across the calibration-density scaling law at 4 excerpts, cube #14 across the (stratification × threshold) composition with priming, cube #15 across the recent half-life × quiet window ratio-invariance, cube #16 across the corpus-composition depth-floor on the contamination scenario, cube #17 across the demonstrated half-life × quiet window ratio-invariance, cube #18 across the (recent-decay × cluster-signal) composition on the multi-round timed-coalition attack, cube #19 across the (cross-cause-sybil × min_signal) precision-recall pivot on the cross-cause identity-clustering visibility surface, cube #20 across the (alpha × stratification × demo-threshold) re-baseline of cube #5's difficulty-aware findings on the stratification closure mechanism, each described below), and cube #5 generalized the single-ASR aggregate to a two-metric (ASR, lockout-rate) split that retrofitted into cube #4.

Honest-weak is wired as the friction-rate archetype — structurally honest-strong with verifier-rejection tolerance, with competence modeled in the content provider rather than the loop. The friction-rate test pins the measurement: the fraction of attempts the verifier refuses is observable from the action log and matches the configured weakness exactly, sitting between honest-strong's 100% pass-through and the hallucinator's 0%. The same cost (a `submit_rejected` action, a burned rate slot, no proposal record) applies — the archetypes differ in intent and fraction, not mechanism.

The sybil-amplified coalition is now wired as the freshness-bypass scenario for stratification: an established Carol+Dave coalition (priming history detected by the cluster signal) recruits Eve, a fresh identity with no shared history. The cross-stratum gate fires correctly against Dave (Carol-already-routed + Carol-Dave co-stratum) but Eve sits in a singleton stratum and the gate has nothing to enforce against her; she walks in and casts the second reject vote. The contested target flips to rejected with three votes — Erin's accept, Carol's reject, Eve's reject — and Dave never votes. The pin is architectural: behavior-dependent defenses (cluster signal, calibration record, reputation) all require accumulated history per identity, so an operator that mints fresh identities and uses each exactly once routes around them by construction. PRD §Identity specs four sybil-resistance layers — binding cost (`attestation_level` on the identity record, `min_attestation_level` gate at every write tool), issuance-frequency cap (below MCP, modeled in the testbed's adversary-budget model as the time primitive on the cost-multiplier), per-identity rate-limit accounting (new `rate_limited` refusal mode at the write surface), cross-cause identity-clustering (curator-side projection parallel to `declinePatterns`) — composing multiplicatively against an adversary budget per the §Adversary testbed adversary-budget model. The four layers are wired in the paragraphs below (slices 2-4 plus the slice-5 testbed-side adversary-budget model), and cubes #6-#9 CI-check the multiplicative composition end-to-end. This standalone scenario stays as the regression handle on the freshness-bypass observation in isolation — the runner mints synthetic identities at attestation 0 and keeps `min_attestation_level=0` (default) so the singleton-stratum-walks-past-cluster-signal closure reads cleanly without the binding-cost gate confounding it; cube #9 reads the same attack pattern with the binding-cost gate active and measures the composition directly.

Cross-cause identity-clustering is now wired as the fourth of the four sybil-resistance layers (`server.curator.identityClusters({ window_seconds, min_signal })`, parallel to `declinePatterns`). The v0 signal is per-(reviewer pair) count of distinct causes where both reviewers cast votes on the same proposal — honest reviewers typically work in one cause (per-cause reputation), so a pair appearing on shared proposals across multiple causes is the cross-cause behavioral fingerprint a sybil farm working multiple causes lights up and a single-cause coalition does not. Two metrics surface per pair: `cross_cause_count` (the headline signal) and `shared_proposal_count` (visibility tiebreaker — high cross_cause_count with low shared_proposal_count reads differently from concentrated co-occurrence in fewer causes; the curator weighs both). Default `min_signal=2` (cross-cause coordination requires at least 2 causes to be cross-cause); `window_seconds` is the rolling window over vote `created_at`, undefined for all-time. Sort order matches `declinePatterns`'s pattern: signal desc, then secondary metric desc, then ids asc as tiebreaker. Specific signals and thresholds remain operationally private at the production instance per PRD §Identity bullet 4 — methodology is public, tuning is not. The new regression scenario pins both halves of the contract: a sybil pair voting across CRC and AMR surfaces with `cross_cause_count=2` at `min_signal=2` (default), an honest pair voting in one cause only is filtered out by construction; lowering `min_signal=1` brings the honest pair in and confirms the sort order (sybil pair first by `cross_cause_count desc`). Computation walks the global `reviewVotes` store once, builds per-proposal reviewer sets, generates pairwise tuples for proposals with ≥2 reviewers, and accumulates causes/proposals per pair; pair canonicalization (sorted-id tuple) collapses (a,b)/(b,a) duplicates. The projection currently surveys vote co-occurrence; declines are first-class encounters in the cluster-stratification primitive (`stratum_include_declines`) and adding them here is a follow-up refinement that broadens the co-engagement notion without changing the surface shape — the curator's two-metric view scales to per-(cause, identity) decline counts as a third metric whenever the seam needs it.

Per-identity rate-limit accounting is now wired as the third of the four sybil-resistance layers (`rate_limit_actions_per_epoch` and `rate_limit_epoch_seconds`, both default Infinity → gate inert). The cap is single-bucket across all write tools — total throughput per identity per epoch — so the cost-multiplier reads as the per-sybil-throughput axis directly: at K sybils with cap T, the coalition's per-epoch budget is K × T. The shared `accountWriteAction` helper fires at the same 12 write-tool seams the binding-cost gate covers (atomically check-and-increment the per-(identity, epoch) counter, throw `ServerError('rate_limited', ...)` when the cap is reached). Refusal mode is the new `rate_limited` ServerErrorCode parallel to `unauthorized` and `not_found` — the recovery path is "wait for the next epoch," not "obtain a higher attestation" or "no work available," and conflating these would mislead a contributor whose throttling is transient. The new code is justified by the distinct recovery path and PRD §Identity's architectural commitment; `errors.ts` notes that adding a code is breaking-change territory and the comment block on the new entry names the rationale (the rate-limit refusal's recovery path is genuinely distinct, so reusing one of the existing three would have surfaced the same drift on the contributor side as the testbed). Counter consumption is positioned in the call order between the binding-cost gate and the tool-specific business logic — unauthorized callers don't burn budget; valid-input-but-business-logic-failed calls do (an adversary can't probe the system with bogus business inputs without paying for each). The store keeps a single record per identity (`{ epoch, count }`) that the helper resets lazily on epoch boundary rather than maintaining one entry per (identity, epoch) tuple — historical per-epoch counts aren't needed for enforcement; slice 4's curator-side identity-clustering projection is where historical surveillance signals surface, not enforcement. Epochs are wall-clock windows (`floor(now_ms / (epoch_seconds * 1000))`), not sliding windows; `FakeClock.advance(ms)` is what testbed scenarios use to cross epoch boundaries deliberately. The new regression scenario pins both halves of the contract: 3 actions land at cap=3, the 4th refuses with `rate_limited`, a *different* write tool also refuses with `rate_limited` (proving the cap is global rather than per-tool), the clock advances past the epoch boundary, and the same identity acts again successfully — counter reset is observable end-to-end.

The binding-cost gate is now wired as the first of the four sybil-resistance layers (`min_attestation_level`, default 0). The IdP records `attestation_level` on the identity at mint, opaque to the server (the server gates on threshold; it does not interpret what the level *means* — that's the IdP's job); the gate fires at every write tool that resolves a caller — `set_capacity`, `request_assignment`, `accept_assignment`, `decline_assignment`, all `propose_*` tools, and `cast_review_vote` — refusing with `unauthorized` mode rather than the rep gates' `not_found` opacity. The mode choice is load-bearing: the binding-cost mismatch is identity-level (a stable property of the IdP-issued credential) and there's no opaque-refusal value to protect (the adversary already knows their own attestation level), so the refusal accurately names the mismatch instead of masking it as the rep gates do for their thresholds. Read-path tools (`query_frontier`, `query_proposals`, `query_reputation`, `fetch_calibration_batch`) are not gated — read access doesn't multiply the per-identity action budget the gate is defending against, and PRD §Identity scopes the gate to the write surface explicitly. A single shared `requireMinAttestation` helper is what fires at every write seam, so coverage extends uniformly without per-tool branching; the new regression scenario (in `testbed.test.ts`) walks a representative cross-section of seams — capacity, assignment pull, contributor-initiated propose, contributor-initiated review — and confirms `unauthorized` at each, then mints an above-threshold identity and confirms the same seams pass with the gate inert. Default 0 leaves the gate inert and preserves all existing scenarios' behavior. The cost-multiplier the gate consumes is measured in slice 5 by the testbed harness's adversary-budget model (PRD §Adversary testbed: adversary budget model) — at K sybils the coalition pays K × per-sybil binding cost upfront, and identities below threshold cannot accrue any per-identity activity that behavior-based defenses (cluster signal, calibration record, reputation gates) would later engage on.

Patient adversary is wired as a drift-bandwidth archetype — a calibration-aware biased reviewer running solo, priming through honest accepts on well-grounded excerpts and passes on the anchor calibration corpus, then drifting on a single contested target. The scenario stacks the strongest current defenses (calibration injection + calibration-aware convergence) and confirms the convergence half holds: the contested target converges to accepted because two honest reviewers reach the distinct-count + weighted-sum gate before the lone biased vote can find a partner.

Two-component reputation bookkeeping is wired as the first half of the patient-adversary defense: every reputation event moves a slow-decay `demonstrated`-competence component and a fast-decay `recent`-activity component together, and `query_reputation` decays each component on read per its own half-life (PRD §Reputation). Half-lives are testbed-tuned config — `demonstrated_half_life_seconds` and `recent_half_life_seconds`, both defaulting to `Infinity` so existing scenarios (no time advanced) see identical pre-decay numbers on both fields and stay unaffected. The patient-adversary bookkeeping scenario is the regression handle: immediately post-drift `demonstrated == recent`, but advancing the clock past several recent half-lives drains `recent` toward zero while `demonstrated` is preserved.

The recent-activity *assignment gate* is now wired as the second half (`assignment_min_recent`, default 0). At `request_assignment`, callers with rep entries in the requested cause whose max decayed `recent` falls below the threshold are refused with `not_found`. Fresh reviewers (no rep entries) bypass the gate by construction so first-assignment bootstrap stays open; contributor-initiated voting (`cast_review_vote` with no `assignment_id`) bypasses too, so a drained-but-honest contributor has the named recovery path. The patient-adversary gate scenario closes the loop the bookkeeping scenario named: Carol primes + drifts on a first contested target, the clock advances past six half-lives, Carol's recent falls below threshold, a fresh contested target is staged — and her second drift attempt fails at `request_assignment` time. Drift bandwidth tightens from "many" (cumulative buffer) to 1 (the drift she landed before the quiet window). `FakeClock.advance(ms)` was added so testbed scenarios travel time deliberately on top of the per-call auto-tick.

The demonstrated-component *eligibility-tier gate* is now wired as the companion gate (`assignment_min_demonstrated`, default 0). Same seam as the recent gate (`request_assignment`), opposite null-policy: callers with no rep entries in the requested cause **fail** the gate when threshold > 0 — the demonstrated tier is "have you proven yourself yet?" and an unproven identity is by construction not in the pool. Bootstrap is the contributor-initiated path (direct `propose_*` and `cast_review_vote` without `assignment_id`), which earns rep without traversing the gate; once the caller's max decayed `demonstrated` clears the threshold, `request_assignment` opens. The eligibility-tier scenario pins the shape: Eve mints fresh, declares cause-level capacity, is refused at `request_assignment` (`not_found`), bootstraps via two accurate contributor-initiated accept votes alongside Erin (also fresh, also bootstrapping symmetrically), and graduates to her first assignment. The two gates compose at the same seam — recent fires against once-active-now-quiet (the patient-adversary signature), demonstrated fires against never-proven (the sybil-amplified-coalition signature) — and together they constitute the visible-history cost a fresh-identity coalition pays before being drawn for review. PRD §Identity's other three sybil-resistance layers (issuance-frequency cap, per-identity rate-limit accounting, cross-cause identity-clustering) are wired in the paragraphs below alongside the testbed harness's adversary-budget model, and cubes #6-#9 read the four-layer composition end-to-end; the binding-cost gate that's already wired (paragraph above) composes with these two rep gates as a separate identity-level layer that fires before any per-identity activity accrues, while the rep gates fire on accumulated activity once it does.

The second parameter sweep cube is now wired across these two gates. Eight cells over (attack pattern ∈ {patient, sybil}, `assignment_min_recent` ∈ {0, 0.5}, `assignment_min_demonstrated` ∈ {0, 1.5}) drive `it.each` through two shared scenario runners — `runPatientAdversaryGateScenario` and `runSybilAmplifiedGateScenario` — and assert per-cell `attack_succeeded` and `false_positive_lockout` under cube #5's two-metric template. Both runners respect what makes their archetypes legible: each runs a contributor-initiated bootstrap rotation that pairs honest reviewers across three convergences on disjoint excerpts so each honest reviewer ends with `demonstrated=2.0` (clears any threshold ≤ 1.5) before the assignment-driven phase begins, with each pair sharing exactly one bootstrap proposal so the honest pool stays singleton in the cluster signal (`stratum_min_shared_proposals: 2` is the floor the rotation sits below). The patient runner uses Carol+Erin+Frank in the rotation (Carol later drifts as the adversary); the sybil runner uses Erin+Frank+George as the honest pool while Carol+Dave coalition-prime separately on three other excerpts and Eve recruits fresh. Without this bootstrap the demo gate would conflate fresh-coalition with the legitimate honest pool by closing both at the cold-start, and the cube would mis-read lockout-driven closures as honest defense. The aggregate groups by defense config and reads both metrics: (recent=0, demo=0) at (ASR=100%, lockout=0%); (recent=0.5, demo=0) at (ASR=50%, lockout=0%) — recent gate closes patient by honest defense, sybil bypasses through the fresh-reviewer recent-gate null-policy; (recent=0, demo=1.5) at (ASR=50%, lockout=0%) — demo gate is inert against patient (Carol's demonstrated buffer holds), closes sybil by Eve's null-policy while the honest pool's bootstrap demonstrated clears the gate, Carol gets routed and rejects, Dave is cross-stratum-gated, and Erin+Frank's accepts converge contested across singleton strata; (recent=0.5, demo=1.5) at (ASR=0%, lockout=0%) — composition closes both attacks by honest defense at both metrics. The orthogonal-defenses headline reads cleanly under the two-metric template — every closure cell is honest defense, no lockout-driven cell pretends to defense success.

The multi-proposal coalition seam — the "avoid co-voting on any contentious item" workaround the mixed-strategy headline pinned by name — is now wired as a standalone scenario. Two coalition members each solo-reject a *different* contested target and decline the partner's, so the cluster signal stays silent by construction (zero shared voting history → no edge metric has data to fire) and convergence stalls at 1 accept + 1 reject on each target. The wired closure stack runs at full strength against this — strongest cluster signals on (contention-weighted, anti-correlation 1.0, agreement 1.0), default assignment gates open — and fires nothing. The terminal outcome is the divergence-closure sweep: both targets archive as `unresolved-archived`, which from the proposer's perspective is the suppression-by-archival result the coalition was driving toward. The decline-pattern projection's *default* small-sample floor (`min_offers=3`) hides the coalition entirely at the 2-target scale; surfacing them requires the curator to drop the threshold to the small-N regime, which in the same act surfaces every casual decliner alongside them — the visibility surface exists but pays its own false-positive cost on the small-pool case.

The decline-pattern → assignment-gate is now wired as the operational form of the curator-side `declinePatterns` projection. PRD §Capacity and assignment commits the gate as the assignment-time enforcement primitive on the same signal: `assignment_max_decline_rate` (default 1.0, gate inert) and `assignment_decline_min_offers` (small-sample floor) consumed at `request_assignment`, refusal as `not_found` so the contributor-facing surface stays structurally indistinguishable from "no work available," and the contributor-initiated path (`cast_review_vote` without `assignment_id`) bypasses by construction so a contributor whose decline rate has spiked retains the recovery path declines are non-punitive precisely *to* preserve. The gate's mechanics are pinned standalone: above-threshold + above min_offers refuses; above-threshold but below min_offers bypasses; fresh reviewer (zero offers) bypasses; the contributor-initiated path bypasses. The numerator and denominator the gate consumes match the curator-side `declinePatterns` projection byte-for-byte — same per-cause cumulative rate — so the assignment-time and curator-side surfaces operate on a single signal.

The gate is *operational* — a curator-tunable enforcement primitive on a known abuse signal — not a *structural* closure of the multi-proposal seam. The gate fires at `request_assignment` time and uses cumulative state, so it can refuse a caller's *next* request after their decline rate exceeds threshold but does not undo declines that already landed. At the minimal seam (2 contested targets, 1 honest reviewer), the coalition's lone-rejects on each target land before the gate could fire, both targets stall at 1 accept + 1 reject, and the divergence-closure sweep archives them regardless of the gate. The gate stays useful as the throttle a curator uses against decline-mediated abuse over a longer time-horizon than a single batch (where the gate's throughput cap binds — coalition size × declines-per-identity is the budget a real-world adversary works under).

The structural defense for paired-decline is the cluster signal extended to include declines as encounters (`stratum_include_declines`, default off). PRD §Reviewer assignment commits the widening: the encounter domain becomes `{accept, reject, decline}`, so "Carol votes A and declines B; Dave declines A and votes B" reads as 100% pair-disagreement on two shared encounters under the existing anti-correlation primitive — the seam evades the vote-only cluster by construction, and the wider domain closes it at the same primitive that already catches vote-only decorrelating coalitions. A real vote takes priority over a decline on the same `(reviewer, proposal)` pair, and decline-involved encounters under contention-weighting count at full weight (declines are inherently informative; the per-proposal accept/reject tally would underweight them by construction). The structural-effect scenario pins both halves: with the knob off, paired-decline encounters yield zero shared vote-history, no edge forms, the cross-stratum gate has nothing to enforce against, and the partner walks into the contested target normally; with the knob on, the cluster forms after the priming, the cross-stratum gate trips, and the partner's request_assignment fails with `not_found`. The whole-scenario closure landed alongside it as a multi-round companion: paired-decline priming on two excerpts builds the cluster *before* the contested target is staged, so when the suppression round begins the gate is already armed. With the knob off, Carol+Dave share zero vote-history through priming, no edge forms, Dave is offered the contested target, his second reject hits `votes_to_reject` and the proposal converges `rejected` — suppression succeeds. With the knob on, the decline-aware encounter domain lights up the anti-correlation edge during priming, Dave's request_assignment in the suppression round is gated, two fresh honest reviewers walk in and the contested target converges `accepted` — same defense stack, terminal status flips at the proposal a real proposer observes. The scenario runs at `stratum_contention_weighted: false`: the decline-aware primitive is what's load-bearing here, and turning contention-weighting on with declines counted at full weight false-clusters honest reviewers with the coalition (a single decline-involved disagreement at full weight outweighs every unanimous-easy vote-agreement reweighted to zero). That interaction is real and worth its own scenario; here the surrounding stack stays at raw weights so the honest pool reads as singletons and the multi-round closure isolates the decline-aware primitive's effect on outcome.

The third parameter sweep cube joins the multi-round closure with the existing vote-only cluster-signal stack as its first ASR-grounded measurement. Four cells over (attack pattern ∈ {co-voting decorrelated, paired-decline multi-round}, `stratum_include_declines` ∈ {off, on}) drive `it.each` through the shared `runDecorrelationScenario` and `runMultiRoundDeclineClosureScenario` helpers (the latter lifted out of the standalone test so the cube and the standalone observation share one source of truth). The aggregate-ASR layer groups by `stratum_include_declines` and surfaces the lift directly: vote-only at 50% (co-voting closed by anti-correlation 1.0; paired-decline open because declines are off-domain), decline-aware at 0% (paired-decline closed at the same primitive while vote-only's co-voting closure carries through unchanged because Carol+Dave never decline anything in the decorrelated runner). The framing is *additive*, not orthogonal: declines extend the encounter domain on top of votes, so decline-aware strictly contains vote-only and the cube reads the lift from the widening rather than a symmetric trade-off between two defenses. The cube format the prior two cubes set down (named cell → expected outcome, `it.each` driving per-cell tests, aggregate-ASR-by-defense-config layered on top) carries through unchanged.

The decline-aware × contention-weighting interaction the multi-round closure isolated to a single load-bearing knob is now closed by a metric refinement: PRD §Reviewer assignment commits a **paired-decline floor** (`stratum_decline_min_paired`, default 2) on contention-weighting's full-weight rule for declines. Without the floor, a single asymmetric decline-involved encounter between an honest pair (one declines, the other votes) lands weight 1 against a pair whose entire vote-vote history sits on unanimous-easy items at contention 0; the weighted-disagreement ratio collapses to 1.0 and the anti-correlation edge fires against honest reviewers who shared no coalition signal. The paired-decline closure has 2 decline-involved encounters per pair by construction, so a floor of 2 closes the over-clustering pathology without weakening the closure. The standalone scenario pins both halves: three honest reviewers prime on two unanimous-easy excerpts, one declines a third "trigger" excerpt while the other two vote accept on it (single asymmetric decline-involved encounter per pair), and a contested target is staged. With the floor at 1 (the buggy regime) all three honest reviewers collapse into one cluster, the contested target's eligible pool covers 1 stratum below `stratum_target_count`, the proposal is flagged stratification-degraded with `votes_to_accept` tightened to 3, only the first requester is routed and the cross-stratum gate refuses the other two — the proposal stalls at 1 accept and the divergence-closure sweep archives it. With the floor at 2 (default) every decline-involved encounter sits below the floor and contributes 0 weight, no edges form, all three honest reviewers stay singletons, all three vote accept and the contested target converges accepted at the second vote. Same defense stack on both halves; only `stratum_decline_min_paired` varies.

The fourth parameter sweep cube joins the over-clustering closure with the paired-decline multi-round closure as a CI-checked property under the (`stratum_contention_weighted`, `stratum_decline_min_paired`) interaction, declines on throughout. Eight cells over (attack pattern ∈ {paired-decline multi-round, over-clustering false-positive}, cw ∈ {off, on}, floor ∈ {1, 2}) drive `it.each` through the multi-round runner (parametrized on cw and floor) and the over-clustering runner (lifted out of the standalone test for cube reuse). Both runners emit `contested_status` directly; the cube reads it through cube #5's two-metric template, keyed on pattern: paired-decline contests are adversarial, so contested ≠ accepted contributes to ASR; over-clustering contests have no adversary in the runner (three honest reviewers, no coalition), so contested ≠ accepted contributes to lockout-rate, since the failure mode is honest-pool collapse by the cluster gate. The aggregate groups by (cw, floor) and reads both metrics: cw=off rows at (ASR=0%, lockout=0%) — the full-weight rule for declines is a cw=on branch, the floor is inert, paired-decline closes via raw anti-correlation on its 2 paired declines, over-clustering's asymmetric trigger lights nothing at raw weights; cw=on at floor=1 at (ASR=0%, lockout=50%) — paired-decline still closes through Frank+Hank as fresh singletons in the suppression round, but over-clustering lands the false-positive lockout directly on contested where the small-honest-pool offers no fresh-singleton fallback; cw=on at floor=2 at (ASR=0%, lockout=0%) — the stable composition. The previous one-metric aggregate read the (cw=on, floor=1) cell as ASR=50%, conflating honest-pool collapse with attack landing; cube #5 introduced the ASR-vs-lockout-rate split that closes the conflation, and cube #4 now reads under the same template. The named-pathology-closure pattern (a defense knob's effect read against the very scenario it was wired to close) is the working template for follow-up cubes.

Difficulty-normalized review credit (PRD §Reputation) lands as the alpha primitive: `review_credit_contention_alpha` (default 1.0 = v0 uniform credit) scales reviewer rep deltas by `alpha + (1 - alpha) * contention`, using the same `2 * min(accepts, rejects) / total_votes` proxy the cluster signal already computes. Three regression cells pin the wiring: alpha=0 unanimous-accepts → zero reviewer credit (the patient-adversary priming-via-easy-items pattern is now expensive when alpha < 1); alpha=0 split (one reject, two accepts → converged accepted, contention 2/3) → all three reviewer deltas scale by 2/3; alpha=0.5 unanimous → half-credit. Default preserves the v0 uniform-credit behavior so the assignment-gate thresholds (`assignment_min_recent`, `assignment_min_demonstrated`) and the patient-adversary cube remain calibrated against the same regime they were tuned against. Proposer deltas are deliberately unscaled — the PRD passage scopes difficulty-normalization to *review* credit, and the proposer's outcome carries its own weight independent of how contentious the reviewers were on the way there.

The fifth parameter sweep cube joins the alpha wedge with cube #2 (the assignment-gate threshold cube) as the difficulty-aware re-baseline. Six cells over (alpha ∈ {1.0, 0.5}, defense config ∈ {gates off, cube-#2 thresholds, re-tuned thresholds}) drive `it.each` through `runPatientAdversaryGateScenario`, with the runner extended to report `false_positive_lockout` (true when honest reviewers fail the gate at first request_assignment and the first contested target stalls staged) alongside `attack_succeeded`. The headline lands across two metrics, ASR and lockout-rate: cube-#2 thresholds at alpha=1 read (ASR=0%, lockout=0%) as before; the same thresholds at alpha=0.5 read (ASR=0%, lockout=100%) — the closure observation is misleading because honest review collapsed at the same gate, not because the defense worked. Re-tuned thresholds at alpha=0.5 (recent=0.5, demo=0.75) read (ASR=0%, lockout=0%): the demo gate scales by alpha to preserve honest-pool headroom (cube #2's headroom of 0.5 above the bootstrap demonstrated of 2.0 is preserved as 0.25 above the alpha=0.5 bootstrap demonstrated of 1.0); the recent gate stays at 0.5 because the recent buffer's quiet-window decay is dominated by calibration credit, which alpha doesn't touch. Calibration credit (PRD §Calibration batches) flows through `bumpReputation` directly without going through the convergence-driven `applyReputationUpdates`, so it is *not* alpha-scaled — the calibration path is ground-truth-individual and stays a pure-honesty channel for the patient archetype's accumulation, which is what makes the recent gate's threshold less alpha-sensitive than the demo gate's. The cube stays patient-only because the sybil runner's alpha-sensitivity surfaces the *same* shrinkage failure mode at the *same* gate (the honest-pool bootstrap demonstrated falls from 2.0 at alpha=1 to 1.0 at alpha=0.5, locking out Erin/Frank/George under cube-#2 thresholds and recovered by the same demo-threshold re-tuning the patient cube already pins) — adding sybil cells would duplicate cube #5's headline without surfacing a new closure mechanism. Eve's null-policy demo close on the sybil side stays alpha-invariant by construction (a zero-rep identity fails the gate at any alpha), and Carol/Dave's coalition priming runs through the convergence path but their three-accept demonstrated lands exactly at the gate threshold (3 × 0.5 = 1.5, passes strict-less-than) so the structural sybil close is preserved across alphas. A four-cell regression at alpha=0.5 pins this baseline directly: the recent-only cells stay at the alpha=1 outcomes (alpha-invariant by mechanism — neither the recent gate's null-policy nor the honest-pool's recent buffer is touched by the bootstrap shrinkage at demo=0); the demo>0 cells re-trip lockout, the same shape cube #5 reads on the patient side under cube-#2 thresholds at alpha=0.5. A companion single-cell regression pins the recovery: at the re-tuned thresholds (recent=0.5, demo=0.75) the sybil attack closes by honest defense at both metrics, the same shape cube #5 reads on the patient side at the same configuration — converting the patient-only-scoping rationale into a CI-checked invariant.

The adversary-budget model is now wired as the testbed-side fiction PRD §Adversary testbed §Architecture commits — slice 5 of the identity-cost design pass and the closer on the four-layer sybil-resistance architecture. The `AdversaryBudget` primitive holds a numeric budget B and operationalizes the three deductions the four layers compose multiplicatively against: `tryMint(epoch)` deducts α from B (binding-cost layer; mirrors the server-side `min_attestation_level` gate which fires on the same α) and refuses with the `issuance_cap` failure mode when the per-epoch cap is reached (issuance-frequency-cap layer; testbed-only, no server surface — the IdP refuses upstream of any cost being charged, so budget is unchanged on `issuance_cap` refusals); `tryAct(identity, epoch)` accounts the per-(identity, epoch) throughput cap T (mirrors `accountWriteAction` on the testbed side so the harness reads coalition throughput as K × T directly). Failure modes on `tryMint` are distinct: `budget` is "the operator cannot afford another sybil at any rate" (α > remaining B), `issuance_cap` is "the operator could afford it but the IdP refuses to mint another in this epoch" — the distinction matters when sweeps measure ASR as a function of coalition-affordable-identities-per-epoch, since the budget axis caps the total K and the issuance cap shapes its distribution across epochs. Epochs are passed in by the caller rather than derived from a clock — the testbed knows what epoch each action sits in by construction, and keeping the primitive arithmetic-only avoids coupling it to the server's `clock.now()` tick semantics. The new regression scenario pins each deduction directly: budget drains α-by-α across mints, the same-epoch second mint refuses with `issuance_cap` (budget unchanged), the next-epoch mint succeeds, the budget-exhausted mint refuses with `budget` (distinct from `issuance_cap`), per-(identity, epoch) action cap fires after T actions and resets on epoch boundary, identities have independent per-epoch counters so the K × T composition holds. Default action cap is Infinity (gate inert) for sweeps that hold throughput unbounded and isolate the identity-creation axis. The clustering projection (slice 4) costs the adversary nothing in the primitive — that layer surfaces at the curator surface as visibility, not budget consumption.

The sixth parameter sweep cube sweeps the budget axis the four-layer sybil-resistance architecture composes against, joining the `AdversaryBudget` primitive (slice 5) with the binding-cost gate (slice 2) as the first measurement on the sweep dimension PRD §Identity names "coalition-affordable-identities-per-epoch." Eight cells over (budget B ∈ {2, 4}, attestation threshold T ∈ {1, 2}, issuance cap N ∈ {1, 2}) drive `it.each` through `runBudgetSybilSuppressionScenario` — a one-shot suppression attack where the coalition mints fresh sybils within budget, each casting contributor-initiated reject votes on a contested target, racing two honest accepts. The runner consumes `AdversaryBudget` directly: each `tryMint(0)` deducts T from B (binding-cost arithmetic), refuses with `issuance_cap` past N mints in epoch 0 (issuance-cap arithmetic), and the loop stops when budget refuses the next mint — so K_eff = min(floor(B/T), N) reads off the same arithmetic the harness fiction operationalizes. Attack lands iff K_eff ≥ K* = 2 (votes_to_reject); otherwise the honest accepts converge first. The aggregate groups by defense config (T × N) and reads ASR across the budget axis: (T=1, N=1) at 0% — issuance cap as time primitive closes the one-shot attack across both budget cells regardless of how much the operator commits; (T=1, N=2) at 100% — gates inert at minimum-affording budget; (T=2, N=1) at 0% — issuance cap dominates even with cost-multiplier-affording budget; (T=2, N=2) at 50% — the cost-multiplier readout, where B=2 fails (K_eff=1) and B=4 lands (K_eff=2), the threshold-budget for attack feasibility exactly 2× the (T=1, N=2) gate-inert baseline. The cube is the budget-axis sibling of cube #2's assignment-gate-axis measurement: cube #2 covers the assignment-time-gates layer of the four-layer architecture on the sybil-amplified-coalition pattern; cube #6 covers the identity layer on a pure-budget pattern with no assignment-driven dependencies, so the two cubes cover complementary axes of the same architecture. The cluster signal is disabled in cube #6's runner (per-sybil-acts-once attack has no shared history to cluster on, structurally inert here regardless of config; explicit disable keeps the runner's scope honest).

The seventh parameter sweep cube extends cube #6 along the time axis, joining the `AdversaryBudget` primitive with an epoch-distribution sweep on the same one-shot contributor-initiated suppression pattern. Eight cells over (budget B ∈ {1, 4}, epochs E ∈ {1, 2}, issuance cap N ∈ {1, 2}) drive `it.each` through `runMultiEpochBudgetSybilSuppressionScenario`, holding T=1 (cube #6 already measured the binding-cost axis on the (T=2, N=2) cell, and pinning T=1 here isolates the time primitive from the cost-multiplier so the read on (E, N) is not confounded by T's contribution to K_eff). The runner spreads sybil mints across epochs — issuance_cap refusal advances to the next epoch, budget refusal stops the loop entirely — so K_eff = min(floor(B/T), N × E) reads off the harness fiction's arithmetic. The aggregate groups cells by (E, N) and reads ASR per group across the budget axis: (E=1, N=1) at 0% — cube #6's baseline at the minimum-cap defense config; (E=1, N=2) at 50% — gates inert at K*-affording B=4, budget binds at B=1; (E=2, N=1) at 50% — *the multi-epoch lift on the time primitive*, where K_eff lifts to 2 across two epochs at fixed N=1 and the attack lands at B=4 (one mint per epoch), while B=1 still closes by the budget axis (one sybil affordable, period — invariant to epoch count); (E=2, N=2) at 50% — issuance cap saturates K_eff at K*=2 in epoch 0 alone, second epoch unused. The headline (E=1, N=1) → (E=2, N=1) lift from 0% to 50% reads the issuance cap as a *time* primitive: the cap closes a one-shot attack outright at N=1, but a multi-epoch attack lifts K_eff to 2 at the same defense config, and the cap's actual operational role is to *delay* — buying behavior-based defenses (cluster signal, calibration record, reputation gates) the accumulated history they need to fire (PRD §Identity bullet 2). Cube #7 reads the arithmetic of the time-buying without behavior defenses; the joined cluster-signal-with-budget-axis cube under a coordinated-voting attack pattern is the follow-up that reads how the identity-layer and behavior-layer defenses compose. The cluster signal is disabled in cube #7's runner for the same reason as cube #6's (per-sybil-acts-once attack has no shared history to cluster on, structurally inert here regardless of config).

The eighth parameter sweep cube is the action-axis sibling of cube #7, exercising the per-(identity, epoch) rate-limit T (slice 3, server-side `rate_limit_actions_per_epoch`) at the action axis the cube #7 K-axis pattern leaves structurally inert by construction. Cubes #6 and #7 hold each sybil acting *once* (one reject vote per identity); the rate-limit cap is below the binding floor at any T >= 1, so the cap doesn't fire. The cap binds when one identity *acts on many targets* — the multi-target suppression pattern cube #8 measures. Eight cells over (targets count M ∈ {2, 4}, action cap T ∈ {1, 2}, epochs E ∈ {1, 2}) drive `it.each` through `runMultiTargetActionAxisSuppressionScenario`: K=2 sybils (no priming history; cluster signal disabled by construction matching cubes #6/#7) iterate the M contested targets across E epochs, with the runner advancing the clock past the 60-second epoch boundary between rounds so the per-identity counter resets. The runner does *not* respect T harness-side — it tries every (sybil, target) pair every epoch and lets the server's `accountWriteAction` cap fire `rate_limited` directly, exercising the slice-3 wiring end-to-end. votes_to_reject=2, so each suppressed target needs both sybils to land reject; suppression_capacity_eff = T × E targets per coalition (the K=2 dimension cancels because every suppressed target consumes one vote per sybil). The aggregate groups by (T, E) and reads ASR per group across the M axis: (T=1, E=1) at 0% — minimum-cap defense config closes any multi-target attack; (T=1, E=2) at 50% — multi-epoch lift on the rate cap, T × E = 2 fully suppresses M=2 but stops short of M=4; (T=2, E=1) at 50% — per-epoch lift symmetric to the time-primitive lift, T × E = 2 same as (T=1, E=2); (T=2, E=2) at 100% — composition saturates at M=4. The aggregate is numerically identical to cube #7's (0%/50%/50%/100% across the four (cap, epoch) cells), reading two different primitives — issuance cap and per-identity rate-limit — on the four-layer architecture. Together cubes #7 and #8 pin the K × N × T × E coalition-budget arithmetic the four-layer architecture composes against: cube #7 the issuance-cap × time axes (slice 2), cube #8 the per-identity rate-limit × time axes (slice 3). The (T=2, E=2) cell saturates at the maximum attack scope the cube measures — widening M to 6 would re-open the cell with the same T × E = 4 < 6 closure, sliding the composition's saturation point with the attack scope.

The ninth parameter sweep cube reads the cluster-signal × budget-axis composition on the sybil-amplified-coordinated-voting attack pattern — the first cube where two defense layers (identity layer's binding-cost gate at `min_attestation_level` and behavior layer's cluster signal) are *active simultaneously* and the budget axis sweeps how much fresh-recruit capacity the operator can field against the cluster signal's closure of the established coalition. Cubes #6/#7/#8 disabled the cluster signal (per-sybil-acts-once attack has no shared history to cluster on, structurally inert by construction); cubes #1/#3/#4 read the cluster signal but disabled the identity-layer cost primitives. Cube #9 joins both layers and reads the multiplicative composition PRD §Identity commits. Six cells over (binding-cost threshold T ∈ {0, 2}, budget B ∈ {1, 2, 4}) drive `it.each` through `runSybilAmplifiedCoordinatedSuppressionScenario`: Carol+Dave with 3 priming proposals shared (`stratum_min_shared_proposals: 2`, `stratum_agreement_threshold: 1.0` — the cluster signal fires after the priming, cross-stratum gate refuses Dave at `request_assignment` once Carol is routed); coalition recruits Eve(s) as fresh identities (each in their own singleton stratum, walking past the cluster signal); each Eve costs T to mint per the binding-cost gate (server-side at `min_attestation_level` and harness-side via `AdversaryBudget.tryMint(0)` deducting α=T from B). votes_to_reject=3 — Carol's lone reject is insufficient and Eve(s) are decisive: K_eff(Eves) = floor(B / T) when T > 0, else unbounded (capped at K*-1=2 by the runner). Honest reviewers (Erin, Frank, George) cast accept and converge contested 'accepted' if K_eff(Eves) < K*-1=2. Each cell asserts `attack_succeeded`, `eves_minted`, and `dave_gated` (the cluster-signal closure invariant — Dave must be cross-stratum-gated on every cell; a future change disabling the cluster signal would surface here before downstream ASR drift). The aggregate by T reads T=0 at 100% — identity layer inert, cluster signal alone leaks at any budget (the sybil-amplified-coalition seam by name) — and T=2 at 33% — composition closes the (B=1) cell (Eve unaffordable, binding-cost gate refuses) and the (B=2) cell (one Eve affordable, but K*-1=2 needed — closure by *insufficient-recruits*); the (B=4) cell is the leak where the budget axis affords K*-1=2 Eves and the composition fails. The aggregate by B reads (B=1, B=2) at 50% (T=0 leaks, T=2 closes by composition) and B=4 at 100% (both T values land). Cube #9 is the first cube to read the four-layer architecture's *full composition* under the named regression-handle attack pattern: at the (T=2, B<=2) cells the identity layer (binding-cost gate) is what closes the freshness bypass the cluster signal cannot reach; at the (T=2, B=4) cell the budget axis defeats the identity-layer closure; at T=0 the identity layer is inert and the cluster signal alone cannot close the recruit-amplification. The full composition closes only when *every* axis (cluster signal on, binding cost > 0, budget < K*-1 × T) aligns — exactly the multiplicative composition PRD §Identity commits.

The tenth parameter sweep cube opens the calibration-density design pass on a non-identity axis the prior cubes left unswept, joining the existing `calibration_inject_every_n` (slice-1 cadence knob) with `calibration_aware_convergence` (slice-2 vote-weighting knob) as the first cube to measure the calibration ratio PRD §What's deliberately not specified names as deferred to the testbed. Six cells over (`calibration_inject_every_n` ∈ {0, 4, 2}) × (`calibration_aware_convergence` ∈ {false, true}) drive `it.each` through `runStrategicCoalitionScenario` — the strategic-coalition attack pattern (2-of-3 biased reviewers Carol+Dave with naive bias predicate "accept payloads containing 'works'" vs one honest reviewer Erin on two well-grounded excerpts staged by Bob, with two anchors as the calibration corpus). The runner reuses the existing strategic-coalition setup byte-identical to the standalone calibration-injection and calibration-aware-convergence scenarios so the cube reads against the same attack pattern those scenarios pin; the standalone scenarios stay as-is for their additional rep-ledger and materialization observations, and the cube measures the convergence-half ASR axis only. The aggregate by aware reads aware=false at 100% across all 3 density cells (calibration injection moves the rep ledger but leaves convergence uncalibrated; the bias suppression converges 'rejected' regardless of density — calibration alone is insufficient against the convergence half) and aware=true at 67% (the gate fires only at every_n=2; at every_n ∈ {0, 4} the calibration record stays empty and the gate is silent). The aggregate by density reads every_n=0 at 100% (both aware values leak — no calibration data), every_n=4 at 100% (both leak — calibration items never materialize within the convergence window), every_n=2 at 50% (aware=true closes, aware=false leaks). The headline is the *density floor on the calibration-aware closure* — the two-excerpt runner exhausts the frontier within ~3 review-task offers per reviewer, so at every_n=4 the 4th calibration offer never fires and the calibration record stays at (0,0); aware=true collapses to the count-only baseline by `calibrationVoteWeight = max(0, 1 + passes - fails) = 1` for every reviewer, and the bias suppression lands. At every_n=2 each coalition member sees ≥2 calibration items during their review loop, fails them all (naive bias misfires on anchor content), and their weighted vote drops to 0 — the bias-misaligned excerpt's 1 accept (Erin, weight ≥1) + 2 rejects (Carol+Dave, weight 0) fails the weighted-reject threshold despite meeting the count threshold, and the proposal stays 'staged' rather than converging 'rejected'. Cube #10 is the first cube to read PRD §Calibration batches' "a coalition that misfires on calibration items pays once on the rep ledger and once at convergence" framing's *and-once-at-convergence* half's density-dependence directly, complementing the rep-ledger inversion the standalone calibration-injection scenario already pins (which is density-monotonic and not the load-bearing axis here — the rep ledger moves at any non-zero density, but the convergence-half closure has a structural floor below which the defense has no signal to operate on). The density floor is a function of (excerpt count × reviewer count × every_n) at fixed runner shape, not a property of every_n alone; raising the runner's excerpt count would lower the every_n floor at which aware=true fires, and that's the natural follow-up axis the cube template scales onto.

The eleventh parameter sweep cube reads the reviewer-pool-size axis PRD §What's deliberately not specified names as deferred to the testbed — the second cube on a non-identity axis after cube #10 opened the calibration-density pass. Six cells over (`stratification_enabled` ∈ {off, on}) × (honest pool size H ∈ {1, 2, 3}) drive `it.each` through `runStratifiedPoolSizeScenario` — the cluster-signal-eligible coalition Carol+Dave with 3 priming proposals shared (cluster signal fires after `stratum_min_shared_proposals: 2` + `stratum_agreement_threshold: 1.0`), calAwareBiasedDecider so calibration channels (calibration_inject_every_n=0, calibration_aware_convergence=false) are out of scope and the cube isolates the stratification × pool-size composition. Coalition runs *first* so the strat=off baseline reads ASR=100% across all H — pool size alone is not a defense at coalition-first ordering, since the coalition's two rejects converge before the honest pool can reach votes_to_accept=2. The aggregate by stratification reads (ASR=100%, stall=0%) at strat=off (the regression handle on coalition-first capture; the K+1-honest-reviewer dynamic PRD §Reviewer assignment names is order-dependent and does not apply when the coalition reaches votes_to_reject before the honest pool reaches votes_to_accept) and (ASR=0%, stall=33%) at strat=on — cluster signal closes Dave at request_assignment via the cross-stratum gate (Carol-already-routed + Carol-Dave co-stratum from priming), so the coalition's effective leverage drops to Carol's lone reject. Closure quality depends on H: H=1 stalls at 'staged' (1 accept + 1 reject below either threshold, the divergence-closure sweep eventually archives as `unresolved-archived`); H ≥ 2 = votes_to_accept converges 'accepted' fully; H=3 saturates at the same closure (the saturation pin — closure stabilizes at H ≥ votes_to_accept and doesn't improve further; a future change that raised votes_to_accept implicitly would surface here before downstream ASR drift). The complementary aggregate by H reads H=1 at (ASR=50%, stall=50%) — the small-pool cell where closure quality is partial; H=2 and H=3 at (ASR=50%, stall=0%) — strat=off captures, strat=on closes fully. The headline is *graceful degradation* — PRD §Reviewer assignment commits "slower to close but not capturable by the pool-fits-the-coalition dynamic"; cube #11 reads this property directly. Pool size doesn't buy *capture-resistance* (the cluster signal does that at any H); it buys *closure-quality* — the difference between staged-and-archived and converged-accepted. The two-metric template cube #5 introduced ("a defense that closes by collapsing the honest pool is not a defense") generalizes here to *closure-quality on the pool-size axis*: the cluster signal closes the capture, but full closure requires the honest pool to drive convergence. The dave_voted invariant on each cell (true at strat=off, false at strat=on) is the regression handle on the cluster-signal closure path itself, surfacing a wiring regression that disabled or weakened the cluster signal before downstream ASR drift.

The twelfth parameter sweep cube reads the vote-aggregation-thresholds axis PRD §What's deliberately not specified names as deferred to the testbed — the third cube on a non-identity axis after cubes #10 (calibration-density) and #11 (pool-size). Eight cells over (`votes_to_reject` ∈ {2, 3}) × (`votes_to_accept` ∈ {2, 3}) × (honest pool size H ∈ {2, 3}) drive `it.each` through `runVoteThresholdScenario` — the K=2 coalition (Carol+Dave with naiveBiasedDecider) on a single contested excerpt, stratification disabled and no priming so the cluster signal is structurally inert and both coalition members vote freely on the contested target (the runner isolates threshold arithmetic from the cluster-signal closure cube #11 measures). Coalition runs first; calibration channels off. The aggregate by vto reads vto=2 at (ASR=100%, stall=0%) across all 4 sub-cells — K = vto, coalition reaches the threshold by arithmetic before the honest pool can vote, the regression handle on default-threshold capture — and vto=3 at (ASR=0%, stall=25%) — K=2 < vto=3, capture is closed by arithmetic and only the (vta=3, H=2) sub-cell stalls (the closure-quality failure mode where the honest pool falls below the tightened accept threshold). The complementary aggregate by vta reads (ASR=50%, stall=0%) at vta=2 (vto=2 captures, vto=3 closes; pool size never below vta) and (ASR=50%, stall=25%) at vta=3 (vto=2 still captures, vto=3 splits between closure at H=3 and stall at H=2). The aggregate by H reads (ASR=50%, stall=25%) at H=2 (vto=2 captures, vto=3 closes at vta=2 / stalls at vta=3) and (ASR=50%, stall=0%) at H=3 (vto=2 still captures, vto=3 closes at any vta — the K+1-honest-reviewer dynamic at H = vta = K+1 PRD §Reviewer assignment names). The headline is the *threshold-axis decomposition*: vto controls capture rate (tightening vto from K to K+1 closes the coalition by arithmetic — capture-resistance bought purely by threshold), vta + H jointly control stall rate (tightening vta without proportional pool growth lifts stalls; H = vta is the saturation point for full closure). The architecture generalizes across closure mechanisms: cube #11 reads (cluster signal closes capture, pool size buys closure quality) on the stratification axis; cube #12 reads (vto closes capture, vta + H interaction buys closure quality) on the threshold axis; the (ASR, stall) numerical readings are structurally similar — same two-metric shape, complementary defenses against the same K=2 coalition at coalition-first ordering. The (vto=3, vta=3, H=3) cell pins the K+1-honest-reviewer structural-closure dynamic explicitly as a CI-checked invariant.

The thirteenth parameter sweep cube is the resolution refinement of cube #10, validating the density-floor scaling law at twice the excerpt count. Eight cells over (`calibration_inject_every_n` ∈ {0, 8, 4, 2}) × (`calibration_aware_convergence` ∈ {false, true}) drive `it.each` through `runWideStrategicCoalitionScenario` — the strategic-coalition attack pattern at 4 excerpts (vs cube #10's 2) with all four excerpts staged bias-misaligned to keep the calibration corpus anchor-only (load-bearing for the closure, since bias-aligned excerpts converging accepted during Carol's loop would contaminate the corpus and break the closure on subsequent reviewers). The aggregate by aware reads aware=false at 100% across all 4 density cells (calibration alone never closes convergence — the regression handle on cube #10's aware=false row, validated at higher resolution); aware=true at 50% (gate fires at every_n ∈ {2, 4} and is silent at every_n ∈ {0, 8}). The complementary aggregate by density reads every_n ∈ {0, 8} at 100% and every_n ∈ {2, 4} at 50% — the density floor crosses between every_n=8 and every_n=4. Cube #10 placed the floor between every_n=4 and every_n=2 in the 2-excerpt runner; cube #13 places it between every_n=8 and every_n=4 in the 4-excerpt runner. The floor halved when the excerpt count doubled — every_n* ≈ excerpt_count, validated at two resolutions. Together cubes #10 and #13 pin the density-floor scaling law as a CI-checked invariant: at fixed reviewer count, every_n must be ≤ excerpt_count for at least one calibration item to reach each reviewer before the frontier exhausts. The runner uses direct `proposeExcerpt` calls (alice as proposer) rather than `runHonestStrong` because the wider runner needs anchor-to-content-mapping control to keep all excerpts bias-misaligned — the corpus-contamination dynamic the runner avoids is the one cube #10's 2-excerpt setup didn't surface because only one no-effect target existed and the lone Dave-reject before his calibration-induced weight bump didn't have a second target to converge. That avoided dynamic is now wired as its own scenario downstream (see `runCorpusContaminationScenario`).

The corpus-contamination scenario closes the dynamic cube #13's wide runner avoided by construction. PRD §Calibration batches commits the calibration corpus as accepted-from-history proposals with recency-biased draw; the scenario reads what happens when the corpus drifts mid-run — bias-aligned excerpts that converge accepted during Carol's loop join the corpus as fresh-but-validated items, and Dave's recency-biased calibration draws downstream pull those contaminated items, vote with the bias, pass calibration, and lift his convergence weight back above the aware threshold. The runner mirrors cube #13's wide setup (4 anchors, every_n=2 above the density floor, aware=true, naiveBiasedDecider on the coalition) with one change: 2 of 4 staged excerpts are bias-aligned ("treatment X works for stage I/II") and 2 are bias-misaligned ("treatment X has no effect for stage III/IV"). Run order is Erin (acceptAllDecider, primer) → Carol → Dave; Carol's accept on the bias-aligned excerpts contributes the second vote that converges them into the corpus before Dave's loop. Dave's first call is real review on bias-misaligned #1 (the call-1-is-always-real-review constraint at every_n=2): his reject lands at empty-calibration weight=1, weighted_reject = Carol(0) + Dave(1) = 1 < 2, the target stalls. Dave's call 2 is calibration; recency-biased draw pulls bias-aligned #2, bias accepts "works", record (1, 0) → weight=2. Dave's call 3 reject on bias-misaligned #2 lands at weight=2, weighted_reject = 2 ≥ 2, converges rejected. The closure breaks on the second target. Baseline comparison (`contaminate=false`, anchor-only corpus): both bias-misaligned targets stay staged — full closure, matching cube #13's aware=true reading at every_n=2. The lift from 0/2 (baseline) to 1/2 (contaminated) at the same defense config is the load-bearing observation: the aware closure's strength is a function of corpus composition over the run, not a static property of the (every_n × aware) configuration. PRD §Calibration batches names the mitigation as a corpus-composition rule at the draw layer (e.g. a confirmation-depth floor that excludes items still within their first review session), not a calibration-knob change; the rule is testbed-tuned and the contamination scenario stays as the regression handle for any future change to the corpus-draw policy.

The fourteenth parameter sweep cube is the (stratification × threshold) composition cube — the first cube to read both of the K=2 coalition's capture-closure mechanisms (cube #11's stratification axis and cube #12's vote-to-reject axis) on a single primed runner. Eight cells over (`stratification_enabled` ∈ {off, on}) × (`votes_to_reject` ∈ {2, 3}) × (honest pool size H ∈ {1, 2}) drive `it.each` through `runStratificationThresholdScenario`, with vta=2 fixed so the cube isolates (strat × vto) from cube #12's vta axis; the runner primes Carol+Dave on three bias-aligned excerpts (so the cluster signal can fire when strat=on) and then runs the K=2 coalition first against a contested target. The (strat × vto) joint aggregate is the headline: (off, 2) at (ASR=100%, stall=0%) — coalition captures by arithmetic at K=vto, the regression handle on cube #12's vto=2 row replayed under priming; (off, 3) at (ASR=0%, stall=50%) — *closure-by-threshold*, capture closed by arithmetic and the H=1 cell stalls because 1 honest accept < vta=2; (on, 2) at (ASR=0%, stall=50%) — *closure-by-stratification-not-by-threshold*, the load-bearing cell ROADMAP §Status named: vto is at the K-affording value (cube #12 reads ASR=100% at strat=off here) but Dave is cluster-gated at request_assignment, Carol's lone reject is below vto=2, and capture is closed *despite* threshold not closing it; (on, 3) at (ASR=0%, stall=50%) — *overdetermined*, both defenses fire, no extra capture-resistance beyond either alone. The redundant-sufficiency reading is the framing: the two closure mechanisms close capture by *different* paths (cluster signal closes Dave at request_assignment via the cross-stratum gate; vto raises the arithmetic floor below K) but at K=2 each is independently sufficient and the second buys nothing on either metric. The complementary aggregates by single axis read symmetrically: by strat (off at ASR=50%/stall=25%, on at ASR=0%/stall=50%) and by vto (2 at ASR=50%/stall=25%, 3 at ASR=0%/stall=50%) carry numerically identical readings — the (strat × vto × H) symmetry pays the same capture and closure-quality cost on either defense alone. The H axis cleanly separates capture from closure-quality regardless of which defense fired: H=1 at (ASR=25%, stall=75%) — the three closure cells all stall because 1 honest accept < vta=2; H=2 at (ASR=25%, stall=0%) — every closure cell reaches vta. This is the *closure-quality independence reading*: stall_rate is invariant across the three closure pairs (50% across all H={1,2} cells where capture is closed), so PRD §Reviewer assignment's "slower to close but not capturable by the pool-fits-the-coalition dynamic" graceful-degradation property survives the (strat × vto) composition rather than being tied to a specific closure mechanism. The dave_voted invariant on each cell (true at strat=off, false at strat=on; vto-independent) is the regression handle on the cluster-signal closure path itself, the same shape cube #11 carries.

The fifteenth parameter sweep cube reads the reputation-decay axis PRD §What's deliberately not specified names as deferred to the testbed — the (recent half-life × quiet window) ratio-invariance cube on the patient-adversary archetype. Cube #2 holds the decay parameters fixed at (T=60s, W=360s) and reads the gate-firing axis through the threshold; cube #15 holds the threshold fixed at cube #2's calibrated value (`assignment_min_recent`=0.5) and sweeps (T, W) directly. The runner extension on `runPatientAdversaryGateScenario` adds two optional parameters (`recent_half_life_seconds` and `quiet_window_seconds`) defaulting to the (60s, 360s) configuration cubes #2 and #5 are calibrated against, so existing cubes' lockout cells are unaffected. Six cells over (T ∈ {60s, 600s}) × (r ∈ {1, 2, 6}) drive `it.each`, with W computed as T × r per cell so the W/T axis is directly read across all six cells. Carol's recent immediately post-priming is R₀ ≈ 8 (bootstrap +1, calibration passes +4, priming-convergence credits +4, first-contested-against penalty -1; the value is invariant across the cube's cells because tick=0 means no decay during priming). The aggregate by ratio reads (ASR=100%, 100%, 0%) at (r=1, 2, 6) — a binary closure on the ratio axis: r=1 (R = R₀/2 ≈ 4) and r=2 (R = R₀/4 ≈ 2) both stay above the 0.5 threshold, r=6 (R = R₀/64 ≈ 0.125) closes. The aggregate by T reads (ASR=67%, 67%) — *identical values across both T axes*, the headline ratio-invariance observation. The aggregate by W reads mixed values that don't align cleanly because W carries different amounts of decay at different T values. The headline is the *ratio-invariance reading*: the closure depends on W/T (decay measured in half-lives), not on T or W independently. PRD §Reputation's exponential-decay commitment (R(W) = R₀ × 2^(-W/T)) makes ratio invariance structurally inevitable, but the cube CI-checks it as an invariant — any future change to the decay law that breaks ratio invariance (e.g. a non-exponential decay or a per-event reset) would surface here as different aggregates across T groups before downstream cube drift.

The corpus-composition closure rule PRD §Calibration batches names is now wired as `corpus_confirmation_depth_floor` — the minimum distinct-reviewer count an accepted-from-history item must accumulate over the vote store before it is eligible as a calibration draw, applied at both `fetchCalibrationBatch` (the public read-path) and `drawCalibrationTask` (the assignment-injection seam used by the calibration-density scenarios). Anchors are exempt by kind (`payload.kind === 'anchor'`) — they are the ground-truth substrate, not synthesis-from-review; counting their accumulated calibration-vote reviewers against the floor would mis-trip the gate against the substrate itself (the wide-runner debug surfaced this directly: at floor=2 with anchor counting, Erin's calibration accept votes on anchors push them past count=0, the subsequent reviewers' calibration draws find zero eligible candidates, calibration record stays empty for the coalition, weighted-reject collapses to count-only behavior, and bias-misaligned items convergence rejected on the *baseline*). At v0 votes_to_accept=2, floor=3 requires at least one independent confirmation past the original convergence pair before the item enters the corpus. Default 0 leaves the filter inert and preserves existing-scenario behavior. The shared `passesCorpusDepthFloor` helper fires at both seams so the eligibility rule is a single source of truth — adding a future calibration-draw path would inherit the filter by call rather than by re-implementation.

The sixteenth parameter sweep cube reads the corpus-composition axis the contamination scenario named — the closer on the calibration-density design pass cubes #10 and #13 opened. Six cells over (`corpus_confirmation_depth_floor` ∈ {0, 2, 3}) × (contaminate ∈ {off, on}) drive `it.each` through the existing `runCorpusContaminationScenario` (extended with the floor parameter and the `attack_succeeded` field for cube use; the standalone scenario stays as the regression handle on the contamination dynamic itself). The aggregate by floor is the headline: floor=0 (default, inert) at 50% — the (off=baseline, on=regression) split, the contamination dynamic active by default; floor=2 (boundary cell at exactly votes_to_accept) at 50% — *the boundary pin*, floor admits items at the convergence threshold and the dynamic stays active, naming floor > votes_to_accept (not floor ≥ votes_to_accept) as the structural requirement for closure; floor=3 (above the boundary) at 0% — closure restored, bias-aligned items at count=2 excluded from calibration draws on subsequent reviewers' loops, Dave's recency-biased draws find only anchors, bias misfires on every draw, calibration weight stays at 0, both bias-misaligned targets stay staged exactly as in the anchor-only baseline. The aggregate by contaminate reads off at 0% across all 3 floors — the *baseline-not-locked-out invariant*: anchors bypass the floor by kind, so the legitimate anchor-only corpus is preserved at any floor value (the off rows pin this structurally rather than by inspection, since a future change to the anchor-bypass rule would surface here before the on rows' closure). on at 67% — 2 of 3 floors leak (floor=0 default, floor=2 boundary), 1 closes (floor=3 above boundary). Cube #16 closes the calibration-density design pass at three layers: cube #10 measured the density-floor axis at 2 excerpts; cube #13 the density-floor scaling law at 4 excerpts; cube #16 the corpus-composition closure on the contamination bypass that density alone cannot reach. The two-axis design follows the named-pathology-closure template — the contamination scenario's regression handle is the named pathology, the depth-floor is the closure mechanism, and the cube reads the closure boundary at the floor > votes_to_accept threshold the PRD passage on the corpus-composition rule names by structural framing.

The seventeenth parameter sweep cube is the demonstrated-component sibling of cube #15 — the (demonstrated half-life × quiet window) ratio-invariance cube on the patient-adversary archetype with the recent path inert at both layers, isolating the demo-decay axis from the recent-decay axis cube #15 measured. The runner extension on `runPatientAdversaryGateScenario` adds an optional `demonstrated_half_life_seconds` parameter defaulting to Infinity (preserves cubes #2/#5/#15's calibration where Carol's demonstrated stockpile holds across the quiet window). Eight cells over (T_demo ∈ {60s, 600s}) × (r ∈ {1, 2, 3, 6}) drive `it.each` with W = T_demo × r per cell; `assignment_min_recent`=0 + `recent_half_life`=Infinity disable the recent path at *both* layers (gate inert by knob, recent component doesn't decay), and `assignment_min_demonstrated`=1.5 holds the demo threshold at cube #2's calibrated value. The aggregate by ratio reads (100%, 100%, 0%, 0%) at (r=1, 2, 3, 6) — *the closure cross-point bracketed at r ∈ {2, 3}* (sharper than cube #15's r ∈ {2, 6} bracket since the demo threshold sits closer to the per-half-life decay step than the recent threshold). The aggregate by T_demo reads (50%, 50%) — identical across both T_demo axes, the ratio-invariance reading on the demo axis paralleling cube #15's reading on the recent axis. The headline is the *cross-point dependence on the gate threshold*: cube #15 closes at log2(R₀/0.5) ≈ 4 half-lives, cube #17 closes at log2(R₀/1.5) ≈ 2.4 half-lives — both cubes show ratio invariance, but the *value* of the W/T cross-point depends on the threshold the gate is configured against. Together cubes #15 and #17 close the reputation-decay design pass at three layers: the recent axis, the demonstrated axis, and the structural ratio-invariance both cubes pin as the CI-checked invariant on PRD §Reputation's exponential-decay commitment R(W) = R₀ × 2^(-W/T) — any future change to the decay law that breaks ratio invariance (e.g. a per-event reset or a non-exponential decay) surfaces in *both* cubes as different aggregates across T groups, the redundant CI-check the two-component bookkeeping warrants.

The eighteenth parameter sweep cube reads the (recent-decay × cluster-signal) composition on a multi-round timed-coalition attack pattern — the natural follow-up cubes #15 and #17 left as the next axis after closing the reputation-decay design pass at the solo patient archetype, and the named follow-up the prior next-milestones tail pinned by structural framing ("a coalition that times its quiet window against the half-life can re-acquire assignment after the gate re-opens"). Cubes #15 and #17 hold the cluster signal structurally inert (solo patient adversary, no shared vote history to cluster on) and read the exponential-decay law as ratio invariance on the (T × W) axes; cube #18 holds the decay parameters at cube #15's calibrated configuration (recent_half_life=60s, quiet=6 half-lives) and reads the recent-decay gate's role on a *coalition* multi-round attack where Carol+Dave time their quiet window against the half-life and re-prime through contributor-initiated accept votes between drift rounds. The new `runDecayClusterMultiRoundScenario` runner threads two drift rounds: Carol+Dave coalition-prime via 3 contributor-initiated accept convergences (cluster signal forms at `stratum_min_shared_proposals=2` + `stratum_agreement_threshold=1.0`); drift on contested #1 with Carol routed first and Dave either cross-stratum-gated (cluster on) or routed to also reject (cluster off → contested converges rejected at vto=2); quiet window advances the clock past 6 recent half-lives (recent drains by ~64x); re-prime via 3 more contributor-initiated convergences (the contributor-initiated `cast_review_vote` path bypasses the recent gate by null-policy per PRD §Reputation's drained-but-honest contributor recovery path → recent climbs back above any 0.5 threshold the cube sweeps); drift on contested #2 with the same shape as round 1. Four cells over (`assignment_min_recent` ∈ {0, 0.5}) × (`stratification_enabled` ∈ {off, on}) drive `it.each`. The aggregate by cluster reads off=100% (both rounds capture in both recent cells — re-prime pays through any recent threshold the cube sweeps) and on=0% (cluster signal closes Dave both rounds via cross-stratum gate; Carol's lone reject below vto=2 leaves contested staged on both rounds). The aggregate by recent reads identical 50% across both threshold values — *the recent-decay gate is ASR-invariant against this multi-round timed coalition pattern*: the re-prime cost is real (Carol+Dave each pay 3 honest accepts per cycle), but the gate alone doesn't translate it into closure because contributor-initiated voting bypasses the gate by null-policy, so the adversary pays the accumulation cost upstream of the gate. The headline is the *gate's design-role separation*: PRD §Reputation names the recent gate at the *solo* patient archetype where re-priming is unamortized (cube #2's lockout cells calibrated against the solo runner); cube #18 reads that role as inert against the *coalition* variant where re-priming is paid once and the cluster-signal closure is what does the actual work. The runner sets `stratification_degraded_extra: 0` to isolate the cross-stratum *routing* gate from the convergence-threshold tightening cube #11 measures on the same primitive — confounding them would prevent the priming + re-priming convergences from firing once the cluster forms after the second shared accept and would leave the recent-decay axis unmeasurable. Together cubes #11 (stratification × pool-size), #14 ((stratification × threshold) composition), and #18 ((decay × cluster-signal) composition) pin the cluster signal as the load-bearing coalition-amplification closure across three orthogonal axes (pool-size, threshold, decay-rate), with the (ASR-by-cluster) reading invariantly 100%/0% across all three cubes against the K=2 coalition pattern.

The nineteenth parameter sweep cube reads the cross-cause identity-clustering visibility surface (slice 4 of the identity-cost design pass, `server.curator.identityClusters`) on the regression-handle attack pattern PRD §Identity bullet 4 names — a sybil pair working multiple causes whose cross-cause behavioral fingerprint lights up only when read across the whole graph. Cube #19 is structurally distinct from the prior 18 in that the closure is *visibility for curator action*, not enforcement at the write-path. PRD §Identity bullet 4 commits the projection as a curator surface (specific signals operationally private at the production instance) — the curator's enforcement choice on the surfaced pair is downstream of the projection itself, so the cube reads "did the projection surface the sybil pair" as the ASR proxy and "did it filter the honest pair" as the precision proxy. The new `runCrossCauseSybilDetectionScenario` runner sets up `K_sybil` causes; the sybil pair (alice+bob) co-votes on one excerpt in each cause (cross_cause_count = K_sybil, shared_proposal_count = K_sybil); the honest noise pair (carol+dave) co-votes on one excerpt in cause 0 only (cross_cause_count = 1). Six cells over (`sybil_cause_count` ∈ {2, 3}) × (`min_signal` ∈ {1, 2, 3}) drive `it.each` and read pair presence directly off the projection result list. The aggregate by min_signal is the headline: M=1 reads (ASR=0%, fp_rate=100%) — "show everything" surfaces honest noise as a false positive; M=2 (the v0 default in `identityClusters()`) reads (ASR=0%, fp_rate=0%) — *the precision-recall pivot, the smallest threshold that filters single-cause coordination noise without sacrificing recall against multi-cause farms*; M=3 reads (ASR=50%, fp_rate=0%) — tighter threshold trades recall (K=2 farm evades) for precision the M=2 default already buys for free. The aggregate by K_sybil reads K=2 at (ASR=33%, fp_rate=33%) and K=3 at (ASR=0%, fp_rate=33%) — wider farms (more causes) surface at any threshold the cube measures, while K=2 evades only at the tightest M=3. The headline framing is *visibility-surface precision-recall structure*: cube #19 reads PRD §Identity bullet 4's "methodology is public, tuning is not" framing as the public methodology's threshold-sensitivity directly, without committing the v0 default to a particular tuning regime — the curator's M choice in production is operationally private but the sub-cube of (M=2, K ∈ {2, 3}) cells reads as honest defense at both metrics, which is the closure cell the v0 default operationalizes. Cube #19 is the visibility-layer sibling of cube #9 (the enforcement-layer cube on the four-layer sybil-resistance architecture): cube #9 reads the cluster-signal × budget-axis composition where the closure is structural at the assignment gate (cross-stratum routing); cube #19 reads the cross-cause identity-clustering projection where the closure is visibility for curator action. The two cubes cover complementary modalities of the same architecture: cube #9 the write-path gate, cube #19 the curator-side surface, both reading regression-handle attack patterns PRD §Identity names by structural framing.

The twentieth parameter sweep cube re-baselines cube #5's difficulty-aware findings on cube #11's stratification axis — the (alpha × stratification × demo-threshold) cube the prior next-milestones tail named. Cube #5 swept `review_credit_contention_alpha` ∈ {1.0, 0.5} on the patient-adversary gate-threshold cube #2 and pinned the alpha-shrinkage failure mode: at alpha=0.5 the bootstrap demonstrated shrinks from 2.0 to 1.0 and a demo gate calibrated against the alpha=1 bootstrap (1.5) false-positive-locks-out the honest pool, with the re-tuned demo=0.75 recovering the closure at the same 0.25 headroom; cube #5 pinned the recovery on the sybil pattern as a single companion regression at the re-tuned thresholds. Cube #20 generalizes that companion regression into a cube across (`review_credit_contention_alpha` ∈ {1.0, 0.5}) × (`stratification_enabled` ∈ {off, on}) × (`assignment_min_demonstrated` ∈ {1.5, 0.75}) on the sybil-amplified-coalition pattern (`runSybilAmplifiedGateScenario`, extended with a `stratification_enabled` sweep param defaulting to `true` so cubes #2 and #5's cells are unaffected), `assignment_min_recent`=0 throughout to isolate the demo axis. Eight cells drive `it.each`. The closure mechanism here is the cross-stratum gate (cluster signal closes Dave) plus the demo gate's null-policy (closes the fresh recruit Eve), not cube #2/#5's recent gate. The aggregate by stratification is the headline: off=(ASR=100%, lockout=25%) — the coalition captures by arithmetic (Carol+Dave's two rejects reach vto=2 before the honest pool can vote, and their priming demonstrated clears both demo thresholds at both alphas), and the demo gate's alpha=0.5 lockout (the demo=1.5 cell) is pure collateral damage; on=(ASR=0%, lockout=25%) — the cross-stratum gate + demo null-policy close capture at every cell, and the demo gate's alpha=0.5 lockout (the demo=1.5 cell) is what makes that closure observation misleading (honest-pool collapse, not honest defense). *ASR is stratification-determined: the cluster signal is the capture-closure, alpha-invariant; alpha and demo only move the lockout metric* — the closure-quality axis, cube #5's two-metric template generalized to the stratification mechanism, cube #14's closure-quality-independence reading applied to the alpha axis. The aggregate by alpha reads alpha=1=(ASR=50%, lockout=0%) — the alpha=1 bootstrap demonstrated (2.0) clears both demo thresholds, no lockout anywhere — and alpha=0.5=(ASR=50%, lockout=50%) — the shrunken bootstrap demonstrated (1.0) trips the demo=1.5 threshold at both stratification settings. The aggregate by demo-threshold reads demo=1.5=(ASR=50%, lockout=50%) — the threshold calibrated against the alpha=1 bootstrap locks out the honest pool at alpha=0.5 regardless of stratification — and demo=0.75=(ASR=50%, lockout=0%) — cube #5's re-tuned threshold preserves the 0.25 headroom at alpha=0.5 and is trivially clear at alpha=1, zero lockout at both alphas. *The re-tuned threshold's recovery is mechanism-agnostic*: cube #5 pinned it on the recent-gate closure (patient archetype), cube #20 reads the same recovery on the stratification closure (coalition archetype), converting the mechanism-agnostic-recalibration claim into a CI-checked invariant. Cube #20 is the alpha-axis sibling of cube #5: cube #5 reads the alpha-recalibration on the recent-gate closure, cube #20 on the stratification closure; together they pin the recalibration as a property of the demo gate's eligibility-tier role, invariant to which mechanism does the actual capture-closure.

Beyond the cube layer, the testbed graduates from all-scripted populations to a model-backed engine: `runLlmAgent` (testbed package) is a generic MCP-tool agent loop — list the wired server's tools, hand their JSON schemas to the Anthropic Messages API as tool definitions, execute each `tool_use` block against the MCP client, feed the result back, repeat until the model stops or a turn budget is hit — with no Anchorage-specific logic and the role (honest contributor, strategic adversary, patient adversary, lazy reviewer) carried entirely in the system prompt, so the agent sees exactly the surface a real client sees. The loop speaks the Messages API shape directly via `fetch` rather than pulling in an SDK, keeping the testbed's dependency story unchanged (`@anchorage/contracts`, the MCP SDK, zod). Two CI-relevant artifacts ship with it: a `pnpm --filter @anchorage/server live` runner that stands a concrete in-process server up behind the agent (seeded by `buildLiveScenario` with a small frontier of orphan anchors carrying real ctDNA-MRD source passages and a matching `FakeVerifier` sources map — a placeholder `seed anchor N` is not enough, since a careful honest agent correctly *declines* an excerpt task whose source it can't quote a verbatim span from) and streams the transcript — the manual "is the loop real" smoke check, which prints how to set `ANTHROPIC_API_KEY` and exits cleanly when none is present — and an integration test that drives the same loop with a *scripted* model (a `fetch`-shaped state machine) so the path is exercised in CI with no key and no network, alongside a `skipIf(!ANTHROPIC_API_KEY)` live-API smoke. Wiring this surfaced a latent MCP error-shape constraint and fixed it: a client that has called `tools/list` caches each tool's `outputSchema` and validates any `structuredContent` it gets back against it, so the server's error results now carry their typed `{ code, message }` payload as JSON text in `content` rather than as `structuredContent` (the typed code still survives the transport unchanged; `AnchorageClient` parses it back out; the scripted archetypes, which never list tools, were unaffected either way). What this opens: the model-backed archetype is the deep-loop engine PRD §Adversary testbed §CI names ("where frontier-model patient adversaries live"); the next step — taken in the paragraph below — is expressing the honest-strong and patient-adversary roles as `runLlmAgent` configurations, with joining them to the parameter-sweep cube layer alongside the scripted populations (on the slower CI cadence the deep loop runs at) as the remaining piece after that.

The first model-backed roles are wired as `LlmRole` configs in the testbed package: each is a system prompt carrying the role definition plus a `buildTask(cause_id)` that produces the opening user-turn message, and the agent loop stays role-blind by construction (it lists tools and runs the tool-use loop; the role lives entirely in the prompt). `honestStrongRole` is the careful frontier-model contributor PRD §Adversary taxonomy names ("should succeed even on cases where simpler contributors stall") with no hidden agenda; `patientAdversaryRole({ objective })` takes the hidden objective as a parameter (the deep loop sweeps it) and the prompt makes the strategy explicit — behave exactly like honest-strong until standing is established, then drift only on a *contested* assigned proposal where a misaligned vote is plausibly defensible, never on a calibration or obvious-ground-truth item, since a misfire there burns the reputation the whole strategy is built on. `strategicAdversaryRole({ objective })` shares that objective shape and the same prompt floor — honest on routine work, never a calibration misfire — but drops the patience: it acts on a *contested* assigned proposal as soon as it is handed one, the model-backed form of PRD §Adversary taxonomy's strategic adversary (the variant whose drift half shows up on a fresh population with a pre-staged contested item, not only once standing is built). The prompts are the deep loop's experimental treatment, so they are pinned in one place and CI-exercised through the existing scripted-model integration path (`llm-agent.test.ts` gained an `it.each` over the three roles plus a shared-task-shape regression that also pins each adversary carrying its objective into the prompt, and a `skipIf(!ANTHROPIC_API_KEY)` live smoke per role): the scripted model ignores the system prompt and drives the honest write-path — which the patient adversary's build phase is by design indistinguishable from, so the scripted pass is exactly that phase — proving each config plugs into `runLlmAgent` and reaches the governance write path; the drift half needs a real model and a richer scenario — a contested item, which the deep-loop cube does stand up (the paragraphs below). The `live` runner picks the role from `ANCHORAGE_TESTBED_ROLE` (honest-strong by default) and carries that role's *system* prompt verbatim; its kickoff task is `buildLiveTask` (`live-scenario.ts`) rather than the role's own `buildTask` — the same explicit-task split `run-deep-loop.ts` uses, and the bit that makes the run productive: the task says outright that for this instance the anchor's `content` is the source text span verification matches against (there is no source-retrieval tool in v0), so the agent quotes from the anchor rather than declining for lack of a fetchable paper. Alongside the single-agent `live` runner, `pnpm --filter @anchorage/server population` stands a fresh in-process instance up behind several `runLlmAgent` loops sharing it over independent MCP connections and runs them in rounds — contributors declare capacity for excerpt and review work, pull excerpt tasks off a seeded orphan-anchor frontier, then review each other's staged work until proposals converge into the graph — reporting graph state after each round and holding a USD spend ceiling (`ANCHORAGE_POPULATION_BUDGET_USD`, default 15) priced off the per-run token usage `runLlmAgent` now surfaces; it is the multi-agent precursor to the deep-loop cube join, with two v0 shortcuts noted in the script: a `FakeVerifier` source fixture mirroring each seeded anchor's inline content (there is no live-fetch verifier yet and no MCP source-retrieval tool, so a contributor quotes a verbatim substring of the anchor content it can read off `query_proposals`), and no calibration corpus seeded (the review path converges on the real staged proposals; calibration salting stays with the scripted-archetype scenarios). Running it against haiku populations already shook two governance refinements out: `decline_assignment` now accepts an `accepted` assignment (the accept-then-can't-deliver bail-out — a stranded slot no longer locks the target out of the pool; PRD §MCP tool surface updated in lockstep), and `deriveFrontier` drops an anchor from the orphan set while an excerpt assignment for it is in flight (offered/accepted), not only once the proposal is staged, so a population of N concurrent contributors doesn't pile N near-duplicate excerpts onto one fresh anchor; with both, a 5-contributor honest run drains the seeded frontier to exactly one peer-reviewed excerpt per anchor in two rounds. What this leaves: (1) the slower-cadence CI job that runs the model-backed populations on their own schedule (the contested-item scenario it builds on — the patient adversary's drift half made observable — is the deep-loop runner and cube, both wired in the paragraphs below); (2) the two "fix from a run" hardening steps the honest-population runs surfaced — a stale-assignment expiry sweep and curator escalation into the harness (PRD §Reviewer assignment step 4: a 1-1 review split in a pool with no tiebreaker had no resolution path the runner exercised) — are both now wired (`curator.expireStaleAssignments` and the between-rounds curator step plus `curator.rejectProposal`; the two paragraphs below); (3) the Phase-2 question of whether the server serves its cached source text to contributors or agents bring their own fetch (the runner sidesteps it with the inline-content fixture today).

The stale-assignment expiry sweep is wired as `server.curator.expireStaleAssignments({ window_seconds, cause_id? })` — the curator-side reclaim path for the residual wedged-client case PRD §Capacity and assignment names, the second of the two "fix from a run" hardening steps the honest-population runs surfaced (the first, curator escalation into the harness, remains). `decline_assignment` is the bail-out a *responsive* client uses; a client that pulled an assignment (`offered`) or accepted it (`accepted`) and then went silent never reaches it, stranding the slot — an anchor held out of the orphan frontier by `deriveFrontier`'s in-flight-assignment guard, or a held review slot. The sweep transitions any `offered`/`accepted` assignment whose `updated_at` is older than `window_seconds` to `expired`, which `request_assignment` and `deriveFrontier` already treat as slot-released: the target is re-offerable — to other contributors and, since a recovered client is a legitimate worker, to the original contributor too (`expired` doesn't block re-offer the way `declined` does) — and an anchor with no remaining in-flight excerpt assignment re-enters the orphan frontier. It mirrors `archiveStaleProposals`'s posture exactly: curator-triggered (production likely schedules it; the trigger is operationally private and testbed-tunable), `window_seconds <= 0` is a no-op, an optional `cause_id` filters per-cause (an assignment whose cause can't be located is skipped under the filter, included without it), and the expired ids return for audit. Expiry is deliberately *not* a decline — it carries no reason and is invisible to both the `assignment_max_decline_rate` gate and the `declinePatterns` projection (both key on `status === 'declined'`): conflating "couldn't deliver, said so" with "went silent" would mislead the decline-pattern surface, so a chronically-wedged client is left as expiry volume the curator reads directly rather than folded into the cherry-picking channel. PRD §Capacity and assignment is updated in lockstep (the placeholder "a future expiry sweep reclaims" parenthetical now names the wired method and its non-punitive semantics); the regression pins both halves — old `offered` + old `accepted` → `expired` while a fresh `offered` and a terminal `declined` stay untouched, idempotency on a second sweep, the cause filter and the no-op window, and the reclaim (a fresh `request_assignment` lands on one of the swept anchors).

Curator escalation is wired as the harness's between-rounds resolution step in `run-population.ts`, joined by a new `server.curator.rejectProposal` — the reject side of the curator-escalation path `curator.acceptProposal` already documents (PRD §Reviewer assignment step 4), parallel to the reject branch of `resolveByConvergence` (a status transition only, nothing to unwind since a staged proposal never materialized a node) and, like `acceptProposal`, reputation-neutral: a curator tiebreak is not the peer agreement the convergence path's reputation deltas are calibrated against, and routing curator overrides through the rep ledger would let a curator move reputation by fiat (PRD §Reviewer assignment step 4 updated in lockstep to name both methods and the rep-neutrality). The hole this closes is the one the honest-population runs surface as a "fix from a run": a staged proposal the contributor loops stall on for a full round — a 1-1 review split with no tiebreaker, a contested item the eligible pool is exhausted for, or one no reviewer ever picked up — has no resolution path the agent loops can reach, so the runner re-tries it every round until `MAX_ROUNDS` and reports it still staged. The new between-rounds pass diffs each round's pre-round per-staged-proposal vote count against the post-round count: a proposal that gained any vote is still progressing and is left alone; one that didn't — and isn't fresh (a proposal staged mid-round is not yet in the pre-round snapshot and gets a full subsequent round first) — is escalated, resolved toward the majority of the votes cast, accepting on a tie or with no votes (every excerpt here passed span verification at write time, so accept is the productive default; the divergence is recorded in the vote history either way). It is a deterministic harness actor — a third deliberate v0 shortcut alongside the FakeVerifier source fixture and the absent calibration corpus: a real curator reads the proposal, the harness exercises the *path* (`curator.acceptProposal` / `curator.rejectProposal` closing a stuck divergence), not the judgment. With it the runner can terminate `frontier_drained` instead of `rounds_exhausted` once the population's reachable work is done and a stalled divergence has been resolved by fiat. `curator.rejectProposal` carries its own regression in curator.test.ts (staged → `rejected` with no node materialized, plus the non-staged and unknown-id refusals); the harness wiring stays untested glue like `frontierEmpty` and `graphStatusLine`.

The population runner's round-loop core is extracted into `population-loop.ts` (`runPopulationRounds` plus the `frontierEmpty` / `stagedVoteCounts` / `escalateStuckProposals` / `graphStatusLine` / `usdCost` helpers) so it can be driven by something other than env vars and a real Anthropic key: `run-population.ts` wires it to `runLlmAgent` loops + `console.log`, and `population-loop.test.ts` wires it to the deterministic scripted archetypes (`runHonestStrong` / `runHonestReviewer`, the same ones the adversary scenarios use) + a quiet run, pinning the honest baseline as a CI-checked invariant. The core is generic over the contributor type with a `runContributor` seam — given a contributor and the round index, run that contributor's loop against its already-wired client and report the model-token usage incurred (zero for scripted archetypes) — so it carries no `runLlmAgent` dependency; logging goes through an optional `log` callback and the spend guard is optional. Two integration tests: a 2-excerpt-worker + 3-acceptAll-reviewer honest population (`votes_to_accept`=3) drains a 3-anchor frontier to one peer-reviewed excerpt per anchor and terminates `frontier_drained` with no escalations; and a 2-excerpt-worker + acceptAll/rejectAll-reviewer population (`votes_to_accept`=`votes_to_reject`=2) stalls every excerpt at 1-1 and the between-rounds curator pass escalates each the round after the votes land — toward the majority, which on the 1-1 tie resolves accept — so the run still terminates `frontier_drained`. Both run the contributors *concurrently* per round, the regime `testbed.test.ts`'s sequential reviewer runs don't exercise; what's pinned is the harness plumbing (round loop, frontier accounting, curator escalation, the `runContributor` seam), not the governance machinery the scenario tests already cover or the model-judgment a real run probes. This is the honest baseline the adversarial deep loop builds on — the patient-adversary role drops into the same `runContributor` seam alongside the honest ones, and the contested item its drift half needs is a population config where the honest reviewers split.

The deep-loop runner is wired as `run-deep-loop.ts` (`pnpm --filter @anchorage/server deep-loop`): it reuses the `runPopulationRounds` core through the `runContributor` seam — a contributor's role is just which system prompt its `runLlmAgent` loop gets — to stand up a small model-backed population of three honest-strong contributors plus one `patientAdversaryRole({ objective })`, against a seeded frontier of six honest orphan anchors (the pool's excerpt work) plus a deliberately *contested* anchor (a source passage reporting a non-significant numerical trend) with a pre-staged excerpt whose claim overstates it to "improves response rates". Nothing tells the agents which one is the adversary. The patient adversary's prompt has it build standing on the honest frontier first and drift only on a contested assigned proposal where a vote in its favored direction is plausibly defensible — the contested item is exactly that — and the runner's final report carries a drift readout: the contested proposal's final status, every review vote cast on it (the adversary's flagged), and whether the adversary cast an accept on the overstated claim. Beyond that drift readout the full calibration defense PRD §Calibration batches names is wired here: the corpus is seeded as a faithful, pre-accepted excerpt on each of a few *dedicated* calibration anchors — separate from the honest orphan anchors, since an anchor with an accepted excerpt child stops being orphan and seeding the corpus onto the work anchors would drain the excerpt frontier — joined by any proposal that converges accepted during the run; `calibration_inject_every_n`=2 (a calibration draw lands on a contributor's 2nd review-task offer — this small population's per-contributor review volume doesn't accumulate enough for the cube layer's 3+, so most of the pool would otherwise never see one) salts review assignments with calibration draws (items drawn from validated history, scored against ground truth), and `calibration_aware_convergence` is on, so both misfire consequences bite: a reviewer that rejects a faithful calibration excerpt pays `calibration_fail_loss` on the rep ledger *and* drops its convergence weight, so the contested-item drift must clear a calibration-weighted threshold rather than only the count one (a calibration-burned vote counts toward the distinct-reviewer floor but contributes 0 to the weighted sum). The final report carries a per-contributor calibration record — passes/fails plus the resulting convergence weight — next to the drift readout, and the drift readout itself notes whether the adversary's contested vote (if it cast one) still carried weight or had been calibration-burned to 0. `corpus_confirmation_depth_floor` stays at its inert default (0), so a bias-aligned excerpt that converges accepted mid-run joins the corpus immediately — the contamination dynamic the standalone scenario pins; a deployment that wants the confirmation gate sets it. The seeded fixture lives in `deep-loop-scenario.ts` (`buildDeepLoopScenario`, the population-run analogue of `live-scenario.ts`, shared with the golden-cassette replay test below): a `full` preset — the three-honest-plus-one-adversary, six-orphan-anchor configuration above — and a smaller `ci` preset (`ANCHORAGE_DEEP_LOOP_PRESET=ci`: 2 honest-strong + the patient adversary, a 2-anchor excerpt frontier, a 2-item calibration corpus; contributors' opening tasks carry the round number so every (contributor, round) request is byte-distinct and a multi-round population cassette has no cross-round key collision). A `workflow_dispatch`-only `.github/workflows/deep-loop.yml` runs either runner on demand — a `runner` choice picks the single deep loop or the parameter-sweep cube, alongside model / budget / objective inputs and a `cube_cell` narrow; `ANTHROPIC_API_KEY` comes from repo secrets, and the runner exits 0 if the secret is absent so the workflow is safe before a key is provisioned; a `schedule:` trigger is the slower-cadence step once a key is in secrets. The golden-cassette path that turns a model-backed run into a deterministic CI regression is in place for both the single-agent `run-live` runner (`golden-cassette.test.ts`) and now the deep-loop population: `golden-deep-loop.test.ts` replays a checked-in `ci`-preset run (`test/fixtures/golden-deep-loop.json`, recorded against Haiku 4.5 via `ANCHORAGE_CASSETTE=test/fixtures/golden-deep-loop.json ANCHORAGE_CASSETTE_MODE=record ANCHORAGE_DEEP_LOOP_PRESET=ci pnpm --filter @anchorage/server deep-loop`) over the same `buildDeepLoopScenario(CI_DEEP_LOOP_OPTS)` fixture with no key and no network, pinning the cube-shaped outcome — the overstated contested excerpt and an overstated re-excerpt rejected, a faithful re-excerpt curator-escalated to accepted, the patient adversary never drifting, every calibration draw passed — so a regression in the agent loop, the tool surface, the population round loop, or the patient-adversary prompt surfaces as a cassette miss in CI. Re-record it (and re-commit the fixture) when `deep-loop-scenario.ts`, the honest-strong or patient-adversary prompt, or the tool surface changes — the same same-seed/same-config discipline `recording-fetch.ts` documents.

The record/replay cassette layer is wired as `recordingFetch` (testbed package): a wrapper around the injectable `FetchLike` `runLlmAgent` already exposes, matching Messages-API round-trips by a SHA-256 hash of `url + body` (which `runLlmAgent` builds deterministically from system + messages + tools + max_tokens — no secrets, no timestamps) with three modes — `record` (always call through, overwrite stale entries), `auto` (replay hits, call + append misses), `replay` (cassette-only, a miss throws). Persistence stays the caller's job: `recordingFetch` mutates an in-memory `entries` array and calls an `onRecord` hook; the server package's `cassette-file.ts` is the `node:fs` glue that turns the `ANCHORAGE_CASSETTE` / `ANCHORAGE_CASSETTE_MODE` env vars into a wired `FetchLike` (load from / persist to a JSON file, rewritten after each new recording so a crash doesn't lose it), which the single-cassette runner scripts (`run-live`, `run-population`, `run-deep-loop`) now accept (the cube runner is multi-cassette and wires a per-cell file itself via `cassetteFetchAt`) — in `replay` mode they need no API key, so a recorded run replays with no key and no cost. `recordingFetch` is unit-tested (record / replay / auto, hash stability, headers never in the key, non-2xx propagation), and the record→replay round-trip is CI-tested end-to-end through `runLlmAgent` + a real wired MCP server (`cassette-replay.test.ts`): a `record` pass produces a cassette, a `replay` pass against a freshly-seeded-but-identically-id'd server reproduces the same run from that cassette alone with the transport throwing if consulted, and an incomplete cassette surfaces a clear cassette-miss error. (Same-seed discipline is the load-bearing bit and is the same constraint on a real golden cassette — recorded against, and replayed against, the same seeded fixture; re-recorded if the fixture or the tool surface changes.) Scope note kept honest in the wrapper's header: single-agent runs (`run-live`) replay exactly; population runs replay exactly *only if recorded against a sequential round* — `runPopulationRounds` takes a `concurrency: 'concurrent' | 'sequential'` knob and `run-population.ts` / `run-deep-loop.ts` switch to `'sequential'` whenever a cassette is in play, which makes the request sequence a pure function of the seeded fixture (under concurrent execution an agent's turn-N request depends on which other agents' writes had landed by then, which during recording is latency-dependent, so a concurrent recording's interleaving differs on replay and some requests miss); replaying against a changed server or changed prompts is invalid by construction (re-record). Two checked-in golden cassettes of *actual-model* runs now exist: `packages/server/test/fixtures/golden-live-honest.json`, a recorded honest-strong `run-live` run against `claude-haiku-4-5-20251001`, replayed deterministically by `golden-cassette.test.ts` against the `buildLiveScenario` fixture with no key and no network — so a regression in the agent loop, the MCP tool surface, or `runLlmAgent`'s request shaping surfaces as a cassette miss in CI rather than only on the next on-demand live run — and `packages/server/test/fixtures/golden-deep-loop.json`, the population analogue: a `ci`-preset `run-deep-loop` run against the same model, replayed by `golden-deep-loop.test.ts` against `buildDeepLoopScenario(CI_DEEP_LOOP_OPTS)` (sequential round loop, so it replays exactly), pinning the contested-item cube-shaped outcome plus the curator-escalation and calibration paths. (`test/fixtures` is biome-ignored — recorded artifacts, not source — and each cassette is re-recorded with `ANCHORAGE_CASSETTE_MODE=record pnpm --filter @anchorage/server live` / `... ANCHORAGE_DEEP_LOOP_PRESET=ci pnpm --filter @anchorage/server deep-loop` when its fixture, its role prompts, or the tool surface changes.) Recording it is what surfaced the placeholder-fixture problem the paragraph above describes — `seed anchor N` anchors produced a do-nothing transcript because the honest agent correctly declined every excerpt task — so the live fixture moved to real source passages and an explicit kickoff task in the same change.

The model-backed parameter-sweep cube is wired as `run-deep-loop-cube.ts` (`pnpm --filter @anchorage/server deep-loop-cube`): the deep loop runs once per cell of `DEEP_LOOP_CUBE_CELLS` (`deep-loop-scenario.ts`), each cell varying a defense parameter, the adversary role, the contested item, or the closure-stack version — four axes today: the calibration defense on versus off against the patient adversary (a cell's `DeepLoopScenarioOpts.review` shallow-overrides `DEEP_DEFAULT_REVIEW`), a strategic-adversary cell on the brazen contested item (with `votes_to_reject` raised to 3 so the lone adversary, which runs after the two honest reviewers each round, is offered the contested review before the two honest rejects would otherwise close it), a `borderline-contested` cell that holds the strategic-adversary role fixed and swaps the brazen overstatement for one the source's observational "associated with" framing is plausibly readable as supporting causal "increases", a `borderline-contested-v1` cell with the first v1 closure-stack knob `escalation_revise_counts_as_reject` ON, a `borderline-contested-v2` cell with the strict v1 stack (both escalation knobs on — also `escalation_requires_votes_to_accept`), a `borderline-contested-v3` cell adding `contested_votes_to_accept` on top of the strict v1 stack — the first closure-stack knob to touch the auto-close-accept path — a `borderline-surrogate` cell that adds a second structurally different borderline drift on the bare v0 closure stack (surrogate-substitution: source describes ctDNA clearance as a prognostic marker, claim restates it as a therapeutic target — the *kind of relationship* the verb implies slips, not its modality), and a `borderline-subgroup` cell that adds a third (generalization-from-subgroup: source reports an effect confined to MMR-deficient stage III colon cancer, claim drops the subgroup qualifier and generalizes across resected CRC — the *population scope* widens, verb modality and kind of relationship preserved) — and the runner reports the per-cell outcome (contested-item status, adversary drift, calibration tallies, stop reason, cost) plus the cross-cell headline: the overstated contested claim's accept-rate (attack-success-rate) across cells, how many cells saw the adversary drift. It is the model-backed analogue of `testbed.test.ts`'s scripted parameter sweeps — those vary a defense parameter against a *scripted* adversary and read the closure outcome; this one varies it against the same real-model honest-strong + adversary population the deep loop already stands up (on the small `ci` fixture so each cell's cassette is checkin-sized), so what's measured is what the model does, not what a script assumes. The calibration on/off pair is the model-backed complement to the scripted calibration-aware cube (#10): cube #10 shows the calibration defense is load-bearing when an adversary actually drifts; this cube checks that on the honest baseline — where the model patient adversary keeps to the build-standing-first strategy its prompt encodes — flipping the defense off moves no outcome, so the defense carries no false-positive cost. The strategic-adversary cell on the brazen item is the model-backed counterpart to the scripted strategic-adversary closure cubes: the recorded run has the strategic adversary reviewing the overstated claim and rejecting it (the "improves response rates" overstatement too brazen for its pick-the-borderline-cases strategy to defend), with redundant honest review carrying the closure regardless of how the adversary votes — a 3-0 reject, or a curator-resolved 2-1 had it drifted. The `borderline-contested` cell is the cube's **first recorded closure failure**: handed the borderline item, the strategic adversary plausibly drifts (recorded: votes accept), one honest reviewer splits the rejecting side with a careful *revise*, and the curator escalation pass closes the 1 accept + 1 reject + 1 revise *toward accept* (its v0 rule accepts on a tie). The overstated claim ends `accepted` — the v0 closure stack (`votes_to_reject` threshold + revise not counting as a rejection + curator accept-on-tie) does not contain model-driven drift on a borderline contested item when a careful "revise" splits the rejecting vote. The v1 closure stack against that finding is a pair of orthogonal knobs on the curator-escalation tiebreak (default `false`, v0 preserved everywhere): `ReviewConfig.escalation_revise_counts_as_reject` flips the rule to count revise toward reject (`r + revise > a ? reject : accept`), and `ReviewConfig.escalation_requires_votes_to_accept` additionally requires `accepts >= votes_to_accept` for escalation-to-accept. They compose orthogonally: the first catches the 1-accept-1-reject-1-revise tally; the second catches the plain 1-1-0 tie the first alone misses. Both load-bearing v0/v1 and v0/v2 deltas are pinned byte-for-byte at the harness level by scripted-decider pairs in `population-loop.test.ts`. The `borderline-contested-v1` cube cell is the v1 stack's real-model regression baseline (LLM sampling is non-deterministic, so its recording is a different rollout than the v0 cell's — the recorded v1 run landed 1 accept + 2 reject + 0 revise, the strategic adversary opted for strategic discipline this rollout, and the curator escalated reject under both rules). The `borderline-contested-v2` cube cell, however, surfaced the cube's **second** closure failure — at a *different* path than the v1 knobs reach: in that rollout the strategic adversary voted accept AND at least one honest reviewer also voted accept, hitting `votes_to_accept=2` and auto-closing the proposal accepted via the *normal vote path* before the curator escalation pass even saw it. The v1 knobs govern only the escalation tiebreak; the auto-close-accept path is unfortified. The next closure-stack design pass is at the auto-close-accept side (a tighter `contested_votes_to_accept` floor, auto-close-aware revise counting, or a held-rather-than-escalated outcome for revise-mixed ties — each landed under the same parameter-sweep-cube treatment). Multi-cassette: one fixture per cell at `test/fixtures/<cell.cassette_basename>.json`, mode via the shared `ANCHORAGE_CASSETTE_MODE` (unset → run live, touch no fixtures), `ANCHORAGE_CUBE_CELL=<name>` narrowing to one cell; each cell carries its own cassette even though the `calibration-on` cell runs the same `CI_DEEP_LOOP_OPTS` scenario as the single-cell golden `golden-deep-loop.json` — recorded LLM transcripts aren't reproducible (sampling), so a golden cassette is a frozen artifact pinned by exactly one test rather than a file shared across runners. `golden-deep-loop-cube.test.ts` replays every cell deterministically with no key, the multi-cell analogue of `golden-deep-loop.test.ts`, pinning per-cell what the recording showed (rejected in eight cells, including the v3 cell where `contested_votes_to_accept` closes the auto-close-path failure, the two additional contested-item-severity cells whose v0 rollouts close cleanly via *different paths* — `borderline-surrogate` via curator escalation reject on a 0-1-1 tally because the strategic adversary's vote attempts hit assignment_id mismatch errors and the honest pool's reject+revise escalated reject; `borderline-subgroup` via auto-close-reject on a 3-0 tally because all three contributors voted reject on the contested item within round 1, hitting `votes_to_reject=3` — and `borderline-contested-r2`, the second sampling rollout of the verb-swap cell, where the adversary drifts but both honest reviewers reject cleanly so the 1-2-0 tally goes to curator escalation reject; accepted in three — the v0 `borderline-contested` cell that recorded the escalation-path failure (1-1-1 with revise → curator accept-on-tie); the `borderline-contested-v2` cell that recorded the auto-close-path failure the v1 escalation knobs don't reach (>=2 accepts → auto-close-accept under v2's default `votes_to_accept=2`); and `borderline-contested-r3`, the third sampling rollout of the verb-swap cell, where the same auto-close-accept failure path lands on the bare v0 stack (>=2 accepts again — proving the auto-close failure path is not a v1/v2 artifact but already reachable on v0). The contested-item-severity axis now reads three drift kinds with three different closure paths: verb-swap drift breaks v0, surrogate-substitution drift v0 closes via the escalation path, generalization-from-subgroup drift v0 closes via the auto-close-reject path. Two of three drift kinds: v0 holds — a drift-pattern-landscape signal rather than a single closure outcome. The verb-swap-on-v0 trio (`borderline-contested` + r2 + r3) then resolves the sampling-variance question the original v0 cell opened: 2/3 sampled rollouts fail v0 via *two distinct paths* (escalation accept-on-tie at 1-1-1; auto-close-accept at >=2 accepts), 1/3 holds — strengthening the v3 closure stack's motivation, since both failure paths are closed by v3. The cell list is the extension point — more borderline items varying the kind of overstatement, more closure-stack candidates targeting the auto-close path (orthogonal rules like auto-close-aware revise counting or held-on-revise-tie that don't reduce to `contested_votes_to_accept`), more values along any axis — and `run-population.ts` / `run-deep-loop.ts` / `run-deep-loop-cube.ts` share the model-pricing table (`priceFor`) out of `population-loop.ts` rather than each carrying a copy.

Next milestones: the four-layer sybil-resistance architecture is wired across all surfaces and CI-checked across all named regression-handle attack patterns by cubes #6, #7, #8, #9 (write-path enforcement layers) and cube #19 (curator-side visibility layer). The identity-cost design pass is closed at the cube level on both modalities. Cubes #10-#18 and #20 opened and joined six non-identity design passes: calibration-density (#10 + #13 pinning the density-floor scaling law at two resolutions, #16 closing the corpus-composition contamination axis), pool-size (#11, graceful-degradation reading on the cluster-signal closure), vote-aggregation-thresholds (#12, threshold-axis decomposition reading on the K+1-honest dynamic), the (stratification × threshold) composition (#14, redundant-sufficiency reading on the two K=2 capture-closure mechanisms), reputation-decay rates (#15 + #17, ratio-invariance reading on the recent and demonstrated W/T closure axes), the (decay × cluster-signal) composition (#18, gate-design-role-separation reading on the multi-round timed-coalition attack), and the difficulty-aware (alpha) re-baseline on two closure mechanisms (#5 the recent-gate closure on the patient archetype, #20 the stratification closure on the coalition archetype). The corpus-contamination dynamic cube #13's runner avoided is wired as its own standalone scenario, pinning the order-dependent intra-run bypass of the aware closure that PRD §Calibration batches names. The parameter-sweep cube layer now covers every named axis in PRD §What's deliberately not specified (calibration ratios, reputation formula constants including decay rates, reviewer pool sizes, vote-aggregation thresholds) plus the difficulty-normalization and identity-cost axes; the natural follow-up is whichever axis the next governance-change touches — the cube template is the regression handle each new knob lands against. The other open thread is the deep loop: the model-backed `runLlmAgent` engine is wired, the honest-strong and patient-adversary roles are `LlmRole` configs CI-exercised through the scripted-model path, the scripted-archetype population baseline is CI-pinned (`population-loop.test.ts`), and the deep-loop runner with the contested item the patient adversary's drift half needs now exists (`run-deep-loop.ts`, runnable on demand via the `deep-loop` workflow) — with the full calibration defense active (corpus seeded on dedicated calibration anchors, `calibration_inject_every_n`=2 salting review assignments, `calibration_aware_convergence` on so a calibration-burned vote can't move the contested convergence) and a per-contributor calibration record in the final report. The record/replay cassette layer (`recordingFetch`, `cassette-file.ts`) is wired, all three runners accept it, the record→replay round-trip is CI-tested through `runLlmAgent` + a wired server (`cassette-replay.test.ts`), and checked-in golden cassettes of real Haiku-4.5 runs replay deterministically in CI for both the single-agent honest-strong `run-live` run (`golden-cassette.test.ts`) and a `ci`-preset deep-loop population run (`golden-deep-loop.test.ts`). The model-backed roles are joined to the parameter-sweep *cube* layer as `run-deep-loop-cube.ts` (one cell per swept-parameter setting, each its own cassette, replayed in CI by `golden-deep-loop-cube.test.ts`) — four axes today: calibration defense on/off, adversary role (a strategic-adversary cell), contested-item severity (a `borderline-contested` cell that recorded the cube's first closure failure on the escalation path, plus a `borderline-surrogate` cell that runs a second structurally different borderline drift — surrogate-substitution: source describes ctDNA clearance as a prognostic marker, claim restates it as a therapeutic target — under the same bare v0 closure stack with the v0 escalation path closing the drift cleanly, plus a `borderline-subgroup` cell that runs a third — generalization-from-subgroup: source reports an effect confined to MMR-deficient stage III colon cancer, claim drops the subgroup qualifier and generalizes across resected CRC — with the v0 auto-close-reject path closing the drift cleanly on a 3-0 tally; two of three drift kinds the v0 stack handles, mapping a drift-pattern landscape, with two further sampling rollouts of the verb-swap cell — `borderline-contested-r2` and `borderline-contested-r3`, identical opts only-the-cassette-differs — resolving the original v0 verb-swap break's sampling-variance question: 2/3 sampled rollouts fail v0 via *two distinct paths* (escalation accept-on-tie at 1-1-1; auto-close-accept at >=2 accepts — the latter proving the auto-close failure path is not a v1/v2 artifact but already reachable on the bare v0 stack), 1/3 holds, both failure paths closed by v3), and closure-stack version (a `borderline-contested-v1` cell with `escalation_revise_counts_as_reject` on, a `borderline-contested-v2` cell with both v1 escalation knobs on that recorded a **second** closure failure at the auto-close-accept path the v1 knobs don't reach, and a `borderline-contested-v3` cell adding `contested_votes_to_accept` on top — the first closure-stack knob to touch the auto-close-accept path, closing the v2 cell's failure on both paths — with the harness pairs in `population-loop.test.ts` pinning the load-bearing v0/v1, v0/v2, and v0/v3 deltas byte-for-byte). The v3 closure-stack knob against the v2 cell's finding is `ReviewConfig.contested_votes_to_accept` (default `0`, inert): when > 0 and the proposal has accumulated any reject or revise vote, the auto-close-accept threshold is raised from `votes_to_accept` to this value on both the auto-close path (`resolveByConvergence`) and the escalation-to-accept path (`escalateStuckProposals` in `population-loop.ts`). The first closure-stack knob to touch the auto-close-accept path — v1/v2 govern only the curator tiebreak — and the one that closes the v2 cube cell's auto-close-path failure on both paths: holds 2-accept-1-revise off the auto-close threshold (`acceptCount=2 < contested_votes_to_accept=3`), then rejects it at escalation (`a=2 < contested_votes_to_accept=3` with dissent present). Self-contained: composes with v1/v2 but does not require either. The load-bearing v0/v3 delta is pinned byte-for-byte by a scripted-decider pair in `population-loop.test.ts`: same 3-reviewer accept/revise/accept sequential setup, knob inert versus `contested_votes_to_accept: 3` — under v0 every excerpt auto-closes accept on the second accept vote (0 escalations, every excerpt materialized as a node); under v3 every excerpt is held off the auto-close path and the between-rounds curator pass escalates each reject (0 excerpt nodes materialized, every proposal `rejected`). The model-backed `borderline-contested-v3` cube cell is the v3 closure stack's real-model regression baseline on the borderline ctDNA-MRD scenario: the recorded rollout exercises v3 directly — strategic adversary drift accept + a confused honest accept + an honest reject (a 2-accept-1-reject-0-revise tally), which under v2 would auto-close accept at `votes_to_accept=2` but under v3 is held off the auto-close threshold (`acceptCount=2 < contested_votes_to_accept=3` with dissent present) and escalates reject (`a=2 < contested_votes_to_accept=3` at escalation). The contested overstated claim ends `rejected`. What's still open: broadening the cube further (more borderline items varying the kind of overstatement, more closure-stack cells exploring orthogonal auto-close rules like auto-close-aware revise counting or held-on-revise-tie); and the *scheduled* CI cadence (a `schedule:` trigger on the deep-loop workflow once an API key is in repo secrets). Live-fetch verification (PMID/DOI resolution) landed as Phase 2 slice 1 (paragraph above; `LiveFetchVerifier` in `packages/server/src/live-fetch-verifier.ts`); URL anchors are refused by default in v0 per PRD §Verification engine. Span verification is wired at the verifier seam and exercised by adversary scenarios.
